---
id: uploading
title: "uploading"
sidebar_label: "uploading"
description: "Documentation for uploading reactors"
api: {"tags":["Engine Execution"],"description":"Executes a Pixel expression and returns the full result in the response.","operationId":"runPixelSync","x-codeSamples":[{"lang":"JavaScript","label":"SEMOSS SDK","source":"const { actions } = useInsight();\nconst ask = (question) => {\nconst { pixelReturn } = await actions.run(\n    ReactorName(key1=[<key1>], key2=[<key2>]);\n\n// get the message\nconst message = pixelReturn[0].output.response;\nconsole.log(message);\n};\n"},{"lang":"Python","label":"SEMOSS SDK","source":"from semoss import Insight\ninsight = Insight(insight_id = '${i}')\ninsight.run_pixel(\"ReactorName(key1=[<key1>], key2=[<key2>])\")\n"}],"requestBody":{"content":{"application/x-www-form-urlencoded":{"schema":{"required":["expression"],"type":"object","properties":{"dropLogging":{"type":"boolean","description":"If true, drop console logging after run","example":true},"expression":{"type":"string","description":"expression=ReactorName(key1=[<key1>], key2=[<key2>])","example":"expression=ReactorName(key1=[<key1>], key2=[<key2>])"},"insightId":{"type":"string","description":"Insight identifier. Use 'new' to create a new insight or omit for temporary.","example":"bf235853-86b7-483f-a0a5-ae489bfed97b"},"tz":{"type":"string","description":"Client timezone ID","example":"America/New_York"}},"title":"RunPixelRequest"}}},"required":true},"responses":{"200":{"description":"Pixel executed","content":{"application/json":{"schema":{"type":"object","example":{"status":"success","data":{"result":"Pixel execution output"}}},"examples":{"Sample Response":{"description":"Sample Response","value":{"status":"success","data":{"result":"Pixel execution output"}}}}}}},"400":{"description":"Invalid request","content":{"application/json":{}}},"401":{"description":"Unauthorized","content":{"application/json":{}}}},"security":[{"basicAuth":[]}],"method":"post","path":"/engine/runPixel","servers":[{"url":"http://localhost:8080/dev-monolith.com","description":"Demo Endpoint URL"},{"url":"http://localhost:8080/Monolith/api","description":"Local Development"}],"securitySchemes":{"basicAuth":{"type":"http","scheme":"basic"}},"info":{"title":"SEMOSS Core","version":"0.0.1-SNAPSHOT"},"postman":{"name":"Execute Pixel synchronously","description":{"content":"Executes a Pixel expression and returns the full result in the response.","type":"text/plain"},"url":{"path":["engine","runPixel"],"host":["{{baseUrl}}"],"query":[],"variable":[]},"header":[{"key":"Content-Type","value":"application/x-www-form-urlencoded"},{"key":"Accept","value":"application/json"}],"method":"POST","body":{"mode":"urlencoded","urlencoded":[]},"auth":{"type":"basic","basic":[{"type":"any","value":"{{basicAuthUsername}}","key":"username"},{"type":"any","value":"{{basicAuthPassword}}","key":"password"}]}}}

---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# uploading

This document contains multiple reactors in the uploading category.

---

## RdbmsReplaceDatabaseExcelUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**

### Usage
```javascript
RdbmsReplaceDatabaseExcelUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>])
```


---

## RdbmsCsvUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **createIndex**

### Usage
```javascript
RdbmsCsvUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], createIndex=[<createIndex>])
```


---

## RdbmsUploadTableData

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**
- **replace**

### Usage
```javascript
RdbmsUploadTableData(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>], replace=[<replace>])
```


---

## TinkerCsvUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **tinkerDriver**

### Usage
```javascript
TinkerCsvUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], tinkerDriver=[<tinkerDriver>])
```


---

## RdbmsReplaceDatabaseUploadTable

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**

### Usage
```javascript
RdbmsReplaceDatabaseUploadTable(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>])
```


---

## RdbmsExternalUpload

### Required Keys
- **conDetails**
- **database**
- **metamodel**
- **existing**

### Usage
```javascript
RdbmsExternalUpload(conDetails=[<conDetails>], database=[<database>], metamodel=[<metamodel>], existing=[<existing>])
```


---

## ExternalJdbcSchema

### Required Keys
- **conDetails**
- **filters**

### Usage
```javascript
ExternalJdbcSchema(conDetails=[<conDetails>], filters=[<filters>])
```


---

## TinkerReplaceDatabaseCsvUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **tinkerDriver**

### Usage
```javascript
TinkerReplaceDatabaseCsvUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], tinkerDriver=[<tinkerDriver>])
```


---

## RdfLoaderSheetUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **customBaseURI**

### Usage
```javascript
RdfLoaderSheetUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], customBaseURI=[<customBaseURI>])
```


---

## RdfReplaceDatabaseLoaderSheetUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **customBaseURI**

### Usage
```javascript
RdfReplaceDatabaseLoaderSheetUpload(database=[<database>], filePath=[<filePath>], space=[<space>], customBaseURI=[<customBaseURI>])
```


---

## RdfCsvUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **customBaseURI**

### Usage
```javascript
RdfCsvUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], customBaseURI=[<customBaseURI>])
```


---

## RdbmsReplaceDatabaseCsvUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **createIndex**

### Usage
```javascript
RdbmsReplaceDatabaseCsvUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], createIndex=[<createIndex>])
```


---

## ExternalJdbcTablesAndViews

### Required Keys
- **conDetails**

### Usage
```javascript
ExternalJdbcTablesAndViews(conDetails=[<conDetails>])
```


---

## RdbmsUploadExcelData

### Required Keys
- **database**
- **filePath**
- **existing**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**
- **replace**

### Usage
```javascript
RdbmsUploadExcelData(database=[<database>], filePath=[<filePath>], existing=[<existing>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>], replace=[<replace>])
```


---

## RdbmsLoaderSheetUpload

### Required Keys
- **database**
- **filePath**

### Usage
```javascript
RdbmsLoaderSheetUpload(database=[<database>], filePath=[<filePath>])
```


---

## RdbmsReplaceDatabaseLoaderSheetUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**

### Usage
```javascript
RdbmsReplaceDatabaseLoaderSheetUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>])
```

