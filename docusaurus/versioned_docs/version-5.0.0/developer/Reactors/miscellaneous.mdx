---
id: Miscellaneous
title: "Miscellaneous"
sidebar_label: "Miscellaneous"
description: "Documentation for Miscellaneous reactors"
api: {"tags":["Engine Execution"],"description":"Executes a Pixel expression and returns the full result in the response.","operationId":"runPixelSync","x-codeSamples":[{"lang":"JavaScript","label":"SEMOSS SDK","source":"const { actions } = useInsight();\nconst ask = (question) => {\nconst { pixelReturn } = await actions.run(\n    ReactorName(key1=[<key1>], key2=[<key2>]);\n\n// get the message\nconst message = pixelReturn[0].output.response;\nconsole.log(message);\n};\n"},{"lang":"Python","label":"SEMOSS SDK","source":"from semoss import Insight\ninsight = Insight(insight_id = '${i}')\ninsight.run_pixel(\"ReactorName(key1=[<key1>], key2=[<key2>])\")\n"}],"requestBody":{"content":{"application/x-www-form-urlencoded":{"schema":{"required":["expression"],"type":"object","properties":{"dropLogging":{"type":"boolean","description":"If true, drop console logging after run","example":true},"expression":{"type":"string","description":"expression=ReactorName(key1=[<key1>], key2=[<key2>])","example":"expression=ReactorName(key1=[<key1>], key2=[<key2>])"},"insightId":{"type":"string","description":"Insight identifier. Use 'new' to create a new insight or omit for temporary.","example":"bf235853-86b7-483f-a0a5-ae489bfed97b"},"tz":{"type":"string","description":"Client timezone ID","example":"America/New_York"}},"title":"RunPixelRequest"}}},"required":true},"responses":{"200":{"description":"Pixel executed","content":{"application/json":{"schema":{"type":"object","example":{"status":"success","data":{"result":"Pixel execution output"}}},"examples":{"Sample Response":{"description":"Sample Response","value":{"status":"success","data":{"result":"Pixel execution output"}}}}}}},"400":{"description":"Invalid request","content":{"application/json":{}}},"401":{"description":"Unauthorized","content":{"application/json":{}}}},"security":[{"basicAuth":[]}],"method":"post","path":"/engine/runPixel","servers":[{"url":"http://localhost:8080/dev-monolith.com","description":"Versioned API Endpoint"},{"url":"http://localhost:8080/Monolith/api","description":"Local Development"}],"securitySchemes":{"basicAuth":{"type":"http","scheme":"basic"}},"info":{"title":"SEMOSSName","version":"0.0.1-SNAPSHOT"},"postman":{"name":"Execute Pixel synchronously","description":{"content":"Executes a Pixel expression and returns the full result in the response.","type":"text/plain"},"url":{"path":["engine","runPixel"],"host":["{{baseUrl}}"],"query":[],"variable":[]},"header":[{"key":"Content-Type","value":"application/x-www-form-urlencoded"},{"key":"Accept","value":"application/json"}],"method":"POST","body":{"mode":"urlencoded","urlencoded":[]},"auth":{"type":"basic","basic":[{"type":"any","value":"{{basicAuthUsername}}","key":"username"},{"type":"any","value":"{{basicAuthPassword}}","key":"password"}]}}}

---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Miscellaneous

This document contains multiple reactors in the Miscellaneous category.

---

## Assimilator

### Required Keys
- **no keys defined**

### Usage
```javascript
Assimilator()
```


---

## ListStoragePathDetails

### Required Keys
- **storage**
- **storagePath**

### Usage
```javascript
ListStoragePathDetails(storage=[<storage>], storagePath=[<storagePath>])
```


---

## OpMedian

### Required Keys
- **numValues**

### Usage
```javascript
OpMedian(numValues=[<numValues>])
```


---

## SetPanelPosition

### Required Keys
- **panel**
- **position**

### Usage
```javascript
SetPanelPosition(panel=[<panel>], position=[<position>])
```


---

## DropInsight

### Required Keys
- **no keys defined**

### Usage
```javascript
DropInsight()
```


---

## GraphPlan

### Required Keys
- **planner**

### Usage
```javascript
GraphPlan(planner=[<planner>])
```


---

## RemoveOwlConcept

### Required Keys
- **database**
- **concept**

### Usage
```javascript
RemoveOwlConcept(database=[<database>], concept=[<concept>])
```


---

## UpdateAsset

### Required Keys
- **repository**
- **fileName**
- **content**

### Usage
```javascript
UpdateAsset(repository=[<repository>], fileName=[<fileName>], content=[<content>])
```


---

## Year

### Required Keys
- **years**

### Usage
```javascript
Year(years=[<years>])
```


---

## CreateStorageEngine

### Required Keys
- **storage**
- **storageDetails**
- **global**

### Usage
```javascript
CreateStorageEngine(storage=[<storage>], storageDetails=[<storageDetails>], global=[<global>])
```


---

## PullFromStorage

### Required Keys
- **storage**
- **storagePath**
- **space**
- **filePath**

### Usage
```javascript
PullFromStorage(storage=[<storage>], storagePath=[<storagePath>], space=[<space>], filePath=[<filePath>])
```


---

## GetPanelFilters

### Required Keys
- **panel**

### Usage
```javascript
GetPanelFilters(panel=[<panel>])
```


---

## GetFunctionEngineDefintion

### Required Keys
- **engine**

### Usage
```javascript
GetFunctionEngineDefintion(engine=[<engine>])
```


---

## AdminProjectInfo

### Required Keys
- **project**
- **metaKeys**

### Usage
```javascript
AdminProjectInfo(project=[<project>], metaKeys=[<metaKeys>])
```


---

## ClosePanel

### Required Keys
- **panel**

### Usage
```javascript
ClosePanel(panel=[<panel>])
```


---

## CacheNativeFrame

### Required Keys
- **frame**
- **frameType**
- **inThread**

### Usage
```javascript
CacheNativeFrame(frame=[<frame>], frameType=[<frameType>], inThread=[<inThread>])
```


---

## RunParameterRecipe

### Required Keys
- **recipe**
- **fill**

### Usage
```javascript
RunParameterRecipe(recipe=[<recipe>], fill=[<fill>])
```


---

## ExportDatabase

### Required Keys
- **database**
- **keepGit**

### Usage
```javascript
ExportDatabase(database=[<database>], keepGit=[<keepGit>])
```


---

## NativeCollectNewCol

### Required Keys
- **qs**

### Usage
```javascript
NativeCollectNewCol(qs=[<qs>])
```


---

## GetConsolidateUIState

### Required Keys
- **output**

### Usage
```javascript
GetConsolidateUIState(output=[<output>])
```


---

## ExternalDatabaseProfile

### Required Keys
- **dbDriver**
- **host**
- **port**
- **username**
- **password**
- **schema**

### Usage
```javascript
ExternalDatabaseProfile(dbDriver=[<dbDriver>], host=[<host>], port=[<port>], username=[<username>], password=[<password>], schema=[<schema>])
```


---

## PredictParquetDataTypes

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
PredictParquetDataTypes(filePath=[<filePath>], space=[<space>])
```


---

## RenameAsset

**Description:** 

        Rename a file or directory

### Required Keys
- **filePath**
- **newValue**

### Optional Keys
- **comment**
- **space**

### Usage
```javascript
RenameAsset(filePath=[<filePath>], newValue=[<newValue>], comment=[<comment>], space=[<space>])
```


---

## GetScreenshot

**Description:** 

        Get the current screenshot of the Browser App rendered on the server.

### Required Keys
- **no keys defined**

### Usage
```javascript
GetScreenshot()
```


---

## DatabaseProfile

### Required Keys
- **frame**
- **database**
- **concepts**

### Usage
```javascript
DatabaseProfile(frame=[<frame>], database=[<database>], concepts=[<concepts>])
```


---

## GetInsightUserAccessRequest

### Required Keys
- **project**
- **id**

### Usage
```javascript
GetInsightUserAccessRequest(project=[<project>], id=[<id>])
```


---

## GetMCPResources

### Required Keys
- **project**

### Usage
```javascript
GetMCPResources(project=[<project>])
```


---

## SetInsightName

### Required Keys
- **project**
- **insightName**
- **id**

### Usage
```javascript
SetInsightName(project=[<project>], insightName=[<insightName>], id=[<id>])
```


---

## CommitAsset

### Required Keys
- **filePath**
- **comment**
- **space**

### Usage
```javascript
CommitAsset(filePath=[<filePath>], comment=[<comment>], space=[<space>])
```


---

## TaskOptions

### Required Keys
- **options**
- **ignoreFilters**

### Usage
```javascript
TaskOptions(options=[<options>], ignoreFilters=[<ignoreFilters>])
```


---

## AddAppCollaborator

### Required Keys
- **repository**
- **collaborator**
- **username**
- **password**

### Usage
```javascript
AddAppCollaborator(repository=[<repository>], collaborator=[<collaborator>], username=[<username>], password=[<password>])
```


---

## ClearFilterModelStateCache

### Usage
```javascript
ClearFilterModelStateCache()
```


---

## ParentValidator

### Required Keys
- **no keys defined**

### Usage
```javascript
ParentValidator()
```


---

## bq

### Required Keys
- **fancy**

### Usage
```javascript
bq(fancy=[<fancy>])
```


---

## FillInput

**Description:** 

        Fill the currently selected input of the Browser App rendered on the server.

### Required Keys
- **input**

### Usage
```javascript
FillInput(input=[<input>])
```


---

## As

### Required Keys
- **no keys defined**

### Usage
```javascript
As()
```


---

## R

### Required Keys
- **no keys defined**

### Usage
```javascript
R()
```


---

## GetProjectList

### Required Keys
- **project**

### Usage
```javascript
GetProjectList(project=[<project>])
```


---

## CtrlXAsset

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
CtrlXAsset(filePath=[<filePath>], space=[<space>])
```


---

## GenerateXRayHashingFrame

### Required Keys
- **filePath**
- **space**
- **frame**
- **override**
- **config**
- **rowComparison**

### Usage
```javascript
GenerateXRayHashingFrame(filePath=[<filePath>], space=[<space>], frame=[<frame>], override=[<override>], config=[<config>], rowComparison=[<rowComparison>])
```


---

## EnginePy

### Required Keys
- **code**
- **engine**

### Usage
```javascript
EnginePy(code=[<code>], engine=[<engine>])
```


---

## SetOpenInsightParamValue

### Required Keys
- **paramMap**

### Usage
```javascript
SetOpenInsightParamValue(paramMap=[<paramMap>])
```


---

## AddInsightAPI

### Required Keys
- **project**
- **id**
- **password**
- **consumer**

### Optional Keys
- **expires**
- **limit**

### Usage
```javascript
AddInsightAPI(project=[<project>], id=[<id>], password=[<password>], consumer=[<consumer>], expires=[<expires>], limit=[<limit>])
```


---

## GetDatabaseMetadata

### Required Keys
- **database**
- **metaKeys**

### Usage
```javascript
GetDatabaseMetadata(database=[<database>], metaKeys=[<metaKeys>])
```


---

## CreateVenvEngine

### Required Keys
- **venv**
- **venvDetails**

### Optional Keys
- **global**

### Usage
```javascript
CreateVenvEngine(venv=[<venv>], venvDetails=[<venvDetails>], global=[<global>])
```


---

## RdbmsCsvUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **createIndex**

### Usage
```javascript
RdbmsCsvUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], createIndex=[<createIndex>])
```


---

## ScheduleJob

### Required Keys
- **jobName**
- **jobGroup**
- **cronExpression**
- **cronTz**
- **recipe**
- **recipeParameters**
- **triggerOnLoad**
- **triggerNow**
- **uiState**
- **jobTags**

### Usage
```javascript
ScheduleJob(jobName=[<jobName>], jobGroup=[<jobGroup>], cronExpression=[<cronExpression>], cronTz=[<cronTz>], recipe=[<recipe>], recipeParameters=[<recipeParameters>], triggerOnLoad=[<triggerOnLoad>], triggerNow=[<triggerNow>], uiState=[<uiState>], jobTags=[<jobTags>])
```


---

## GetEngineMetadata

### Required Keys
- **engine**
- **metaKeys**

### Usage
```javascript
GetEngineMetadata(engine=[<engine>], metaKeys=[<metaKeys>])
```


---

## LSASpaceColumnLearned

### Required Keys
- **column**

### Usage
```javascript
LSASpaceColumnLearned(column=[<column>])
```


---

## ListInsightAPI

### Required Keys
- **project**
- **id**

### Usage
```javascript
ListInsightAPI(project=[<project>], id=[<id>])
```


---

## LLMInstruct

### Required Keys
- **engine**
- **command**

### Optional Keys
- **paramValues**
- **context**

### Usage
```javascript
LLMInstruct(engine=[<engine>], command=[<command>], paramValues=[<paramValues>], context=[<context>])
```


---

## GetPromptMetaValues

### Required Keys
- **metaKeys**

### Usage
```javascript
GetPromptMetaValues(metaKeys=[<metaKeys>])
```


---

## OpMean

### Required Keys
- **numValues**

### Usage
```javascript
OpMean(numValues=[<numValues>])
```


---

## GetWorkspaceRooms

### Required Keys
- **workspaceId**

### Optional Keys
- **offset**
- **limit**
- **filters**
- **sort**

### Usage
```javascript
GetWorkspaceRooms(workspaceId=[<workspaceId>], offset=[<offset>], limit=[<limit>], filters=[<filters>], sort=[<sort>])
```


---

## OpAsString

### Required Keys
- **values**

### Usage
```javascript
OpAsString(values=[<values>])
```


---

## GetProjectMarkdown

### Required Keys
- **project**

### Usage
```javascript
GetProjectMarkdown(project=[<project>])
```


---

## VectorDatabaseQuery

### Required Keys
- **engine**
- **command**

### Optional Keys
- **paramValues**
- **metaFilters**
- **limit**
- **filters**

### Usage
```javascript
VectorDatabaseQuery(engine=[<engine>], command=[<command>], paramValues=[<paramValues>], metaFilters=[<metaFilters>], limit=[<limit>], filters=[<filters>])
```


---

## ExportRunPixelTaskDataToFile

**Description:** 

        This reactor is used to flush a string in the format of the JSON response for the runPixel endpoint. It will grab a specific index of the pixel return (or assume last index) to be in the task data format and write that to a file for the user to download

### Required Keys
- **value**
- **index**
- **fileName**
- **deleteAfterInsightClose**

### Usage
```javascript
ExportRunPixelTaskDataToFile(value=[<value>], index=[<index>], fileName=[<fileName>], deleteAfterInsightClose=[<deleteAfterInsightClose>])
```


---

## PullEngineFromCloud

### Required Keys
- **engine**

### Usage
```javascript
PullEngineFromCloud(engine=[<engine>])
```


---

## OpenProject

### Required Keys
- **project**

### Usage
```javascript
OpenProject(project=[<project>])
```


---

## EditDatabasePropertyDataType

### Required Keys
- **database**
- **concept**
- **column**
- **dataType**

### Usage
```javascript
EditDatabasePropertyDataType(database=[<database>], concept=[<concept>], column=[<column>], dataType=[<dataType>])
```


---

## SyncAppFiles

### Required Keys
- **database**
- **repository**
- **username**
- **password**
- **dual**
- **syncDatabase**
- **files**

### Usage
```javascript
SyncAppFiles(database=[<database>], repository=[<repository>], username=[<username>], password=[<password>], dual=[<dual>], syncDatabase=[<syncDatabase>], files=[<files>])
```


---

## AdminGetProjectAvailables

### Required Keys
- **project**

### Usage
```javascript
AdminGetProjectAvailables(project=[<project>])
```


---

## SubqueryJoin

### Required Keys
- **subQs**
- **subQsAlias**
- **on**
- **jType**

### Usage
```javascript
SubqueryJoin(subQs=[<subQs>], subQsAlias=[<subQsAlias>], on=[<on>], jType=[<jType>])
```


---

## RReplaceDatabaseCsvUpload

### Required Keys
- **database**
- **filePath**
- **space**

### Usage
```javascript
RReplaceDatabaseCsvUpload(database=[<database>], filePath=[<filePath>], space=[<space>])
```


---

## GetGuardrailEngineDefintion

### Required Keys
- **engine**

### Usage
```javascript
GetGuardrailEngineDefintion(engine=[<engine>])
```


---

## GetDatabaseConnections

### Required Keys
- **columns**
- **database**

### Usage
```javascript
GetDatabaseConnections(columns=[<columns>], database=[<database>])
```


---

## Quarter

### Required Keys
- **columns**

### Usage
```javascript
Quarter(columns=[<columns>])
```


---

## PySource

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
PySource(filePath=[<filePath>], space=[<space>])
```


---

## ExecuteFunctionEngine

### Required Keys
- **engine**

### Usage
```javascript
ExecuteFunctionEngine(engine=[<engine>])
```


---

## ToPdf

### Required Keys
- **html**
- **filePath**
- **space**
- **outputFilePath**
- **fileName**
- **url**
- **mustache**
- **mustacheVars**
- **pdfSignatureBlock**
- **pdfSignatureLabel**
- **pdfPageNumbers**
- **pdfPageNumbersIgnoreFirst**
- **pdfStartPageNumbers**
- **imageWaitTime**

### Usage
```javascript
ToPdf(html=[<html>], filePath=[<filePath>], space=[<space>], outputFilePath=[<outputFilePath>], fileName=[<fileName>], url=[<url>], mustache=[<mustache>], mustacheVars=[<mustacheVars>], pdfSignatureBlock=[<pdfSignatureBlock>], pdfSignatureLabel=[<pdfSignatureLabel>], pdfPageNumbers=[<pdfPageNumbers>], pdfPageNumbersIgnoreFirst=[<pdfPageNumbersIgnoreFirst>], pdfStartPageNumbers=[<pdfStartPageNumbers>], imageWaitTime=[<imageWaitTime>])
```


---

## ReadInsightTheme

### Required Keys
- **no keys defined**

### Usage
```javascript
ReadInsightTheme()
```


---

## MyDatabases

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **onlyFavorites**
- **database**
- **permissionFilters**
- **metaKeys**
- **metaFilters**
- **noMeta**
- **userT**

### Usage
```javascript
MyDatabases(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], onlyFavorites=[<onlyFavorites>], database=[<database>], permissionFilters=[<permissionFilters>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], noMeta=[<noMeta>], userT=[<userT>])
```


---

## CloseURL

**Description:** 

        Close the URL of the Browser App rendered on the server.

### Optional Keys
- **url**

### Usage
```javascript
CloseURL(url=[<url>])
```


---

## LoggingInput

### Required Keys
- **arguments**

### Usage
```javascript
LoggingInput(arguments=[<arguments>])
```


---

## RegexFilter

### Required Keys
- **columns**

### Usage
```javascript
RegexFilter(columns=[<columns>])
```


---

## IfQueryFilterComponentAnd

### Required Keys
- **filters**

### Usage
```javascript
IfQueryFilterComponentAnd(filters=[<filters>])
```


---

## GetEngineMetakeyOptions

### Required Keys
- **metaKeys**

### Usage
```javascript
GetEngineMetakeyOptions(metaKeys=[<metaKeys>])
```


---

## SetInsightTheme

### Required Keys
- **no keys defined**

### Usage
```javascript
SetInsightTheme()
```


---

## RefreshPanelTask

### Required Keys
- **panel**
- **limit**

### Usage
```javascript
RefreshPanelTask(panel=[<panel>], limit=[<limit>])
```


---

## OpenEmptyInsight

### Required Keys
- **recipe**
- **params**

### Usage
```javascript
OpenEmptyInsight(recipe=[<recipe>], params=[<params>])
```


---

## RunImpliedInsights

### Usage
```javascript
RunImpliedInsights()
```


---

## GenerateXRayHashing

### Required Keys
- **filePath**
- **space**
- **database**
- **override**
- **config**
- **rowComparison**

### Usage
```javascript
GenerateXRayHashing(filePath=[<filePath>], space=[<space>], database=[<database>], override=[<override>], config=[<config>], rowComparison=[<rowComparison>])
```


---

## OneDriveFileRetriever

### Required Keys
- **id**

### Usage
```javascript
OneDriveFileRetriever(id=[<id>])
```


---

## RunOutlier

### Required Keys
- **instance**
- **subsetSize**
- **numRuns**
- **attributes**

### Usage
```javascript
RunOutlier(instance=[<instance>], subsetSize=[<subsetSize>], numRuns=[<numRuns>], attributes=[<attributes>])
```


---

## OpAnd

### Required Keys
- **values**

### Usage
```javascript
OpAnd(values=[<values>])
```


---

## SyncStorageToLocal

### Required Keys
- **storage**
- **storagePath**
- **space**
- **filePath**

### Usage
```javascript
SyncStorageToLocal(storage=[<storage>], storagePath=[<storagePath>], space=[<space>], filePath=[<filePath>])
```


---

## GitStatus

### Required Keys
- **database**

### Usage
```javascript
GitStatus(database=[<database>])
```


---

## SetInsightCacheable

### Required Keys
- **project**
- **id**
- **cache**
- **cacheMinutes**
- **cacheCron**
- **cacheEncrypt**

### Usage
```javascript
SetInsightCacheable(project=[<project>], id=[<id>], cache=[<cache>], cacheMinutes=[<cacheMinutes>], cacheCron=[<cacheCron>], cacheEncrypt=[<cacheEncrypt>])
```


---

## GetProjectMetadata

### Required Keys
- **project**
- **metaKeys**

### Usage
```javascript
GetProjectMetadata(project=[<project>], metaKeys=[<metaKeys>])
```


---

## CurrentVariables

### Required Keys
- **no keys defined**

### Usage
```javascript
CurrentVariables()
```


---

## CreateNLPViz

### Required Keys
- **database**
- **columns**
- **sortPixel**
- **frame**
- **panel**
- **vizSelection**

### Usage
```javascript
CreateNLPViz(database=[<database>], columns=[<columns>], sortPixel=[<sortPixel>], frame=[<frame>], panel=[<panel>], vizSelection=[<vizSelection>])
```


---

## InsightRecipe

### Required Keys
- **project**
- **id**
- **vec**

### Usage
```javascript
InsightRecipe(project=[<project>], id=[<id>], vec=[<vec>])
```


---

## MyEngineProject

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **onlyFavorites**
- **type**
- **subType**
- **engine**
- **metaKeys**
- **metaFilters**
- **permissionFilters**
- **noMeta**
- **onlyPortals**

### Usage
```javascript
MyEngineProject(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], onlyFavorites=[<onlyFavorites>], type=[<type>], subType=[<subType>], engine=[<engine>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], permissionFilters=[<permissionFilters>], noMeta=[<noMeta>], onlyPortals=[<onlyPortals>])
```


---

## GetShareAuthToken

### Required Keys
- **no keys defined**

### Usage
```javascript
GetShareAuthToken()
```


---

## PushInsightFolderToCloud

### Required Keys
- **project**
- **id**

### Usage
```javascript
PushInsightFolderToCloud(project=[<project>], id=[<id>])
```


---

## IsInsightParameterized

### Required Keys
- **project**
- **id**

### Usage
```javascript
IsInsightParameterized(project=[<project>], id=[<id>])
```


---

## InsightPixelList

### Required Keys
- **no keys defined**

### Usage
```javascript
InsightPixelList()
```


---

## Coalesce

### Required Keys
- **columns**

### Usage
```javascript
Coalesce(columns=[<columns>])
```


---

## EmptyData

### Required Keys
- **value**

### Usage
```javascript
EmptyData(value=[<value>])
```


---

## ExportImage

### Required Keys
- **baseUrl**
- **url**
- **fileName**
- **filePath**
- **imageWaitTime**

### Usage
```javascript
ExportImage(baseUrl=[<baseUrl>], url=[<url>], fileName=[<fileName>], filePath=[<filePath>], imageWaitTime=[<imageWaitTime>])
```


---

## Storage

### Required Keys
- **storage**

### Usage
```javascript
Storage(storage=[<storage>])
```


---

## PredictToolArguments

### Required Keys
- **engine**
- **toolName**

### Optional Keys
- **toolMeta**
- **stepId**
- **context**
- **roomId**

### Usage
```javascript
PredictToolArguments(engine=[<engine>], toolName=[<toolName>], toolMeta=[<toolMeta>], stepId=[<stepId>], context=[<context>], roomId=[<roomId>])
```


---

## CollectPivot

### Required Keys
- **rowGroups**
- **columns**
- **values**
- **frameType**

### Usage
```javascript
CollectPivot(rowGroups=[<rowGroups>], columns=[<columns>], values=[<values>], frameType=[<frameType>])
```


---

## CreateEmbeddingsFromDocuments

### Required Keys
- **engine**
- **filePaths**

### Optional Keys
- **paramValues**
- **space**

### Usage
```javascript
CreateEmbeddingsFromDocuments(engine=[<engine>], filePaths=[<filePaths>], paramValues=[<paramValues>], space=[<space>])
```


---

## FrameFilterModelVisibleValues

### Required Keys
- **column**
- **filterWord**
- **limit**
- **offset**
- **panel**

### Usage
```javascript
FrameFilterModelVisibleValues(column=[<column>], filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], panel=[<panel>])
```


---

## Vision

**Description:** 

        This method is used to run a vision task with an Image-Text-to-Text model

### Required Keys
- **engine**
- **command**
- **image**

### Optional Keys
- **paramValues**

### Usage
```javascript
Vision(engine=[<engine>], command=[<command>], image=[<image>], paramValues=[<paramValues>])
```


---

## PixelSource

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
PixelSource(filePath=[<filePath>], space=[<space>])
```


---

## RdbmsLoaderSheetUpload

### Required Keys
- **database**
- **filePath**

### Usage
```javascript
RdbmsLoaderSheetUpload(database=[<database>], filePath=[<filePath>])
```


---

## GenericGuardrailInput

### Usage
```javascript
GenericGuardrailInput()
```


---

## GetProjectAuthorizationHeader

### Required Keys
- **project**

### Usage
```javascript
GetProjectAuthorizationHeader(project=[<project>])
```


---

## GetDSEGraphMetaModel

### Required Keys
- **host**
- **port**
- **username**
- **password**
- **graphName**
- **graphTypeId**
- **useLabel**

### Usage
```javascript
GetDSEGraphMetaModel(host=[<host>], port=[<port>], username=[<username>], password=[<password>], graphName=[<graphName>], graphTypeId=[<graphTypeId>], useLabel=[<useLabel>])
```


---

## FilterModelState

### Required Keys
- **column**
- **filterWord**
- **limit**
- **offset**
- **panel**
- **dynamic**
- **optionsCache**

### Usage
```javascript
FilterModelState(column=[<column>], filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], panel=[<panel>], dynamic=[<dynamic>], optionsCache=[<optionsCache>])
```


---

## EncryptPdf

### Required Keys
- **filePath**
- **space**
- **password**
- **allowPrint**
- **allowCopy**
- **allowModify**
- **allowAssemble**
- **readOnly**

### Usage
```javascript
EncryptPdf(filePath=[<filePath>], space=[<space>], password=[<password>], allowPrint=[<allowPrint>], allowCopy=[<allowCopy>], allowModify=[<allowModify>], allowAssemble=[<allowAssemble>], readOnly=[<readOnly>])
```


---

## QueryFilterComponentSimple

### Required Keys
- **filters**

### Usage
```javascript
QueryFilterComponentSimple(filters=[<filters>])
```


---

## DisableInsightAPI

### Required Keys
- **apikey**

### Usage
```javascript
DisableInsightAPI(apikey=[<apikey>])
```


---

## PushToStorage

### Required Keys
- **storage**
- **storagePath**
- **space**
- **filePath**
- **metadata**

### Usage
```javascript
PushToStorage(storage=[<storage>], storagePath=[<storagePath>], space=[<space>], filePath=[<filePath>], metadata=[<metadata>])
```


---

## DatabaseMetadataToPdf

### Required Keys
- **database**

### Usage
```javascript
DatabaseMetadataToPdf(database=[<database>])
```


---

## GetTableHeader

### Required Keys
- **url**
- **alias_map**

### Usage
```javascript
GetTableHeader(url=[<url>], alias_map=[<alias_map>])
```


---

## RemoteModelShutdown

### Required Keys
- **engine**

### Usage
```javascript
RemoteModelShutdown(engine=[<engine>])
```


---

## OpLen

### Required Keys
- **values**

### Usage
```javascript
OpLen(values=[<values>])
```


---

## RunSimilarity

### Required Keys
- **instance**
- **attributes**

### Usage
```javascript
RunSimilarity(instance=[<instance>], attributes=[<attributes>])
```


---

## RetrieveValue

### Required Keys
- **no keys defined**

### Usage
```javascript
RetrieveValue()
```


---

## GetFrameFiltersQS

### Required Keys
- **no keys defined**

### Usage
```javascript
GetFrameFiltersQS()
```


---

## GetProjectMetakeyOptions

### Required Keys
- **metaKeys**

### Usage
```javascript
GetProjectMetakeyOptions(metaKeys=[<metaKeys>])
```


---

## CreateAppFromTemplate

### Required Keys
- **project**
- **projectTemplate**
- **global**
- **provider**
- **url**

### Usage
```javascript
CreateAppFromTemplate(project=[<project>], projectTemplate=[<projectTemplate>], global=[<global>], provider=[<provider>], url=[<url>])
```


---

## SuperParentValidator

### Required Keys
- **no keys defined**

### Usage
```javascript
SuperParentValidator()
```


---

## ClickXY

**Description:** 

        Click on the x, y coordinate of the Browser App rendered on the server.

### Required Keys
- **x**
- **y**

### Usage
```javascript
ClickXY(x=[<x>], y=[<y>])
```


---

## CheckHeaders

### Required Keys
- **headerMap**

### Usage
```javascript
CheckHeaders(headerMap=[<headerMap>])
```


---

## AddPanelIfAbsent

### Required Keys
- **panel**
- **sheet**

### Usage
```javascript
AddPanelIfAbsent(panel=[<panel>], sheet=[<sheet>])
```


---

## NewAppAssetsFile

**Description:** 

        Create a new empty file in the projects assets folder

### Required Keys
- **project**
- **filePath**

### Optional Keys
- **comment**

### Usage
```javascript
NewAppAssetsFile(project=[<project>], filePath=[<filePath>], comment=[<comment>])
```


---

## EditOwlPropertyConceptualName

### Required Keys
- **database**
- **concept**
- **column**
- **conceptual**

### Usage
```javascript
EditOwlPropertyConceptualName(database=[<database>], concept=[<concept>], column=[<column>], conceptual=[<conceptual>])
```


---

## BrowseAsset

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
BrowseAsset(filePath=[<filePath>], space=[<space>])
```


---

## UniqueCount

### Required Keys
- **columns**

### Usage
```javascript
UniqueCount(columns=[<columns>])
```


---

## GetProjectPOP3EmailSession

### Required Keys
- **project**

### Usage
```javascript
GetProjectPOP3EmailSession(project=[<project>])
```


---

## DownloadAsset

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
DownloadAsset(filePath=[<filePath>], space=[<space>])
```


---

## DeleteDatabase

### Required Keys
- **database**

### Usage
```javascript
DeleteDatabase(database=[<database>])
```


---

## RemovePanelOrnaments

### Required Keys
- **panel**
- **traversal**

### Usage
```javascript
RemovePanelOrnaments(panel=[<panel>], traversal=[<traversal>])
```


---

## CopyInsightPermissions

### Required Keys
- **sourceProject**
- **sourceInsight**
- **targetProject**
- **targetInsight**

### Usage
```javascript
CopyInsightPermissions(sourceProject=[<sourceProject>], sourceInsight=[<sourceInsight>], targetProject=[<targetProject>], targetInsight=[<targetInsight>])
```


---

## MapLambda

### Required Keys
- **lambda**
- **columns**
- **params**

### Usage
```javascript
MapLambda(lambda=[<lambda>], columns=[<columns>], params=[<params>])
```


---

## SetMultiTypePanelSort

### Required Keys
- **panel**
- **sortConfig**

### Usage
```javascript
SetMultiTypePanelSort(panel=[<panel>], sortConfig=[<sortConfig>])
```


---

## RemoveJobFromDB

**Description:** 

        This reactor deletes multiple jobs from both Quartz Scheduler and SMSS_JOB_RECIPES table, ensuring proper permission before deletion.

### Required Keys
- **jobId**
- **jobGroup**

### Usage
```javascript
RemoveJobFromDB(jobId=[<jobId>], jobGroup=[<jobGroup>])
```


---

## UnfilterPanel

### Required Keys
- **panel**
- **columns**
- **taskRefresh**

### Usage
```javascript
UnfilterPanel(panel=[<panel>], columns=[<columns>], taskRefresh=[<taskRefresh>])
```


---

## DeleteAppRepo

### Required Keys
- **database**
- **repository**
- **username**
- **password**

### Usage
```javascript
DeleteAppRepo(database=[<database>], repository=[<repository>], username=[<username>], password=[<password>])
```


---

## AddDatabaseProperty

### Required Keys
- **database**
- **concept**
- **column**
- **dataType**

### Optional Keys
- **logicalNames**
- **description**
- **additionalDataTypes**

### Usage
```javascript
AddDatabaseProperty(database=[<database>], concept=[<concept>], column=[<column>], dataType=[<dataType>], logicalNames=[<logicalNames>], description=[<description>], additionalDataTypes=[<additionalDataTypes>])
```


---

## ProjectPy

### Required Keys
- **code**
- **project**

### Usage
```javascript
ProjectPy(code=[<code>], project=[<project>])
```


---

## MonthName

### Required Keys
- **columns**

### Usage
```javascript
MonthName(columns=[<columns>])
```


---

## Badd

### Required Keys
- **fancy**
- **embed**

### Usage
```javascript
Badd(fancy=[<fancy>], embed=[<embed>])
```


---

## DeleteAsset

**Description:** 

        Delete a single or multiple files in the space's file repository

### Required Keys
- **filePath**
- **space**
- **comment**

### Usage
```javascript
DeleteAsset(filePath=[<filePath>], space=[<space>], comment=[<comment>])
```


---

## UndeterminedPipeline

### Required Keys
- **no keys defined**

### Usage
```javascript
UndeterminedPipeline()
```


---

## MyRemoteModelsStatus

### Required Keys
- **no keys defined**

### Usage
```javascript
MyRemoteModelsStatus()
```


---

## VoteDatabase

### Required Keys
- **database**
- **vote**

### Usage
```javascript
VoteDatabase(database=[<database>], vote=[<vote>])
```


---

## ToTxt

### Required Keys
- **task**
- **delimiter**
- **fileName**
- **filePath**
- **appendTimestamp**

### Usage
```javascript
ToTxt(task=[<task>], delimiter=[<delimiter>], fileName=[<fileName>], filePath=[<filePath>], appendTimestamp=[<appendTimestamp>])
```


---

## Iterate

### Required Keys
- **qs**
- **useFrameFilters**

### Usage
```javascript
Iterate(qs=[<qs>], useFrameFilters=[<useFrameFilters>])
```


---

## RunAssociatedLearning

### Required Keys
- **ruleSide**
- **column**
- **values**
- **attributes**
- **conf**
- **support**
- **lift**
- **panel**

### Usage
```javascript
RunAssociatedLearning(ruleSide=[<ruleSide>], column=[<column>], values=[<values>], attributes=[<attributes>], conf=[<conf>], support=[<support>], lift=[<lift>], panel=[<panel>])
```


---

## UploadApp

**Description:** 

        Import an app from an exported .smss-app file

### Required Keys
- **filePath**
- **space**
- **global**

### Usage
```javascript
UploadApp(filePath=[<filePath>], space=[<space>], global=[<global>])
```


---

## CollectGGPlot

### Required Keys
- **ggplot**
- **format**

### Usage
```javascript
CollectGGPlot(ggplot=[<ggplot>], format=[<format>])
```


---

## MooseChat

### Required Keys
- **command**

### Optional Keys
- **paramValues**
- **context**
- **project**
- **model**
- **contentLength**
- **MOOSE_ENDPOINT**

### Usage
```javascript
MooseChat(command=[<command>], paramValues=[<paramValues>], context=[<context>], project=[<project>], model=[<model>], contentLength=[<contentLength>], MOOSE_ENDPOINT=[<MOOSE_ENDPOINT>])
```


---

## Assignment

### Required Keys
- **no keys defined**

### Usage
```javascript
Assignment()
```


---

## UnfilterFrame

### Required Keys
- **columns**
- **taskRefresh**

### Usage
```javascript
UnfilterFrame(columns=[<columns>], taskRefresh=[<taskRefresh>])
```


---

## GetFrameMetamodel

### Required Keys
- **frame**

### Usage
```javascript
GetFrameMetamodel(frame=[<frame>])
```


---

## Asset

### Required Keys
- **no keys defined**

### Usage
```javascript
Asset()
```


---

## UpdateQueryData

### Required Keys
- **no keys defined**

### Usage
```javascript
UpdateQueryData()
```


---

## ApplyFormatting

### Usage
```javascript
ApplyFormatting()
```


---

## GetFrameFilters

### Required Keys
- **no keys defined**

### Usage
```javascript
GetFrameFilters()
```


---

## Substring

### Required Keys
- **columns**

### Usage
```javascript
Substring(columns=[<columns>])
```


---

## CheckProjectName

### Required Keys
- **project**

### Usage
```javascript
CheckProjectName(project=[<project>])
```


---

## NLPQuery2

### Required Keys
- **command**
- **json**
- **tokenCount**
- **frame**
- **allFrames**
- **dialect**
- **engine**

### Usage
```javascript
NLPQuery2(command=[<command>], json=[<json>], tokenCount=[<tokenCount>], frame=[<frame>], allFrames=[<allFrames>], dialect=[<dialect>], engine=[<engine>])
```


---

## AddMetaTags

### Required Keys
- **database**
- **concept**
- **values**

### Usage
```javascript
AddMetaTags(database=[<database>], concept=[<concept>], values=[<values>])
```


---

## NLPQuery3

### Required Keys
- **command**
- **json**
- **tokenCount**
- **frame**
- **allFrames**
- **dialect**
- **engine**

### Usage
```javascript
NLPQuery3(command=[<command>], json=[<json>], tokenCount=[<tokenCount>], frame=[<frame>], allFrames=[<allFrames>], dialect=[<dialect>], engine=[<engine>])
```


---

## FuzzyMerge

### Required Keys
- **qs**
- **joins**
- **frame**
- **fedFrame**
- **matches**
- **nonMatches**
- **propagation**

### Usage
```javascript
FuzzyMerge(qs=[<qs>], joins=[<joins>], frame=[<frame>], fedFrame=[<fedFrame>], matches=[<matches>], nonMatches=[<nonMatches>], propagation=[<propagation>])
```


---

## GetSurveyMonkeySurveys

### Required Keys
- **no keys defined**

### Usage
```javascript
GetSurveyMonkeySurveys()
```


---

## GetDatabaseList

### Required Keys
- **limit**
- **offset**

### Usage
```javascript
GetDatabaseList(limit=[<limit>], offset=[<offset>])
```


---

## SaveOwlPositions

### Required Keys
- **database**
- **positionMap**

### Usage
```javascript
SaveOwlPositions(database=[<database>], positionMap=[<positionMap>])
```


---

## CreateEmbeddingsFromVectorCSVFile

### Required Keys
- **engine**
- **filePaths**

### Optional Keys
- **paramValues**
- **space**

### Usage
```javascript
CreateEmbeddingsFromVectorCSVFile(engine=[<engine>], filePaths=[<filePaths>], paramValues=[<paramValues>], space=[<space>])
```


---

## GetClientBlocks

### Optional Keys
- **filters**

### Usage
```javascript
GetClientBlocks(filters=[<filters>])
```


---

## ProviderProcessor

### Required Keys
- **no keys defined**

### Usage
```javascript
ProviderProcessor()
```


---

## CreateFrame

### Required Keys
- **frameType**
- **override**
- **alias**

### Usage
```javascript
CreateFrame(frameType=[<frameType>], override=[<override>], alias=[<alias>])
```


---

## RetrieveInsightPipeline

### Required Keys
- **project**
- **id**

### Usage
```javascript
RetrieveInsightPipeline(project=[<project>], id=[<id>])
```


---

## GetUserMemory

### Required Keys
- **no keys defined**

### Usage
```javascript
GetUserMemory()
```


---

## PostRequest

### Required Keys
- **url**
- **headersMap**
- **bodyMap**
- **useApplicationCert**

### Usage
```javascript
PostRequest(url=[<url>], headersMap=[<headersMap>], bodyMap=[<bodyMap>], useApplicationCert=[<useApplicationCert>])
```


---

## ExportApp

**Description:** 

        Export an app as a single .smss-app file

### Required Keys
- **project**

### Usage
```javascript
ExportApp(project=[<project>])
```


---

## AdminRClearAllUserRserves

### Required Keys
- **no keys defined**

### Usage
```javascript
AdminRClearAllUserRserves()
```


---

## SetSheetBackground

### Required Keys
- **sheet**
- **sheetLabel**

### Usage
```javascript
SetSheetBackground(sheet=[<sheet>], sheetLabel=[<sheetLabel>])
```


---

## PostgresCopy

### Required Keys
- **database**
- **table**
- **format**
- **nullValue**
- **delimiter**
- **filePath**
- **space**

### Usage
```javascript
PostgresCopy(database=[<database>], table=[<table>], format=[<format>], nullValue=[<nullValue>], delimiter=[<delimiter>], filePath=[<filePath>], space=[<space>])
```


---

## GetRoomOptions

### Required Keys
- **roomId**

### Usage
```javascript
GetRoomOptions(roomId=[<roomId>])
```


---

## RdbmsUploadExcelData

### Required Keys
- **database**
- **filePath**
- **existing**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**
- **replace**

### Usage
```javascript
RdbmsUploadExcelData(database=[<database>], filePath=[<filePath>], existing=[<existing>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>], replace=[<replace>])
```


---

## SwapFrame

### Required Keys
- **frame**

### Usage
```javascript
SwapFrame(frame=[<frame>])
```


---

## If

### Required Keys
- **condition**
- **true**
- **false**

### Usage
```javascript
If(condition=[<condition>], true=[<true>], false=[<false>])
```


---

## GetDSEGraphProperties

### Required Keys
- **host**
- **port**
- **username**
- **password**
- **graphName**

### Usage
```javascript
GetDSEGraphProperties(host=[<host>], port=[<port>], username=[<username>], password=[<password>], graphName=[<graphName>])
```


---

## OpPower

### Required Keys
- **numValues**

### Usage
```javascript
OpPower(numValues=[<numValues>])
```


---

## DropBoxUploader

### Required Keys
- **filename**

### Usage
```javascript
DropBoxUploader(filename=[<filename>])
```


---

## ReconnectServer

### Usage
```javascript
ReconnectServer()
```


---

## SortBy

### Required Keys
- **column**
- **values**

### Usage
```javascript
SortBy(column=[<column>], values=[<values>])
```


---

## FrameFilterModelFilteredValues

### Required Keys
- **column**
- **filterWord**
- **limit**
- **offset**
- **panel**

### Usage
```javascript
FrameFilterModelFilteredValues(column=[<column>], filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], panel=[<panel>])
```


---

## OpenURL

**Description:** 

        Open the URL of the Browser App rendered on the server.

### Required Keys
- **url**

### Usage
```javascript
OpenURL(url=[<url>])
```


---

## ExtractAndSetDependencies

**Description:** 

        Extract engine ids from the project's folder and adds the engine ids as project dependencies

### Required Keys
- **project**

### Optional Keys
- **filePath**

### Usage
```javascript
ExtractAndSetDependencies(project=[<project>], filePath=[<filePath>])
```


---

## ExportEngine

### Required Keys
- **engine**
- **keepGit**

### Usage
```javascript
ExportEngine(engine=[<engine>], keepGit=[<keepGit>])
```


---

## FileRead

### Required Keys
- **filePath**
- **space**
- **sheetName**
- **sheetRange**
- **dataTypeMap**
- **delimiter**
- **newHeaders**
- **additionalDataTypes**

### Usage
```javascript
FileRead(filePath=[<filePath>], space=[<space>], sheetName=[<sheetName>], sheetRange=[<sheetRange>], dataTypeMap=[<dataTypeMap>], delimiter=[<delimiter>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>])
```


---

## ParseSQL2Wrapper

### Required Keys
- **sql**

### Usage
```javascript
ParseSQL2Wrapper(sql=[<sql>])
```


---

## SetInsightOrnament

### Required Keys
- **insightOrnament**

### Usage
```javascript
SetInsightOrnament(insightOrnament=[<insightOrnament>])
```


---

## PanelExists

### Required Keys
- **panel**

### Usage
```javascript
PanelExists(panel=[<panel>])
```


---

## UploadBulkOwlRelationships

### Required Keys
- **database**
- **filePath**
- **space**
- **sync**

### Usage
```javascript
UploadBulkOwlRelationships(database=[<database>], filePath=[<filePath>], space=[<space>], sync=[<sync>])
```


---

## AdminExportUserDatabasePermissions

### Required Keys
- **fileName**
- **filePath**
- **password**
- **database**

### Usage
```javascript
AdminExportUserDatabasePermissions(fileName=[<fileName>], filePath=[<filePath>], password=[<password>], database=[<database>])
```


---

## FillForm

### Required Keys
- **command**
- **form_fields**

### Optional Keys
- **project**

### Usage
```javascript
FillForm(command=[<command>], form_fields=[<form_fields>], project=[<project>])
```


---

## EchoE

**Description:** 

        This reactor returns back the encoded input the user enters

### Required Keys
- **value**

### Usage
```javascript
EchoE(value=[<value>])
```


---

## ExportProject

### Required Keys
- **project**
- **keepGit**

### Usage
```javascript
ExportProject(project=[<project>], keepGit=[<keepGit>])
```


---

## DropBoxFileRetriever

### Required Keys
- **path**

### Usage
```javascript
DropBoxFileRetriever(path=[<path>])
```


---

## LevelMaker

### Required Keys
- **no keys defined**

### Usage
```javascript
LevelMaker()
```


---

## CollectNewCol

### Required Keys
- **qs**

### Usage
```javascript
CollectNewCol(qs=[<qs>])
```


---

## GetXrayConfigFile

### Required Keys
- **fileName**

### Usage
```javascript
GetXrayConfigFile(fileName=[<fileName>])
```


---

## Join

### Required Keys
- **joins**

### Usage
```javascript
Join(joins=[<joins>])
```


---

## GetPhysicalToLogicalMapping

### Required Keys
- **no keys defined**

### Usage
```javascript
GetPhysicalToLogicalMapping()
```


---

## RunLSILearned

### Required Keys
- **column**
- **numValue**

### Usage
```javascript
RunLSILearned(column=[<column>], numValue=[<numValue>])
```


---

## GetFrameDatabaseJoins

### Required Keys
- **frame**
- **database**

### Usage
```javascript
GetFrameDatabaseJoins(frame=[<frame>], database=[<database>])
```


---

## Neg

### Required Keys
- **no keys defined**

### Usage
```javascript
Neg()
```


---

## GetDatabaseConcepts

### Required Keys
- **database**

### Usage
```javascript
GetDatabaseConcepts(database=[<database>])
```


---

## EditOwlDescription

### Required Keys
- **database**
- **concept**
- **column**
- **description**

### Usage
```javascript
EditOwlDescription(database=[<database>], concept=[<concept>], column=[<column>], description=[<description>])
```


---

## CtrlCAsset

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
CtrlCAsset(filePath=[<filePath>], space=[<space>])
```


---

## MakePixelMCP

**Description:** 

        Generates a mcp/pixel_mcp.json file from a set of reactors

### Required Keys
- **project**

### Optional Keys
- **reactor**
- **comment**

### Usage
```javascript
MakePixelMCP(project=[<project>], reactor=[<reactor>], comment=[<comment>])
```


---

## AddPanelColorByValue

### Required Keys
- **panel**
- **name**
- **qs**
- **options**

### Usage
```javascript
AddPanelColorByValue(panel=[<panel>], name=[<name>], qs=[<qs>], options=[<options>])
```


---

## IfError

### Required Keys
- **no keys defined**

### Usage
```javascript
IfError()
```


---

## SaveAppBlocksJson

### Required Keys
- **project**
- **json**
- **comment**

### Usage
```javascript
SaveAppBlocksJson(project=[<project>], json=[<json>], comment=[<comment>])
```


---

## Task

### Required Keys
- **taskId**

### Usage
```javascript
Task(taskId=[<taskId>])
```


---

## ExportToPPTNN

### Required Keys
- **fileName**
- **filePath**
- **usePanel**
- **height**
- **width**
- **slideLayout**
- **shapeIndex**
- **export_template**

### Usage
```javascript
ExportToPPTNN(fileName=[<fileName>], filePath=[<filePath>], usePanel=[<usePanel>], height=[<height>], width=[<width>], slideLayout=[<slideLayout>], shapeIndex=[<shapeIndex>], export_template=[<export_template>])
```


---

## UnsortPanel

### Required Keys
- **panel**
- **columns**

### Usage
```javascript
UnsortPanel(panel=[<panel>], columns=[<columns>])
```


---

## GetInsightFrames

### Required Keys
- **project**
- **id**
- **frame**

### Usage
```javascript
GetInsightFrames(project=[<project>], id=[<id>], frame=[<frame>])
```


---

## PredictDataTypes

### Required Keys
- **filePath**
- **space**
- **delimiter**
- **rowCount**

### Usage
```javascript
PredictDataTypes(filePath=[<filePath>], space=[<space>], delimiter=[<delimiter>], rowCount=[<rowCount>])
```


---

## QueryAll

### Required Keys
- **no keys defined**

### Usage
```javascript
QueryAll()
```


---

## InsightHandle

### Required Keys
- **no keys defined**

### Usage
```javascript
InsightHandle()
```


---

## Moose

### Required Keys
- **command**

### Optional Keys
- **project**

### Usage
```javascript
Moose(command=[<command>], project=[<project>])
```


---

## AddOpenAIKey

### Optional Keys
- **apikey**

### Usage
```javascript
AddOpenAIKey(apikey=[<apikey>])
```


---

## GetGraphMetaModel

### Required Keys
- **filePath**
- **space**
- **graphTypeId**
- **useLabel**

### Usage
```javascript
GetGraphMetaModel(filePath=[<filePath>], space=[<space>], graphTypeId=[<graphTypeId>], useLabel=[<useLabel>])
```


---

## SetPanelView

### Required Keys
- **panel**
- **panelView**
- **panelViewOptions**

### Usage
```javascript
SetPanelView(panel=[<panel>], panelView=[<panelView>], panelViewOptions=[<panelViewOptions>])
```


---

## UploadDatabase

**Description:** 

        Import a new engine. The user who uploads will by default be the owner of the engine

### Required Keys
- **filePath**
- **space**
- **global**

### Usage
```javascript
UploadDatabase(filePath=[<filePath>], space=[<space>], global=[<global>])
```


---

## Sort

### Required Keys
- **columns**
- **sort**

### Usage
```javascript
Sort(columns=[<columns>], sort=[<sort>])
```


---

## OpenInsight

### Required Keys
- **project**
- **id**
- **params**
- **additionalPixels**
- **paramValues**
- **cache**
- **useExistingIfOpen**
- **origin**

### Usage
```javascript
OpenInsight(project=[<project>], id=[<id>], params=[<params>], additionalPixels=[<additionalPixels>], paramValues=[<paramValues>], cache=[<cache>], useExistingIfOpen=[<useExistingIfOpen>], origin=[<origin>])
```


---

## UnlockProjects

### Required Keys
- **project**

### Usage
```javascript
UnlockProjects(project=[<project>])
```


---

## GetDatabaseMetakeyOptions

### Required Keys
- **metaKeys**

### Usage
```javascript
GetDatabaseMetakeyOptions(metaKeys=[<metaKeys>])
```


---

## SetInsightMetadata

**Description:** 

        Define metadata for an insight

### Required Keys
- **project**
- **id**
- **meta**
- **encoded**
- **jsonCleanup**

### Usage
```javascript
SetInsightMetadata(project=[<project>], id=[<id>], meta=[<meta>], encoded=[<encoded>], jsonCleanup=[<jsonCleanup>])
```


---

## MakeDirectory

### Required Keys
- **filePath**

### Optional Keys
- **space**

### Usage
```javascript
MakeDirectory(filePath=[<filePath>], space=[<space>])
```


---

## DeleteMetaTags

### Required Keys
- **database**
- **concept**
- **values**

### Usage
```javascript
DeleteMetaTags(database=[<database>], concept=[<concept>], values=[<values>])
```


---

## ActivateVirtualEnv

### Required Keys
- **engine**

### Usage
```javascript
ActivateVirtualEnv(engine=[<engine>])
```


---

## Bupd

### Required Keys
- **fancy**
- **embed**

### Usage
```javascript
Bupd(fancy=[<fancy>], embed=[<embed>])
```


---

## AdminResetPasswordRules

### Required Keys
- **no keys defined**

### Usage
```javascript
AdminResetPasswordRules()
```


---

## GetProjectSMSS

### Required Keys
- **project**

### Usage
```javascript
GetProjectSMSS(project=[<project>])
```


---

## GetPlaceHolders

### Required Keys
- **project**
- **template_name**

### Usage
```javascript
GetPlaceHolders(project=[<project>], template_name=[<template_name>])
```


---

## OpSumIf

### Required Keys
- **values**
- **criteria**
- **sumRange**

### Usage
```javascript
OpSumIf(values=[<values>], criteria=[<criteria>], sumRange=[<sumRange>])
```


---

## GenerateH2FrameFromRVariable

### Required Keys
- **variable**
- **override**

### Usage
```javascript
GenerateH2FrameFromRVariable(variable=[<variable>], override=[<override>])
```


---

## GetAppAssets

**Description:** 

        Retrieve the contents of a file in the projects assets folder

### Required Keys
- **project**
- **filePath**

### Usage
```javascript
GetAppAssets(project=[<project>], filePath=[<filePath>])
```


---

## ToCsv

### Required Keys
- **task**
- **fileName**
- **filePath**
- **appendTimestamp**

### Usage
```javascript
ToCsv(task=[<task>], fileName=[<fileName>], filePath=[<filePath>], appendTimestamp=[<appendTimestamp>])
```


---

## RunDataQuality

### Required Keys
- **rule**
- **column**
- **options**
- **inputTable**

### Usage
```javascript
RunDataQuality(rule=[<rule>], column=[<column>], options=[<options>], inputTable=[<inputTable>])
```


---

## DownloadProjectInsights

### Required Keys
- **project**

### Usage
```javascript
DownloadProjectInsights(project=[<project>])
```


---

## UploadUserFile

### Required Keys
- **filePath**
- **relativePath**

### Usage
```javascript
UploadUserFile(filePath=[<filePath>], relativePath=[<relativePath>])
```


---

## DeleteInsightRecipeStep

### Required Keys
- **pixelId**
- **propagate**

### Usage
```javascript
DeleteInsightRecipeStep(pixelId=[<pixelId>], propagate=[<propagate>])
```


---

## HoverXY

**Description:** 

        Hover over the x, y coordinate of the Browser App rendered on the server.

### Required Keys
- **x**
- **y**

### Usage
```javascript
HoverXY(x=[<x>], y=[<y>])
```


---

## GenericSelectorFunction

### Required Keys
- **columns**

### Usage
```javascript
GenericSelectorFunction(columns=[<columns>])
```


---

## SetFilterModelState

### Required Keys
- **panel**
- **frame**
- **filters**

### Usage
```javascript
SetFilterModelState(panel=[<panel>], frame=[<frame>], filters=[<filters>])
```


---

## ImportParamOptions

### Required Keys
- **no keys defined**

### Usage
```javascript
ImportParamOptions()
```


---

## TransposeRows

### Required Keys
- **no keys defined**

### Usage
```javascript
TransposeRows()
```


---

## RunDatabaseDescriptionGenerator

### Required Keys
- **database**
- **size**
- **words**

### Usage
```javascript
RunDatabaseDescriptionGenerator(database=[<database>], size=[<size>], words=[<words>])
```


---

## Format

### Required Keys
- **type**
- **options**

### Usage
```javascript
Format(type=[<type>], options=[<options>])
```


---

## VectorAttachFileToSource

**Description:** 

        Attach a file as being the source for a vector database entry

### Required Keys
- **engine**
- **filePath**

### Optional Keys
- **source**
- **space**

### Usage
```javascript
VectorAttachFileToSource(engine=[<engine>], filePath=[<filePath>], source=[<source>], space=[<space>])
```


---

## FrameType

### Required Keys
- **frame**

### Usage
```javascript
FrameType(frame=[<frame>])
```


---

## ResetPanelOrnaments

### Required Keys
- **panel**

### Usage
```javascript
ResetPanelOrnaments(panel=[<panel>])
```


---

## GetModelAPI

### Required Keys
- **model**

### Usage
```javascript
GetModelAPI(model=[<model>])
```


---

## MigrateImagesToCouch

**Description:** 

        Reactor to migrate images from remote image folders to CouchDB

### Optional Keys
- **repull**
- **filterInvalidIds**
- **verifyMissing**

### Usage
```javascript
MigrateImagesToCouch(repull=[<repull>], filterInvalidIds=[<filterInvalidIds>], verifyMissing=[<verifyMissing>])
```


---

## AdminUploadDatabasePermissions

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
AdminUploadDatabasePermissions(filePath=[<filePath>], space=[<space>])
```


---

## RunClustering

### Required Keys
- **instance**
- **numClusters**
- **attributes**

### Usage
```javascript
RunClustering(instance=[<instance>], numClusters=[<numClusters>], attributes=[<attributes>])
```


---

## OpContains

### Required Keys
- **array**
- **value**

### Usage
```javascript
OpContains(array=[<array>], value=[<value>])
```


---

## PublishProject

### Required Keys
- **project**
- **release**

### Usage
```javascript
PublishProject(project=[<project>], release=[<release>])
```


---

## UnvoteEngine

### Required Keys
- **engine**

### Usage
```javascript
UnvoteEngine(engine=[<engine>])
```


---

## GetPlaysheetParams

### Required Keys
- **project**
- **id**

### Usage
```javascript
GetPlaysheetParams(project=[<project>], id=[<id>])
```


---

## ListDocumentsInVectorDatabase

**Description:** 

        Get the unique list of 'Source' values that have been uploaded into this vector database. Typically the 'Souce' represents the files that have been uploaded, but in the case of custom chunking the value will be returned even if the file itself was never uploaded

### Required Keys
- **engine**

### Optional Keys
- **paramValues**

### Usage
```javascript
ListDocumentsInVectorDatabase(engine=[<engine>], paramValues=[<paramValues>])
```


---

## GitlabListJobsForProject

**Description:** 

        This reactor returns a list of JSON maps for the last 20 pipeline jobs that ran for a GitLab project

### Required Keys
- **host**
- **gitlabProjectId**
- **gitlabPrivateToken**
- **gitlabJobScope**
- **useApplicationCert**

### Usage
```javascript
GitlabListJobsForProject(host=[<host>], gitlabProjectId=[<gitlabProjectId>], gitlabPrivateToken=[<gitlabPrivateToken>], gitlabJobScope=[<gitlabJobScope>], useApplicationCert=[<useApplicationCert>])
```


---

## GetDatabaseMarkdown

### Required Keys
- **database**

### Usage
```javascript
GetDatabaseMarkdown(database=[<database>])
```


---

## OpenUserRoom

### Optional Keys
- **recipe**
- **id**
- **params**

### Usage
```javascript
OpenUserRoom(recipe=[<recipe>], id=[<id>], params=[<params>])
```


---

## SendSuccessMessage

### Required Keys
- **message**

### Usage
```javascript
SendSuccessMessage(message=[<message>])
```


---

## OpAbsolute

### Required Keys
- **numValues**

### Usage
```javascript
OpAbsolute(numValues=[<numValues>])
```


---

## SchedulerHistory

### Required Keys
- **filters**
- **sort**
- **limit**
- **offset**
- **jobTags**

### Usage
```javascript
SchedulerHistory(filters=[<filters>], sort=[<sort>], limit=[<limit>], offset=[<offset>], jobTags=[<jobTags>])
```


---

## SnowflakeCopyInto

**Description:** 

        Utility method to execute the COPY INTO command from a file in a stage. Snowflake docs found here: https://docs.snowflake.com/en/sql-reference/sql/copy-into-table

### Required Keys
- **database**
- **table**

### Optional Keys
- **userStage**
- **matchByColumnName**
- **dateFormat**
- **pattern**
- **formatName**
- **fieldOptionallyEnclosedBy**
- **fileCompression**
- **parseHeader**
- **timeFormat**
- **files**
- **timestampFormat**
- **tableStage**
- **force**
- **namedStage**
- **fileType**

### Usage
```javascript
SnowflakeCopyInto(database=[<database>], table=[<table>], userStage=[<userStage>], matchByColumnName=[<matchByColumnName>], dateFormat=[<dateFormat>], pattern=[<pattern>], formatName=[<formatName>], fieldOptionallyEnclosedBy=[<fieldOptionallyEnclosedBy>], fileCompression=[<fileCompression>], parseHeader=[<parseHeader>], timeFormat=[<timeFormat>], files=[<files>], timestampFormat=[<timestampFormat>], tableStage=[<tableStage>], force=[<force>], namedStage=[<namedStage>], fileType=[<fileType>])
```


---

## ToTsv

### Required Keys
- **task**
- **fileName**
- **filePath**
- **appendTimestamp**

### Usage
```javascript
ToTsv(task=[<task>], fileName=[<fileName>], filePath=[<filePath>], appendTimestamp=[<appendTimestamp>])
```


---

## UpdateSemanticData

### Required Keys
- **no keys defined**

### Usage
```javascript
UpdateSemanticData()
```


---

## GetCurrentURL

**Description:** 

        Get the current URL of the Browser App rendered on the server.

### Required Keys
- **no keys defined**

### Usage
```javascript
GetCurrentURL()
```


---

## SetAppConfig

### Required Keys
- **no keys defined**

### Usage
```javascript
SetAppConfig()
```


---

## Sheet

### Required Keys
- **sheet**

### Usage
```javascript
Sheet(sheet=[<sheet>])
```


---

## FrameFilterModelNumericRange

### Required Keys
- **column**
- **panel**

### Usage
```javascript
FrameFilterModelNumericRange(column=[<column>], panel=[<panel>])
```


---

## UsabilityScore

### Required Keys
- **database**

### Usage
```javascript
UsabilityScore(database=[<database>])
```


---

## SaveInsight

### Required Keys
- **project**
- **insightName**
- **layout**
- **global**
- **recipe**
- **params**
- **description**
- **tags**
- **image**
- **encoded**
- **cache**
- **cacheMinutes**
- **cacheEncrypt**
- **schemaName**

### Usage
```javascript
SaveInsight(project=[<project>], insightName=[<insightName>], layout=[<layout>], global=[<global>], recipe=[<recipe>], params=[<params>], description=[<description>], tags=[<tags>], image=[<image>], encoded=[<encoded>], cache=[<cache>], cacheMinutes=[<cacheMinutes>], cacheEncrypt=[<cacheEncrypt>], schemaName=[<schemaName>])
```


---

## ClosePanelIfExists

### Required Keys
- **panel**

### Usage
```javascript
ClosePanelIfExists(panel=[<panel>])
```


---

## RegenSQL

### Required Keys
- **no keys defined**

### Usage
```javascript
RegenSQL()
```


---

## GetMCPTools

### Required Keys
- **project**

### Usage
```javascript
GetMCPTools(project=[<project>])
```


---

## LoadProjectMetadata

**Description:** 

        Define metadata on a project through a JSON file

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
LoadProjectMetadata(filePath=[<filePath>], space=[<space>])
```


---

## AddDatabaseStructure

### Required Keys
- **database**
- **metamodelAdd**

### Usage
```javascript
AddDatabaseStructure(database=[<database>], metamodelAdd=[<metamodelAdd>])
```


---

## GetUsageStatistics

**Description:** 

        This reactor generates usage reports for a specific engine (model/agent), based on the provided parameters.

Required parameters:
  â€¢ agentId     â€“ engineId string identifying the model (e.g., "e4449559-bcff-4941-ae72-0e3f18e06660")
  â€¢ startDate   â€“ inclusive start timestamp (format: yyyy-MM-dd HH:mm:ss.SSS)
  â€¢ endDate     â€“ inclusive end timestamp (format: yyyy-MM-dd HH:mm:ss.SSS)
  â€¢ type        â€“ determines the report granularity; must be either "User" or "App"

When type = "User":
  Returns a map containing key "userReport" mapped to a list of records.
  Each record includes:
    â€¢ userName        â€“ name or identifier of the user
    â€¢ userId          â€“ unique identifier of the user
    â€¢ messages        â€“ number of messages generated by the user
    â€¢ tokens          â€“ total tokens consumed by the user
    â€¢ avg_tokens      â€“ average tokens per message
    â€¢ last_utilized_date â€“ timestamp of the last usage by the user

When type = "App":
  Returns a map containing key "appReport" mapped to a list of records.
  Each record includes:
    â€¢ appName or application identifier
    â€¢ appId or equivalent
    â€¢ messages        â€“ number of messages generated by the app
    â€¢ tokens          â€“ total tokens consumed by the app
    â€¢ avg_tokens      â€“ average tokens per message
    â€¢ last_utilized_date â€“ timestamp of the last usage by the app

When type is neither "User" nor "App":
  Returns a map containing key "error" with the message "Invalid type. Use 'App' or 'User'".

Use case examples:
  GetUsageStatistics(agentId="â€¦", startDate="2025-08-01 00:00:00.000", endDate="2025-08-04 23:59:59.999", type="User");
  GetUsageStatistics(agentId="â€¦", startDate="2025-08-01 00:00:00.000", endDate="2025-08-04 23:59:59.999", type="App");


### Required Keys
- **agentId**
- **startDate**
- **endDate**
- **type**

### Usage
```javascript
GetUsageStatistics(agentId=[<agentId>], startDate=[<startDate>], endDate=[<endDate>], type=[<type>])
```


---

## ExportProjectApp

**Description:** 

        Export an app as a single zip file

### Required Keys
- **project**

### Usage
```javascript
ExportProjectApp(project=[<project>])
```


---

## CleanUpDatabases

### Required Keys
- **password**
- **dryRun**
- **cleanUpCloudStorage**

### Usage
```javascript
CleanUpDatabases(password=[<password>], dryRun=[<dryRun>], cleanUpCloudStorage=[<cleanUpCloudStorage>])
```


---

## TableToXLSX

### Required Keys
- **sheet**
- **html**

### Optional Keys
- **headers**
- **fileName**
- **theader**
- **merge**
- **column_gutter**
- **export_template**
- **rowgutter**
- **tfooter**

### Usage
```javascript
TableToXLSX(sheet=[<sheet>], html=[<html>], headers=[<headers>], fileName=[<fileName>], theader=[<theader>], merge=[<merge>], column_gutter=[<column_gutter>], export_template=[<export_template>], rowgutter=[<rowgutter>], tfooter=[<tfooter>])
```


---

## AuditDatabase

### Required Keys
- **database**
- **tables**
- **columns**
- **dateTimeField**
- **value**

### Usage
```javascript
AuditDatabase(database=[<database>], tables=[<tables>], columns=[<columns>], dateTimeField=[<dateTimeField>], value=[<value>])
```


---

## ExternalJdbcSchema

### Required Keys
- **conDetails**
- **filters**

### Usage
```javascript
ExternalJdbcSchema(conDetails=[<conDetails>], filters=[<filters>])
```


---

## LoadPyFromFile

### Required Keys
- **filePath**
- **alias**
- **space**

### Usage
```javascript
LoadPyFromFile(filePath=[<filePath>], alias=[<alias>], space=[<space>])
```


---

## Py

### Required Keys
- **no keys defined**

### Usage
```javascript
Py()
```


---

## GetProjectPortalDetails

### Required Keys
- **project**

### Usage
```javascript
GetProjectPortalDetails(project=[<project>])
```


---

## AddFrameFilter

### Required Keys
- **frame**
- **filters**
- **taskRefresh**

### Usage
```javascript
AddFrameFilter(frame=[<frame>], filters=[<filters>], taskRefresh=[<taskRefresh>])
```


---

## RemoveDocumentFromVectorDatabase

### Required Keys
- **engine**
- **fileNames**

### Optional Keys
- **paramValues**

### Usage
```javascript
RemoveDocumentFromVectorDatabase(engine=[<engine>], fileNames=[<fileNames>], paramValues=[<paramValues>])
```


---

## SharePointSiteSelector

### Required Keys
- **serverUrl**

### Usage
```javascript
SharePointSiteSelector(serverUrl=[<serverUrl>])
```


---

## GetCurrentUser

### Required Keys
- **no keys defined**

### Usage
```javascript
GetCurrentUser()
```


---

## DeleteWorkspace

### Required Keys
- **workspaceId**

### Usage
```javascript
DeleteWorkspace(workspaceId=[<workspaceId>])
```


---

## Group

### Required Keys
- **columns**

### Usage
```javascript
Group(columns=[<columns>])
```


---

## Parent2Validator

### Required Keys
- **no keys defined**

### Usage
```javascript
Parent2Validator()
```


---

## LoadClient

### Required Keys
- **no keys defined**

### Usage
```javascript
LoadClient()
```


---

## CheckoutVersion

### Required Keys
- **version**

### Usage
```javascript
CheckoutVersion(version=[<version>])
```


---

## LoggingOutput

### Required Keys
- **arguments**

### Usage
```javascript
LoggingOutput(arguments=[<arguments>])
```


---

## RunLOF

### Required Keys
- **instance**
- **kNeighbors**
- **attributes**

### Usage
```javascript
RunLOF(instance=[<instance>], kNeighbors=[<kNeighbors>], attributes=[<attributes>])
```


---

## CachedPanel

### Required Keys
- **panel**

### Usage
```javascript
CachedPanel(panel=[<panel>])
```


---

## GetUDF

### Required Keys
- **database**

### Usage
```javascript
GetUDF(database=[<database>])
```


---

## Timestamp

### Required Keys
- **date**
- **format**

### Usage
```javascript
Timestamp(date=[<date>], format=[<format>])
```


---

## MyDiscoverableProjects

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **projectType**
- **project**
- **metaKeys**
- **metaFilters**
- **noMeta**
- **onlyPortals**

### Usage
```javascript
MyDiscoverableProjects(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], projectType=[<projectType>], project=[<project>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], noMeta=[<noMeta>], onlyPortals=[<onlyPortals>])
```


---

## Union

### Required Keys
- **frame1**
- **frame2**
- **unionType**
- **mapping**

### Usage
```javascript
Union(frame1=[<frame1>], frame2=[<frame2>], unionType=[<unionType>], mapping=[<mapping>])
```


---

## Ask

### Optional Keys
- **fullPrompt**
- **command**

### Usage
```javascript
Ask(fullPrompt=[<fullPrompt>], command=[<command>])
```


---

## CreateJanusGraphDatabase

### Required Keys
- **database**
- **filePath**
- **graphTypeId**
- **graphNameId**
- **graphMetamodel**
- **useLabel**

### Usage
```javascript
CreateJanusGraphDatabase(database=[<database>], filePath=[<filePath>], graphTypeId=[<graphTypeId>], graphNameId=[<graphNameId>], graphMetamodel=[<graphMetamodel>], useLabel=[<useLabel>])
```


---

## SetSheetState

### Required Keys
- **state**

### Usage
```javascript
SetSheetState(state=[<state>])
```


---

## GitVersion

### Required Keys
- **app**

### Usage
```javascript
GitVersion(app=[<app>])
```


---

## FrameProfile

### Required Keys
- **frame**

### Usage
```javascript
FrameProfile(frame=[<frame>])
```


---

## GoogleFileRetriever

### Required Keys
- **id**
- **type**

### Usage
```javascript
GoogleFileRetriever(id=[<id>], type=[<type>])
```


---

## RCsvUpload

### Required Keys
- **database**
- **filePath**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**

### Usage
```javascript
RCsvUpload(database=[<database>], filePath=[<filePath>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>])
```


---

## GitInit

### Required Keys
- **no keys defined**

### Usage
```javascript
GitInit()
```


---

## SetProjectPropertiesContent

### Required Keys
- **project**
- **propMap**

### Usage
```javascript
SetProjectPropertiesContent(project=[<project>], propMap=[<propMap>])
```


---

## SetPanelState

### Required Keys
- **state**

### Usage
```javascript
SetPanelState(state=[<state>])
```


---

## SyncGraphToR

### Required Keys
- **frame**

### Usage
```javascript
SyncGraphToR(frame=[<frame>])
```


---

## RemoveDatabaseColumn

### Required Keys
- **database**
- **concept**
- **column**
- **dataType**

### Usage
```javascript
RemoveDatabaseColumn(database=[<database>], concept=[<concept>], column=[<column>], dataType=[<dataType>])
```


---

## Select

### Required Keys
- **columns**

### Usage
```javascript
Select(columns=[<columns>])
```


---

## UpdateEngineFiles

### Required Keys
- **engine**
- **payload**

### Usage
```javascript
UpdateEngineFiles(engine=[<engine>], payload=[<payload>])
```


---

## GetNLPVizOptions

### Required Keys
- **database**
- **columns**
- **frame**

### Usage
```javascript
GetNLPVizOptions(database=[<database>], columns=[<columns>], frame=[<frame>])
```


---

## GetRoomMessages

### Required Keys
- **roomId**

### Optional Keys
- **offset**
- **limit**
- **sort**

### Usage
```javascript
GetRoomMessages(roomId=[<roomId>], offset=[<offset>], limit=[<limit>], sort=[<sort>])
```


---

## AddTemplate

### Required Keys
- **project**
- **template_name**
- **template_file**

### Usage
```javascript
AddTemplate(project=[<project>], template_name=[<template_name>], template_file=[<template_file>])
```


---

## BrowseEngineAssets

**Description:** 

        List the files and directories from a relative filePath input from within the engine folder

### Required Keys
- **engine**

### Optional Keys
- **filePath**

### Usage
```javascript
BrowseEngineAssets(engine=[<engine>], filePath=[<filePath>])
```


---

## SetPanelSort

### Required Keys
- **panel**
- **columns**
- **sort**

### Usage
```javascript
SetPanelSort(panel=[<panel>], columns=[<columns>], sort=[<sort>])
```


---

## EngineInfo

### Required Keys
- **engine**
- **metaKeys**

### Usage
```javascript
EngineInfo(engine=[<engine>], metaKeys=[<metaKeys>])
```


---

## ELogMessage

### Required Keys
- **message**

### Usage
```javascript
ELogMessage(message=[<message>])
```


---

## RuntimeClassTest

### Required Keys
- **no keys defined**

### Usage
```javascript
RuntimeClassTest()
```


---

## GetConsolidatedCodeExecution

### Required Keys
- **write**

### Usage
```javascript
GetConsolidatedCodeExecution(write=[<write>])
```


---

## AdminMyEngines

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **engineTypes**
- **engine**
- **metaKeys**
- **metaFilters**
- **noMeta**
- **userT**

### Usage
```javascript
AdminMyEngines(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], engineTypes=[<engineTypes>], engine=[<engine>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], noMeta=[<noMeta>], userT=[<userT>])
```


---

## RequestEngine

### Required Keys
- **engine**
- **permission**
- **comment**

### Usage
```javascript
RequestEngine(engine=[<engine>], permission=[<permission>], comment=[<comment>])
```


---

## CreateVectorDatabaseEngine

### Required Keys
- **database**
- **conDetails**

### Optional Keys
- **global**

### Usage
```javascript
CreateVectorDatabaseEngine(database=[<database>], conDetails=[<conDetails>], global=[<global>])
```


---

## FindSemanticInstanceOwlRelationships

### Required Keys
- **database**
- **tables**
- **store**

### Usage
```javascript
FindSemanticInstanceOwlRelationships(database=[<database>], tables=[<tables>], store=[<store>])
```


---

## FrameHeaders

### Required Keys
- **frame**
- **headerTypes**
- **reset**

### Usage
```javascript
FrameHeaders(frame=[<frame>], headerTypes=[<headerTypes>], reset=[<reset>])
```


---

## AddPanelOrnaments

### Required Keys
- **panel**
- **ornaments**

### Usage
```javascript
AddPanelOrnaments(panel=[<panel>], ornaments=[<ornaments>])
```


---

## PinRoom

### Required Keys
- **roomId**
- **pinned**

### Usage
```javascript
PinRoom(roomId=[<roomId>], pinned=[<pinned>])
```


---

## MyEngines

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **onlyFavorites**
- **engineTypes**
- **engine**
- **permissionFilters**
- **metaKeys**
- **metaFilters**
- **noMeta**
- **userT**
- **sort**

### Usage
```javascript
MyEngines(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], onlyFavorites=[<onlyFavorites>], engineTypes=[<engineTypes>], engine=[<engine>], permissionFilters=[<permissionFilters>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], noMeta=[<noMeta>], userT=[<userT>], sort=[<sort>])
```


---

## AddToolExecution

**Description:** 

        Add a tool execution input message to the message history.
If all the tools have been executed from the previous tool response message, this will return the LLM response.
Otherwise, a default string message that more tools responses are needed


### Required Keys
- **engine**
- **roomId**
- **toolId**

### Optional Keys
- **toolExecutionResponse**
- **tool_execution_response**
- **toolParameterValues**
- **toolName**

### Usage
```javascript
AddToolExecution(engine=[<engine>], roomId=[<roomId>], toolId=[<toolId>], toolExecutionResponse=[<toolExecutionResponse>], tool_execution_response=[<tool_execution_response>], toolParameterValues=[<toolParameterValues>], toolName=[<toolName>])
```


---

## CtrlVAsset

### Required Keys
- **space**
- **filePath**

### Usage
```javascript
CtrlVAsset(space=[<space>], filePath=[<filePath>])
```


---

## DeleteAppAssets

**Description:** 

        Delete a single or multiple files in the projects assets folder

### Required Keys
- **project**
- **filePath**

### Optional Keys
- **comment**

### Usage
```javascript
DeleteAppAssets(project=[<project>], filePath=[<filePath>], comment=[<comment>])
```


---

## RdfCsvUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **customBaseURI**

### Usage
```javascript
RdfCsvUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], customBaseURI=[<customBaseURI>])
```


---

## DropAppRepo

### Required Keys
- **database**
- **repository**

### Usage
```javascript
DropAppRepo(database=[<database>], repository=[<repository>])
```


---

## PullInsightFolderFromCloud

### Required Keys
- **project**
- **id**

### Usage
```javascript
PullInsightFolderFromCloud(project=[<project>], id=[<id>])
```


---

## GetWorkspace

### Required Keys
- **workspaceId**

### Optional Keys
- **withResources**

### Usage
```javascript
GetWorkspace(workspaceId=[<workspaceId>], withResources=[<withResources>])
```


---

## RuntimeClassTestSingle

### Required Keys
- **no keys defined**

### Usage
```javascript
RuntimeClassTestSingle()
```


---

## SemanticBlending

### Required Keys
- **columns**
- **display**
- **randomVals**
- **genFrame**
- **frameName**

### Usage
```javascript
SemanticBlending(columns=[<columns>], display=[<display>], randomVals=[<randomVals>], genFrame=[<genFrame>], frameName=[<frameName>])
```


---

## NLSQueryHelper

### Required Keys
- **query**
- **database**
- **helpOn**
- **global**

### Usage
```javascript
NLSQueryHelper(query=[<query>], database=[<database>], helpOn=[<helpOn>], global=[<global>])
```


---

## RSource

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
RSource(filePath=[<filePath>], space=[<space>])
```


---

## GLiNERGuardrailEngine

### Required Keys
- **prompt**
- **labels**
- **threshold**

### Usage
```javascript
GLiNERGuardrailEngine(prompt=[<prompt>], labels=[<labels>], threshold=[<threshold>])
```


---

## Collect

### Required Keys
- **task**
- **limit**
- **meta**

### Usage
```javascript
Collect(task=[<task>], limit=[<limit>], meta=[<meta>])
```


---

## SetInsightGraphOptions

### Required Keys
- **options**

### Usage
```javascript
SetInsightGraphOptions(options=[<options>])
```


---

## AdminGetEngineUsagePerProject

### Required Keys
- **engine**
- **limit**
- **offset**
- **startDate**
- **endDate**

### Usage
```javascript
AdminGetEngineUsagePerProject(engine=[<engine>], limit=[<limit>], offset=[<offset>], startDate=[<startDate>], endDate=[<endDate>])
```


---

## GetMyOpenAiKeyModelsList

### Required Keys
- **openaiKey**

### Optional Keys
- **organizationId**

### Usage
```javascript
GetMyOpenAiKeyModelsList(openaiKey=[<openaiKey>], organizationId=[<organizationId>])
```


---

## UserDir

### Required Keys
- **relativePath**

### Usage
```javascript
UserDir(relativePath=[<relativePath>])
```


---

## GoogleGmailSendEmail

**Description:** 

        Send an email from the logged in gmail account

### Required Keys
- **subject**
- **to**
- **message**

### Usage
```javascript
GoogleGmailSendEmail(subject=[<subject>], to=[<to>], message=[<message>])
```


---

## VoteEngine

### Required Keys
- **engine**
- **vote**

### Usage
```javascript
VoteEngine(engine=[<engine>], vote=[<vote>])
```


---

## ChangeQuery

### Required Keys
- **querypart**

### Usage
```javascript
ChangeQuery(querypart=[<querypart>])
```


---

## SubmitLlmFeedback

### Required Keys
- **messageId**
- **rating**

### Optional Keys
- **feedbackText**

### Usage
```javascript
SubmitLlmFeedback(messageId=[<messageId>], rating=[<rating>], feedbackText=[<feedbackText>])
```


---

## ExecuteStreamingFunctionEngine

### Required Keys
- **engine**
- **map**

### Usage
```javascript
ExecuteStreamingFunctionEngine(engine=[<engine>], map=[<map>])
```


---

## AddInsightParameter

### Required Keys
- **paramStruct**

### Usage
```javascript
AddInsightParameter(paramStruct=[<paramStruct>])
```


---

## UpdatePanelComment

### Required Keys
- **panel**
- **comment**

### Usage
```javascript
UpdatePanelComment(panel=[<panel>], comment=[<comment>])
```


---

## GoogleGmailSummarizeTopKEmails

**Description:** 

        Summarize the top k emails

### Required Keys
- **limit**

### Usage
```javascript
GoogleGmailSummarizeTopKEmails(limit=[<limit>])
```


---

## GetDatabaseTablePhysicalStructure

### Required Keys
- **database**
- **table**

### Usage
```javascript
GetDatabaseTablePhysicalStructure(database=[<database>], table=[<table>])
```


---

## CustomWrapper

### Required Keys
- **no keys defined**

### Usage
```javascript
CustomWrapper()
```


---

## RecommendedDatabases

### Required Keys
- **display**

### Usage
```javascript
RecommendedDatabases(display=[<display>])
```


---

## GetPanelFiltersQS

### Required Keys
- **panel**

### Usage
```javascript
GetPanelFiltersQS(panel=[<panel>])
```


---

## AddDefaultInsights

### Required Keys
- **project**
- **database**
- **insights**

### Usage
```javascript
AddDefaultInsights(project=[<project>], database=[<database>], insights=[<insights>])
```


---

## ConvertRunPixelTaskDataToQueryStruct

**Description:** 

        This reactor is used to flush a string in the format of the JSON response for the runPixel endpoint. It will grab a specific index of the pixel return (or assume last index) to be in the task data format and write that to file and return a query struct for importing that response into a frame

### Required Keys
- **value**
- **index**
- **fileName**
- **deleteAfterInsightClose**

### Usage
```javascript
ConvertRunPixelTaskDataToQueryStruct(value=[<value>], index=[<index>], fileName=[<fileName>], deleteAfterInsightClose=[<deleteAfterInsightClose>])
```


---

## GetInsights

### Required Keys
- **project**
- **filterWord**
- **limit**
- **offset**
- **onlyFavorites**
- **sort**
- **metaFilters**
- **noMeta**

### Usage
```javascript
GetInsights(project=[<project>], filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], onlyFavorites=[<onlyFavorites>], sort=[<sort>], metaFilters=[<metaFilters>], noMeta=[<noMeta>])
```


---

## CopyDatabaseTableSchema

### Required Keys
- **sourceDatabase**
- **sourceTable**
- **targetDatabase**
- **targetTable**
- **override**
- **ignoreOWL**
- **nameConversions**
- **typeConversions**
- **fuzzyConversions**

### Usage
```javascript
CopyDatabaseTableSchema(sourceDatabase=[<sourceDatabase>], sourceTable=[<sourceTable>], targetDatabase=[<targetDatabase>], targetTable=[<targetTable>], override=[<override>], ignoreOWL=[<ignoreOWL>], nameConversions=[<nameConversions>], typeConversions=[<typeConversions>], fuzzyConversions=[<fuzzyConversions>])
```


---

## PushEngineToCloud

### Required Keys
- **engine**

### Usage
```javascript
PushEngineToCloud(engine=[<engine>])
```


---

## Filter

### Required Keys
- **filters**

### Usage
```javascript
Filter(filters=[<filters>])
```


---

## DropBoxListFiles

### Usage
```javascript
DropBoxListFiles()
```


---

## MakeNotebookCellMCP

**Description:** 

        Generates a function from a specific cell in the mcp_driver that is written to py/mcp_driver.py.
The function is then added to the mcp/py_mcp.json.


### Required Keys
- **project**
- **cellId**

### Optional Keys
- **model**
- **comment**

### Usage
```javascript
MakeNotebookCellMCP(project=[<project>], cellId=[<cellId>], model=[<model>], comment=[<comment>])
```


---

## GoogleListFiles

### Usage
```javascript
GoogleListFiles()
```


---

## AddOwlConcept

### Required Keys
- **database**
- **concept**
- **column**
- **dataType**
- **additionalDataType**
- **conceptual**
- **description**
- **logicalNames**

### Usage
```javascript
AddOwlConcept(database=[<database>], concept=[<concept>], column=[<column>], dataType=[<dataType>], additionalDataType=[<additionalDataType>], conceptual=[<conceptual>], description=[<description>], logicalNames=[<logicalNames>])
```


---

## BrowseAppAssets

**Description:** 

        List the files and directories from a relative filePath input from within the projects assets folder

### Required Keys
- **project**

### Optional Keys
- **filePath**

### Usage
```javascript
BrowseAppAssets(project=[<project>], filePath=[<filePath>])
```


---

## AdminGetEngineSMSS

### Required Keys
- **engine**

### Usage
```javascript
AdminGetEngineSMSS(engine=[<engine>])
```


---

## GenerateQuery

### Required Keys
- **sql**

### Usage
```javascript
GenerateQuery(sql=[<sql>])
```


---

## RemovePanelComment

### Required Keys
- **panel**
- **comment**

### Usage
```javascript
RemovePanelComment(panel=[<panel>], comment=[<comment>])
```


---

## RenameAppAsset

**Description:** 

        Rename a file or directory in the projects assets folder

### Required Keys
- **project**
- **filePath**
- **newValue**

### Optional Keys
- **comment**

### Usage
```javascript
RenameAppAsset(project=[<project>], filePath=[<filePath>], newValue=[<newValue>], comment=[<comment>])
```


---

## MakeInsightMosfet

### Required Keys
- **project**
- **id**
- **override**

### Usage
```javascript
MakeInsightMosfet(project=[<project>], id=[<id>], override=[<override>])
```


---

## RetrievePanelOrnaments

### Required Keys
- **panel**
- **traversal**

### Usage
```javascript
RetrievePanelOrnaments(panel=[<panel>], traversal=[<traversal>])
```


---

## GetEngineAssetsBase64

**Description:** 

        Retrieve the contents of an image file as base64 encoded string in the engine

### Required Keys
- **engine**
- **filePath**

### Usage
```javascript
GetEngineAssetsBase64(engine=[<engine>], filePath=[<filePath>])
```


---

## AddPrompt

### Required Keys
- **map**

### Usage
```javascript
AddPrompt(map=[<map>])
```


---

## AddPanelEvents

### Required Keys
- **panel**
- **events**

### Usage
```javascript
AddPanelEvents(panel=[<panel>], events=[<events>])
```


---

## Count

### Required Keys
- **columns**

### Usage
```javascript
Count(columns=[<columns>])
```


---

## RunNumericalCorrelation

### Required Keys
- **attributes**
- **defaultValue**
- **panel**

### Usage
```javascript
RunNumericalCorrelation(attributes=[<attributes>], defaultValue=[<defaultValue>], panel=[<panel>])
```


---

## ClearDatabaseMetadataCache

### Required Keys
- **database**

### Usage
```javascript
ClearDatabaseMetadataCache(database=[<database>])
```


---

## AddPanelConfig

### Required Keys
- **panel**
- **config**

### Usage
```javascript
AddPanelConfig(panel=[<panel>], config=[<config>])
```


---

## Database

### Required Keys
- **database**

### Usage
```javascript
Database(database=[<database>])
```


---

## RunPlaysheet

### Required Keys
- **project**
- **id**
- **params**

### Usage
```javascript
RunPlaysheet(project=[<project>], id=[<id>], params=[<params>])
```


---

## LoadNLPSearch

### Usage
```javascript
LoadNLPSearch()
```


---

## GetInsightCachedDateTime

### Usage
```javascript
GetInsightCachedDateTime()
```


---

## GetSMTPSession

### Required Keys
- **project**

### Usage
```javascript
GetSMTPSession(project=[<project>])
```


---

## TinkerCsvUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **tinkerDriver**

### Usage
```javascript
TinkerCsvUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], tinkerDriver=[<tinkerDriver>])
```


---

## OpList

### Required Keys
- **array**

### Usage
```javascript
OpList(array=[<array>])
```


---

## SetPanelSheet

### Required Keys
- **panel**
- **sheet**

### Usage
```javascript
SetPanelSheet(panel=[<panel>], sheet=[<sheet>])
```


---

## OpenTab

### Required Keys
- **webURL**

### Usage
```javascript
OpenTab(webURL=[<webURL>])
```


---

## LLM2

**Description:** 

        This method is used to run an LLM text-generation call

### Required Keys
- **engine**
- **command**

### Optional Keys
- **useHistory**
- **paramValues**
- **image**
- **context**
- **roomId**
- **url**

### Usage
```javascript
LLM2(engine=[<engine>], command=[<command>], useHistory=[<useHistory>], paramValues=[<paramValues>], image=[<image>], context=[<context>], roomId=[<roomId>], url=[<url>])
```


---

## GetTaskHeaders

### Required Keys
- **task**

### Usage
```javascript
GetTaskHeaders(task=[<task>])
```


---

## InitAppRepo

### Required Keys
- **database**
- **repository**
- **username**
- **password**
- **syncDatabase**

### Usage
```javascript
InitAppRepo(database=[<database>], repository=[<repository>], username=[<username>], password=[<password>], syncDatabase=[<syncDatabase>])
```


---

## DatabaseRenameColumn

### Required Keys
- **database**
- **concept**
- **column**
- **newValue**

### Usage
```javascript
DatabaseRenameColumn(database=[<database>], concept=[<concept>], column=[<column>], newValue=[<newValue>])
```


---

## ListStoragePath

### Required Keys
- **storage**
- **storagePath**

### Usage
```javascript
ListStoragePath(storage=[<storage>], storagePath=[<storagePath>])
```


---

## UpdatePrompt

### Required Keys
- **map**

### Usage
```javascript
UpdatePrompt(map=[<map>])
```


---

## Login

### Required Keys
- **username**
- **password**

### Usage
```javascript
Login(username=[<username>], password=[<password>])
```


---

## FrameCache

### Required Keys
- **frameCache**

### Usage
```javascript
FrameCache(frameCache=[<frameCache>])
```


---

## AdminLockAccounts

### Required Keys
- **days**

### Usage
```javascript
AdminLockAccounts(days=[<days>])
```


---

## RunPlaysheetMethod

### Required Keys
- **method**
- **params**

### Usage
```javascript
RunPlaysheetMethod(method=[<method>], params=[<params>])
```


---

## OpMax

### Required Keys
- **numValues**

### Usage
```javascript
OpMax(numValues=[<numValues>])
```


---

## MoveUserAsset

### Required Keys
- **filePath**
- **relativePath**

### Usage
```javascript
MoveUserAsset(filePath=[<filePath>], relativePath=[<relativePath>])
```


---

## AskTool

### Required Keys
- **engine**
- **command**

### Optional Keys
- **paramValues**
- **context**
- **engine_tools**
- **project_tools**

### Usage
```javascript
AskTool(engine=[<engine>], command=[<command>], paramValues=[<paramValues>], context=[<context>], engine_tools=[<engine_tools>], project_tools=[<project_tools>])
```


---

## DeleteEngineAssets

**Description:** 

        Delete a single or multiple files in the engine folder

### Required Keys
- **engine**
- **filePath**

### Usage
```javascript
DeleteEngineAssets(engine=[<engine>], filePath=[<filePath>])
```


---

## CollectAll

### Required Keys
- **task**
- **meta**

### Usage
```javascript
CollectAll(task=[<task>], meta=[<meta>])
```


---

## GetEngineAssets

**Description:** 

        Retrieve the contents of a file in the engine

### Required Keys
- **engine**
- **filePath**

### Usage
```javascript
GetEngineAssets(engine=[<engine>], filePath=[<filePath>])
```


---

## GetUserInfo

### Required Keys
- **no keys defined**

### Usage
```javascript
GetUserInfo()
```


---

## ConvertFileReference

### Required Keys
- **fileKey**

### Usage
```javascript
ConvertFileReference(fileKey=[<fileKey>])
```


---

## CreateNewRdbmsInternalDatabase

### Required Keys
- **conDetails**
- **database**
- **metamodelAdd**

### Usage
```javascript
CreateNewRdbmsInternalDatabase(conDetails=[<conDetails>], database=[<database>], metamodelAdd=[<metamodelAdd>])
```


---

## FlatMapLambda

### Required Keys
- **lambda**
- **columns**
- **params**

### Usage
```javascript
FlatMapLambda(lambda=[<lambda>], columns=[<columns>], params=[<params>])
```


---

## QueryTranslator

### Required Keys
- **query**
- **sourceDB**
- **targetDB**

### Usage
```javascript
QueryTranslator(query=[<query>], sourceDB=[<sourceDB>], targetDB=[<targetDB>])
```


---

## SnowflakeRemoveFiles

**Description:** 

        Utility method to remove a file from '@~/`<userStage>`' or '@%`<tableStage>`' or '@`<namedStage>`' on snowflake depending on the parameters provided. '@~' is for user stage, @% is for table stage, and @ is for a named stage. Snowflake docs found here: https://docs.snowflake.com/en/sql-reference/sql/remove

### Required Keys
- **database**

### Optional Keys
- **userStage**
- **tableStage**
- **namedStage**

### Usage
```javascript
SnowflakeRemoveFiles(database=[<database>], userStage=[<userStage>], tableStage=[<tableStage>], namedStage=[<namedStage>])
```


---

## RemovePackageFromVenv

### Required Keys
- **engine**
- **paramValues**

### Usage
```javascript
RemovePackageFromVenv(engine=[<engine>], paramValues=[<paramValues>])
```


---

## DeleteFromStorage

### Required Keys
- **storage**
- **storagePath**
- **leaveFolderStructure**

### Usage
```javascript
DeleteFromStorage(storage=[<storage>], storagePath=[<storagePath>], leaveFolderStructure=[<leaveFolderStructure>])
```


---

## Help

### Required Keys
- **reset**

### Usage
```javascript
Help(reset=[<reset>])
```


---

## SetProjectMetakeyOptions

### Required Keys
- **metaoptions**

### Usage
```javascript
SetProjectMetakeyOptions(metaoptions=[<metaoptions>])
```


---

## SaveTaxScenario

### Required Keys
- **no keys defined**

### Usage
```javascript
SaveTaxScenario()
```


---

## TaxIdsProcessor

### Required Keys
- **no keys defined**

### Usage
```javascript
TaxIdsProcessor()
```


---

## GroupConcat

### Required Keys
- **columns**

### Usage
```javascript
GroupConcat(columns=[<columns>])
```


---

## ConvertToQuery

### Required Keys
- **ignoreState**

### Usage
```javascript
ConvertToQuery(ignoreState=[<ignoreState>])
```


---

## RemoteEngineRun

### Required Keys
- **payload**

### Usage
```javascript
RemoteEngineRun(payload=[<payload>])
```


---

## GetProjectUserAccessRequest

### Required Keys
- **project**

### Usage
```javascript
GetProjectUserAccessRequest(project=[<project>])
```


---

## DatabaseColumnUnique

### Required Keys
- **database**
- **columns**

### Usage
```javascript
DatabaseColumnUnique(database=[<database>], columns=[<columns>])
```


---

## AddPanel

### Required Keys
- **panel**
- **sheet**

### Usage
```javascript
AddPanel(panel=[<panel>], sheet=[<sheet>])
```


---

## SetDatabaseMetadata

**Description:** 

        Define metadata on a datasource

### Required Keys
- **database**
- **meta**
- **encoded**
- **jsonCleanup**

### Usage
```javascript
SetDatabaseMetadata(database=[<database>], meta=[<meta>], encoded=[<encoded>], jsonCleanup=[<jsonCleanup>])
```


---

## GrabScalarElement

### Required Keys
- **task**
- **cleanUp**

### Usage
```javascript
GrabScalarElement(task=[<task>], cleanUp=[<cleanUp>])
```


---

## Ratio

### Required Keys
- **instance**
- **attributes**

### Usage
```javascript
Ratio(instance=[<instance>], attributes=[<attributes>])
```


---

## EmbeddedScript

### Required Keys
- **no keys defined**

### Usage
```javascript
EmbeddedScript()
```


---

## RemoteModelStart

### Required Keys
- **engine**

### Usage
```javascript
RemoteModelStart(engine=[<engine>])
```


---

## CLPModel

### Required Keys
- **conceptualNames**

### Usage
```javascript
CLPModel(conceptualNames=[<conceptualNames>])
```


---

## RetrievePanelEvents

### Required Keys
- **panel**
- **traversal**

### Usage
```javascript
RetrievePanelEvents(panel=[<panel>], traversal=[<traversal>])
```


---

## PullDatabaseFromCloud

### Required Keys
- **database**

### Usage
```javascript
PullDatabaseFromCloud(database=[<database>])
```


---

## CreateAsset

### Required Keys
- **repository**
- **fileName**
- **content**

### Usage
```javascript
CreateAsset(repository=[<repository>], fileName=[<fileName>], content=[<content>])
```


---

## EditOwlConceptDataType

### Required Keys
- **database**
- **concept**
- **dataType**
- **additionalDataType**

### Usage
```javascript
EditOwlConceptDataType(database=[<database>], concept=[<concept>], dataType=[<dataType>], additionalDataType=[<additionalDataType>])
```


---

## FrameHeaderExists

### Required Keys
- **frame**
- **column**

### Usage
```javascript
FrameHeaderExists(frame=[<frame>], column=[<column>])
```


---

## GetOptimizedRecipe

### Required Keys
- **no keys defined**

### Usage
```javascript
GetOptimizedRecipe()
```


---

## RdfLoaderSheetUpload

### Required Keys
- **database**
- **filePath**
- **existing**
- **customBaseURI**

### Usage
```javascript
RdfLoaderSheetUpload(database=[<database>], filePath=[<filePath>], existing=[<existing>], customBaseURI=[<customBaseURI>])
```


---

## SharePointListFiles

### Required Keys
- **driveId**

### Usage
```javascript
SharePointListFiles(driveId=[<driveId>])
```


---

## GetOwlDescriptions

### Required Keys
- **database**
- **concept**
- **column**

### Usage
```javascript
GetOwlDescriptions(database=[<database>], concept=[<concept>], column=[<column>])
```


---

## ReplacePanelFilter

### Required Keys
- **panel**
- **filters**
- **taskRefresh**

### Usage
```javascript
ReplacePanelFilter(panel=[<panel>], filters=[<filters>], taskRefresh=[<taskRefresh>])
```


---

## EditScheduledJob

### Required Keys
- **jobId**
- **jobName**
- **jobGroup**
- **cronExpression**
- **cronTz**
- **recipe**
- **recipeParameters**
- **triggerOnLoad**
- **triggerNow**
- **uiState**
- **curJobName**
- **curJobGroup**
- **jobTags**

### Usage
```javascript
EditScheduledJob(jobId=[<jobId>], jobName=[<jobName>], jobGroup=[<jobGroup>], cronExpression=[<cronExpression>], cronTz=[<cronTz>], recipe=[<recipe>], recipeParameters=[<recipeParameters>], triggerOnLoad=[<triggerOnLoad>], triggerNow=[<triggerNow>], uiState=[<uiState>], curJobName=[<curJobName>], curJobGroup=[<curJobGroup>], jobTags=[<jobTags>])
```


---

## GetDatabaseTableStructure

### Required Keys
- **database**

### Usage
```javascript
GetDatabaseTableStructure(database=[<database>])
```


---

## ExportToExcel

### Optional Keys
- **headers**
- **fileName**
- **panelOrderIds**
- **theader**
- **filePath**
- **project**
- **password**
- **merge**
- **limit**
- **width**
- **column_gutter**
- **placeHolderData**
- **export_template**
- **exportAudit**
- **rowgutter**
- **tfooter**
- **height**

### Usage
```javascript
ExportToExcel(headers=[<headers>], fileName=[<fileName>], panelOrderIds=[<panelOrderIds>], theader=[<theader>], filePath=[<filePath>], project=[<project>], password=[<password>], merge=[<merge>], limit=[<limit>], width=[<width>], column_gutter=[<column_gutter>], placeHolderData=[<placeHolderData>], export_template=[<export_template>], exportAudit=[<exportAudit>], rowgutter=[<rowgutter>], tfooter=[<tfooter>], height=[<height>])
```


---

## AddPanelSort

### Required Keys
- **panel**
- **columns**
- **sort**

### Usage
```javascript
AddPanelSort(panel=[<panel>], columns=[<columns>], sort=[<sort>])
```


---

## LogMessage

### Required Keys
- **message**

### Usage
```javascript
LogMessage(message=[<message>])
```


---

## ExportToPPT

### Required Keys
- **task**
- **fileName**
- **filePath**

### Usage
```javascript
ExportToPPT(task=[<task>], fileName=[<fileName>], filePath=[<filePath>])
```


---

## GetInsightFrameStructure

### Required Keys
- **project**
- **id**

### Usage
```javascript
GetInsightFrameStructure(project=[<project>], id=[<id>])
```


---

## UniqueAverage

### Required Keys
- **columns**

### Usage
```javascript
UniqueAverage(columns=[<columns>])
```


---

## DatabaseRenameTable

### Required Keys
- **database**
- **concept**
- **newValue**

### Usage
```javascript
DatabaseRenameTable(database=[<database>], concept=[<concept>], newValue=[<newValue>])
```


---

## NewEngineAssetsFile

**Description:** 

        Create a new empty file in the engine folder

### Required Keys
- **engine**
- **filePath**

### Usage
```javascript
NewEngineAssetsFile(engine=[<engine>], filePath=[<filePath>])
```


---

## Day

### Required Keys
- **days**

### Usage
```javascript
Day(days=[<days>])
```


---

## ListAllRecordsInVectorDatabase

**Description:** 

        Get the list of all the chunks that have been uploaded into this vector database. This will include the following fields: 'Source', 'Source', 'Modality', 'Divider', 'Part', 'Tokens', 'Content'

### Required Keys
- **engine**

### Optional Keys
- **paramValues**

### Usage
```javascript
ListAllRecordsInVectorDatabase(engine=[<engine>], paramValues=[<paramValues>])
```


---

## AddBulkOwlRelationships

### Required Keys
- **database**
- **frame**
- **propagation**
- **store**

### Usage
```javascript
AddBulkOwlRelationships(database=[<database>], frame=[<frame>], propagation=[<propagation>], store=[<store>])
```


---

## SetSheetLabel

### Required Keys
- **sheet**
- **sheetLabel**

### Usage
```javascript
SetSheetLabel(sheet=[<sheet>], sheetLabel=[<sheetLabel>])
```


---

## ListAppRemotes

### Required Keys
- **database**

### Usage
```javascript
ListAppRemotes(database=[<database>])
```


---

## MakePythonMCP

**Description:** 

        Generates a mcp/py_mcp.json file from the py/mcp_driver.py file function.
If the project is a no-code app, the mcp_driver notebook sheet will be transformed
into a py/mcp_driver.py file to then generate the mcp/py_mcp.json.


### Required Keys
- **project**

### Optional Keys
- **model**
- **comment**

### Usage
```javascript
MakePythonMCP(project=[<project>], model=[<model>], comment=[<comment>])
```


---

## DeleteDatabaseStructure

### Required Keys
- **database**
- **metamodelDelete**

### Usage
```javascript
DeleteDatabaseStructure(database=[<database>], metamodelDelete=[<metamodelDelete>])
```


---

## EditOwlLogicalNames

### Required Keys
- **database**
- **concept**
- **column**
- **logicalNames**

### Usage
```javascript
EditOwlLogicalNames(database=[<database>], concept=[<concept>], column=[<column>], logicalNames=[<logicalNames>])
```


---

## DeleteInsightParameter

### Required Keys
- **paramName**

### Usage
```javascript
DeleteInsightParameter(paramName=[<paramName>])
```


---

## Update

### Required Keys
- **columns**
- **values**

### Usage
```javascript
Update(columns=[<columns>], values=[<values>])
```


---

## SelectTable

### Required Keys
- **table**

### Usage
```javascript
SelectTable(table=[<table>])
```


---

## PredictOwlDescription

### Required Keys
- **database**
- **concept**
- **column**

### Usage
```javascript
PredictOwlDescription(database=[<database>], concept=[<concept>], column=[<column>])
```


---

## UploadProjectApp

**Description:** 

        Import an app from an exported zip smss-app file

### Required Keys
- **filePath**
- **space**
- **global**

### Usage
```javascript
UploadProjectApp(filePath=[<filePath>], space=[<space>], global=[<global>])
```


---

## SetAdditionalDataType

### Required Keys
- **format**
- **columns**

### Usage
```javascript
SetAdditionalDataType(format=[<format>], columns=[<columns>])
```


---

## CalcVar

### Required Keys
- **no keys defined**

### Usage
```javascript
CalcVar()
```


---

## OpSumProduct

### Required Keys
- **array**

### Usage
```javascript
OpSumProduct(array=[<array>])
```


---

## DeletePanelFilter

### Required Keys
- **panel**
- **index**
- **taskRefresh**

### Usage
```javascript
DeletePanelFilter(panel=[<panel>], index=[<index>], taskRefresh=[<taskRefresh>])
```


---

## UUID

### Required Keys
- **no keys defined**

### Usage
```javascript
UUID()
```


---

## OpNotEmpty

### Required Keys
- **values**

### Usage
```javascript
OpNotEmpty(values=[<values>])
```


---

## RemoveFrame

### Required Keys
- **variable**
- **dropNow**

### Usage
```javascript
RemoveFrame(variable=[<variable>], dropNow=[<dropNow>])
```


---

## ParallelRun

### Required Keys
- **parallel_worker**

### Usage
```javascript
ParallelRun(parallel_worker=[<parallel_worker>])
```


---

## SearchAppAssets

**Description:** 

        Recursively search files and directories within the project's assets folder, filtered by search term and optional searching flags.

### Required Keys
- **project**
- **filePath**
- **search**

### Optional Keys
- **options**

### Usage
```javascript
SearchAppAssets(project=[<project>], filePath=[<filePath>], search=[<search>], options=[<options>])
```


---

## AdminGetAllEngineUsage

### Required Keys
- **engine**
- **limit**
- **offset**
- **startDate**
- **endDate**

### Usage
```javascript
AdminGetAllEngineUsage(engine=[<engine>], limit=[<limit>], offset=[<offset>], startDate=[<startDate>], endDate=[<endDate>])
```


---

## OpConcat

### Required Keys
- **array**

### Usage
```javascript
OpConcat(array=[<array>])
```


---

## RunPlan

### Required Keys
- **no keys defined**

### Usage
```javascript
RunPlan()
```


---

## GetInsightMetakeyOptions

### Required Keys
- **metaKeys**

### Usage
```javascript
GetInsightMetakeyOptions(metaKeys=[<metaKeys>])
```


---

## CollectVizNetwork

### Required Keys
- **vizNetwork**

### Usage
```javascript
CollectVizNetwork(vizNetwork=[<vizNetwork>])
```


---

## GoogleGmailGetUnreadEmails

**Description:** 

        Get the list of unread email

### Required Keys
- **limit**

### Usage
```javascript
GoogleGmailGetUnreadEmails(limit=[<limit>])
```


---

## RunKeyAttributes

### Required Keys
- **column**

### Usage
```javascript
RunKeyAttributes(column=[<column>])
```


---

## GetEngineMarkdown

### Required Keys
- **engine**

### Usage
```javascript
GetEngineMarkdown(engine=[<engine>])
```


---

## GetMCPInternalTools

### Required Keys
- **project**

### Usage
```javascript
GetMCPInternalTools(project=[<project>])
```


---

## ReloadDatabase

### Required Keys
- **database**

### Usage
```javascript
ReloadDatabase(database=[<database>])
```


---

## AddOwlRelationship

### Required Keys
- **database**
- **startT**
- **endT**
- **startC**
- **endC**
- **store**

### Usage
```javascript
AddOwlRelationship(database=[<database>], startT=[<startT>], endT=[<endT>], startC=[<startC>], endC=[<endC>], store=[<store>])
```


---

## UniqueSum

### Required Keys
- **columns**

### Usage
```javascript
UniqueSum(columns=[<columns>])
```


---

## GetEngineUsagePerUser

### Required Keys
- **engine**
- **limit**
- **offset**
- **startDate**
- **endDate**

### Usage
```javascript
GetEngineUsagePerUser(engine=[<engine>], limit=[<limit>], offset=[<offset>], startDate=[<startDate>], endDate=[<endDate>])
```


---

## SetContext

**Description:** 

        This reactor is deprecated. Please update to LoadApp(project='') instead

### Required Keys
- **project**

### Optional Keys
- **loadPath**

### Usage
```javascript
SetContext(project=[<project>], loadPath=[<loadPath>])
```


---

## GetInsightMetamodel

### Required Keys
- **project**
- **id**

### Usage
```javascript
GetInsightMetamodel(project=[<project>], id=[<id>])
```


---

## GenerateFrameFromPyVariable

### Required Keys
- **variable**
- **override**

### Usage
```javascript
GenerateFrameFromPyVariable(variable=[<variable>], override=[<override>])
```


---

## GetNodePoolsInfo

### Required Keys
- **no keys defined**

### Usage
```javascript
GetNodePoolsInfo()
```


---

## GetAssetComment

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
GetAssetComment(filePath=[<filePath>], space=[<space>])
```


---

## OpPaste

### Required Keys
- **array**
- **sep**

### Usage
```javascript
OpPaste(array=[<array>], sep=[<sep>])
```


---

## QueryFilterComponentAnd

### Required Keys
- **filters**

### Usage
```javascript
QueryFilterComponentAnd(filters=[<filters>])
```


---

## GitlabListBranchesForProject

**Description:** 

        This reactor returns a list of JSON maps for the branches that exist for a GitLab project

### Required Keys
- **host**
- **gitlabProjectId**
- **gitlabPrivateToken**
- **useApplicationCert**

### Usage
```javascript
GitlabListBranchesForProject(host=[<host>], gitlabProjectId=[<gitlabProjectId>], gitlabPrivateToken=[<gitlabPrivateToken>], useApplicationCert=[<useApplicationCert>])
```


---

## AddMetaDescription

### Required Keys
- **database**
- **concept**
- **description**

### Usage
```javascript
AddMetaDescription(database=[<database>], concept=[<concept>], description=[<description>])
```


---

## SetFilterRefresh

### Required Keys
- **no keys defined**

### Usage
```javascript
SetFilterRefresh()
```


---

## RenameEngineAsset

**Description:** 

        Rename a file or directory in the projects assets folder

### Required Keys
- **engine**
- **filePath**
- **newValue**

### Usage
```javascript
RenameEngineAsset(engine=[<engine>], filePath=[<filePath>], newValue=[<newValue>])
```


---

## GetProjectIMAPEmailSession

### Required Keys
- **project**

### Usage
```javascript
GetProjectIMAPEmailSession(project=[<project>])
```


---

## AddPreDefinedParameter

### Required Keys
- **preDefinedParamStruct**
- **database**

### Usage
```javascript
AddPreDefinedParameter(preDefinedParamStruct=[<preDefinedParamStruct>], database=[<database>])
```


---

## RdbmsReplaceDatabaseUploadTable

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**

### Usage
```javascript
RdbmsReplaceDatabaseUploadTable(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>])
```


---

## GitlabPullJobArtifactById

**Description:** 

        This reactor pulls the artifact for a specific GitLab project job execution

### Required Keys
- **host**
- **gitlabProjectId**
- **gitlabJobId**
- **gitlabPrivateToken**
- **useApplicationCert**

### Usage
```javascript
GitlabPullJobArtifactById(host=[<host>], gitlabProjectId=[<gitlabProjectId>], gitlabJobId=[<gitlabJobId>], gitlabPrivateToken=[<gitlabPrivateToken>], useApplicationCert=[<useApplicationCert>])
```


---

## ImageCapture

### Required Keys
- **project**
- **url**
- **params**
- **imageWaitTime**

### Usage
```javascript
ImageCapture(project=[<project>], url=[<url>], params=[<params>], imageWaitTime=[<imageWaitTime>])
```


---

## SyncLocalToStorage

### Required Keys
- **storage**
- **storagePath**
- **space**
- **filePath**
- **metadata**

### Usage
```javascript
SyncLocalToStorage(storage=[<storage>], storagePath=[<storagePath>], space=[<space>], filePath=[<filePath>], metadata=[<metadata>])
```


---

## GetSpecificConceptProperties

### Required Keys
- **concept**
- **database**

### Usage
```javascript
GetSpecificConceptProperties(concept=[<concept>], database=[<database>])
```


---

## Date

**Description:** 

        Get todays date or return a date based on a specific date input and format

### Optional Keys
- **date**
- **format**

### Usage
```javascript
Date(date=[<date>], format=[<format>])
```


---

## SetInsightGoldenLayout

### Required Keys
- **no keys defined**

### Usage
```javascript
SetInsightGoldenLayout()
```


---

## UnlockEngine

### Required Keys
- **engine**

### Usage
```javascript
UnlockEngine(engine=[<engine>])
```


---

## AdminLoadLdapUsers

### Required Keys
- **searchContextName**
- **searchFilter**
- **searchContextScope**
- **authProvider**
- **updateAttributes**

### Usage
```javascript
AdminLoadLdapUsers(searchContextName=[<searchContextName>], searchFilter=[<searchFilter>], searchContextScope=[<searchContextScope>], authProvider=[<authProvider>], updateAttributes=[<updateAttributes>])
```


---

## RAlg

### Required Keys
- **filename**
- **function**

### Usage
```javascript
RAlg(filename=[<filename>], function=[<function>])
```


---

## SetParams

### Required Keys
- **pixelId**
- **value**
- **column**

### Optional Keys
- **id_type**
- **operatoru**
- **table**
- **operator**

### Usage
```javascript
SetParams(pixelId=[<pixelId>], value=[<value>], column=[<column>], id_type=[<id_type>], operatoru=[<operatoru>], table=[<table>], operator=[<operator>])
```


---

## ResetFrameToOriginalName

### Required Keys
- **frame**

### Usage
```javascript
ResetFrameToOriginalName(frame=[<frame>])
```


---

## Query

### Required Keys
- **query**

### Usage
```javascript
Query(query=[<query>])
```


---

## RemoveTask

### Required Keys
- **taskId**
- **dropNow**

### Usage
```javascript
RemoveTask(taskId=[<taskId>], dropNow=[<dropNow>])
```


---

## AdminReloadSocialProperties

### Required Keys
- **no keys defined**

### Usage
```javascript
AdminReloadSocialProperties()
```


---

## OpSum

### Required Keys
- **numValues**

### Usage
```javascript
OpSum(numValues=[<numValues>])
```


---

## NewEngineAssetsDirectory

**Description:** 

        Create a new empty directory in the engine folder

### Required Keys
- **engine**
- **filePath**

### Usage
```javascript
NewEngineAssetsDirectory(engine=[<engine>], filePath=[<filePath>])
```


---

## AddPackageToVenv

### Required Keys
- **engine**
- **paramValues**

### Usage
```javascript
AddPackageToVenv(engine=[<engine>], paramValues=[<paramValues>])
```


---

## CollectNewTemporalCol

### Required Keys
- **qs**

### Usage
```javascript
CollectNewTemporalCol(qs=[<qs>])
```


---

## SyncDatabaseWithLocalMaster

### Required Keys
- **database**

### Usage
```javascript
SyncDatabaseWithLocalMaster(database=[<database>])
```


---

## StoreValue

### Required Keys
- **no keys defined**

### Usage
```javascript
StoreValue()
```


---

## SearchAppCollaborator

### Required Keys
- **collaborator**
- **username**
- **password**

### Usage
```javascript
SearchAppCollaborator(collaborator=[<collaborator>], username=[<username>], password=[<password>])
```


---

## GetImageAsset

### Required Keys
- **filePath**

### Optional Keys
- **version**
- **space**

### Usage
```javascript
GetImageAsset(filePath=[<filePath>], version=[<version>], space=[<space>])
```


---

## ExternalUpdateJdbcTablesAndViews

### Required Keys
- **database**

### Usage
```javascript
ExternalUpdateJdbcTablesAndViews(database=[<database>])
```


---

## DownloadEngineAsset

**Description:** 

        Download a file or directory from within the engine folder

### Required Keys
- **engine**

### Optional Keys
- **filePath**

### Usage
```javascript
DownloadEngineAsset(engine=[<engine>], filePath=[<filePath>])
```


---

## Purge

### Required Keys
- **frame**
- **filters**

### Usage
```javascript
Purge(frame=[<frame>], filters=[<filters>])
```


---

## AddBlock

### Required Keys
- **name**

### Optional Keys
- **json**
- **section**

### Usage
```javascript
AddBlock(name=[<name>], json=[<json>], section=[<section>])
```


---

## ProjectInfo

### Required Keys
- **project**
- **metaKeys**

### Usage
```javascript
ProjectInfo(project=[<project>], metaKeys=[<metaKeys>])
```


---

## OpPaste0

### Required Keys
- **array**
- **sep**

### Usage
```javascript
OpPaste0(array=[<array>], sep=[<sep>])
```


---

## PauseJobTrigger

### Required Keys
- **jobId**
- **jobGroup**

### Usage
```javascript
PauseJobTrigger(jobId=[<jobId>], jobGroup=[<jobGroup>])
```


---

## OpMin

### Required Keys
- **numValues**

### Usage
```javascript
OpMin(numValues=[<numValues>])
```


---

## GoogleGmailList

**Description:** 

        Get the list of emails

### Required Keys
- **limit**

### Usage
```javascript
GoogleGmailList(limit=[<limit>])
```


---

## OpArrayLength

### Required Keys
- **array**

### Usage
```javascript
OpArrayLength(array=[<array>])
```


---

## GetAvailableTags

### Required Keys
- **project**

### Usage
```javascript
GetAvailableTags(project=[<project>])
```


---

## ToExcel

### Required Keys
- **task**
- **fileName**
- **filePath**
- **password**
- **panel**

### Usage
```javascript
ToExcel(task=[<task>], fileName=[<fileName>], filePath=[<filePath>], password=[<password>], panel=[<panel>])
```


---

## RunGPT2Description

### Required Keys
- **descriptionType**
- **database**
- **table**
- **numDescriptions**

### Usage
```javascript
RunGPT2Description(descriptionType=[<descriptionType>], database=[<database>], table=[<table>], numDescriptions=[<numDescriptions>])
```


---

## ProjectR

### Required Keys
- **code**
- **project**

### Usage
```javascript
ProjectR(code=[<code>], project=[<project>])
```


---

## GetAllEngineUsage

### Required Keys
- **engine**
- **limit**
- **offset**
- **startDate**
- **endDate**

### Usage
```javascript
GetAllEngineUsage(engine=[<engine>], limit=[<limit>], offset=[<offset>], startDate=[<startDate>], endDate=[<endDate>])
```


---

## Offset

### Required Keys
- **numValue**

### Usage
```javascript
Offset(numValue=[<numValue>])
```


---

## GetTraversalOptions

### Required Keys
- **logical**
- **values**

### Usage
```javascript
GetTraversalOptions(logical=[<logical>], values=[<values>])
```


---

## SetInsightParamValue

### Required Keys
- **paramName**
- **paramValue**

### Usage
```javascript
SetInsightParamValue(paramName=[<paramName>], paramValue=[<paramValue>])
```


---

## Week

### Required Keys
- **columns**

### Usage
```javascript
Week(columns=[<columns>])
```


---

## GreedyJsonProcessor

### Required Keys
- **no keys defined**

### Usage
```javascript
GreedyJsonProcessor()
```


---

## GetJanusGraphProperties

### Required Keys
- **filePath**

### Usage
```javascript
GetJanusGraphProperties(filePath=[<filePath>])
```


---

## SnowflakeListFiles

**Description:** 

        Utility method to return the 'list @~/`<userStage>`' or 'list @%`<tableStage>`' or 'list @`<namedStage>`' query on snowflake depending on the parameters provided. '@~' is for user stage, @% is for table stage, and @ is for a named stage. Snowflake docs found here: https://docs.snowflake.com/en/user-guide/data-load-local-file-system-stage

### Required Keys
- **database**

### Optional Keys
- **userStage**
- **tableStage**
- **namedStage**

### Usage
```javascript
SnowflakeListFiles(database=[<database>], userStage=[<userStage>], tableStage=[<tableStage>], namedStage=[<namedStage>])
```


---

## DeleteTemplate

### Required Keys
- **project**
- **template_name**
- **template_file**

### Usage
```javascript
DeleteTemplate(project=[<project>], template_name=[<template_name>], template_file=[<template_file>])
```


---

## DeletePrompt

### Required Keys
- **promptId**

### Usage
```javascript
DeletePrompt(promptId=[<promptId>])
```


---

## RdfConvertLiteralType

### Required Keys
- **database**
- **concept**
- **column**
- **dataType**

### Usage
```javascript
RdfConvertLiteralType(database=[<database>], concept=[<concept>], column=[<column>], dataType=[<dataType>])
```


---

## Embeddings

**Description:** 

        This reactor is used to interact with Embedding Model Engines. If the model does not support embeddings it will return "This model does not support embeddings."

### Required Keys
- **engine**
- **values**

### Optional Keys
- **paramValues**
- **encoded**

### Usage
```javascript
Embeddings(engine=[<engine>], values=[<values>], paramValues=[<paramValues>], encoded=[<encoded>])
```


---

## GoogleGmailProfileById

**Description:** 

        Get the user profile details

### Usage
```javascript
GoogleGmailProfileById()
```


---

## SearchEngineAssets

**Description:** 

        Recursively search files and directories within the engine's assets folder, filtered by search term and optional searching flags.

### Required Keys
- **engine**
- **filePath**
- **search**

### Optional Keys
- **options**

### Usage
```javascript
SearchEngineAssets(engine=[<engine>], filePath=[<filePath>], search=[<search>], options=[<options>])
```


---

## RemoveFrameFilter

### Required Keys
- **filters**
- **taskRefresh**

### Usage
```javascript
RemoveFrameFilter(filters=[<filters>], taskRefresh=[<taskRefresh>])
```


---

## MyDiscoverableEngines

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **engineTypes**
- **engine**
- **metaKeys**
- **metaFilters**
- **noMeta**

### Usage
```javascript
MyDiscoverableEngines(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], engineTypes=[<engineTypes>], engine=[<engine>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], noMeta=[<noMeta>])
```


---

## GetXrayConfigList

### Required Keys
- **no keys defined**

### Usage
```javascript
GetXrayConfigList()
```


---

## ReloadInsightClasses

**Description:** 

        This reactor is deprecated. Please use CompileAppReactors(project='', release='') instead

### Required Keys
- **project**
- **release**

### Usage
```javascript
ReloadInsightClasses(project=[<project>], release=[<release>])
```


---

## GetFrameFilterState

### Required Keys
- **column**
- **filterWord**
- **limit**
- **offset**
- **panel**
- **dynamic**
- **optionsCache**

### Usage
```javascript
GetFrameFilterState(column=[<column>], filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], panel=[<panel>], dynamic=[<dynamic>], optionsCache=[<optionsCache>])
```


---

## AddDatabaseIndex

### Required Keys
- **database**
- **concept**
- **column**

### Optional Keys
- **force**
- **createIndex**

### Usage
```javascript
AddDatabaseIndex(database=[<database>], concept=[<concept>], column=[<column>], force=[<force>], createIndex=[<createIndex>])
```


---

## GetUserMetakeyOptions

**Description:** 

        Retrieve information about possible metadata keys applicable to users

### Required Keys
- **metaKeys**

### Usage
```javascript
GetUserMetakeyOptions(metaKeys=[<metaKeys>])
```


---

## CurrentFrame

### Required Keys
- **no keys defined**

### Usage
```javascript
CurrentFrame()
```


---

## RequestInsight

### Required Keys
- **project**
- **id**
- **permission**
- **comment**

### Usage
```javascript
RequestInsight(project=[<project>], id=[<id>], permission=[<permission>], comment=[<comment>])
```


---

## UpdateForms

### Required Keys
- **database**
- **form_input**

### Usage
```javascript
UpdateForms(database=[<database>], form_input=[<form_input>])
```


---

## GetUserActiveSavedInsights

### Required Keys
- **no keys defined**

### Usage
```javascript
GetUserActiveSavedInsights()
```


---

## AdminGetEngineMarkdown

### Required Keys
- **engine**

### Usage
```javascript
AdminGetEngineMarkdown(engine=[<engine>])
```


---

## ListWorkspaces

### Required Keys
- **limit**
- **offset**
- **filters**
- **sort**

### Usage
```javascript
ListWorkspaces(limit=[<limit>], offset=[<offset>], filters=[<filters>], sort=[<sort>])
```


---

## SharePointWebDavPull

### Required Keys
- **path**

### Usage
```javascript
SharePointWebDavPull(path=[<path>])
```


---

## UpdateEngineAppLink

### Required Keys
- **engine**
- **project**

### Usage
```javascript
UpdateEngineAppLink(engine=[<engine>], project=[<project>])
```


---

## RemoveOwlProperty

### Required Keys
- **database**
- **concept**
- **column**

### Usage
```javascript
RemoveOwlProperty(database=[<database>], concept=[<concept>], column=[<column>])
```


---

## LoadApp

**Description:** 

        Set the context for the insight in order to have access to app assets including custom reactors and python scripts

### Required Keys
- **project**

### Optional Keys
- **loadPath**

### Usage
```javascript
LoadApp(project=[<project>], loadPath=[<loadPath>])
```


---

## Year

### Required Keys
- **columns**

### Usage
```javascript
Year(columns=[<columns>])
```


---

## bdel

### Required Keys
- **fancy**

### Usage
```javascript
bdel(fancy=[<fancy>])
```


---

## CheckPyPackages

### Required Keys
- **reload**

### Usage
```javascript
CheckPyPackages(reload=[<reload>])
```


---

## CreateRoom

### Optional Keys
- **vectorDb**
- **function**
- **name**
- **context**
- **workspaceId**

### Usage
```javascript
CreateRoom(vectorDb=[<vectorDb>], function=[<function>], name=[<name>], context=[<context>], workspaceId=[<workspaceId>])
```


---

## ProjectGitDetails

### Required Keys
- **project**

### Usage
```javascript
ProjectGitDetails(project=[<project>])
```


---

## FileReference

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
FileReference(filePath=[<filePath>], space=[<space>])
```


---

## OpFilter

### Required Keys
- **no keys defined**

### Usage
```javascript
OpFilter()
```


---

## ResetAllFilters

### Required Keys
- **no keys defined**

### Usage
```javascript
ResetAllFilters()
```


---

## GetInsightParameters

### Required Keys
- **no keys defined**

### Usage
```javascript
GetInsightParameters()
```


---

## ReplayRecipe

### Required Keys
- **no keys defined**

### Usage
```javascript
ReplayRecipe()
```


---

## GetShareSessionToken

### Required Keys
- **no keys defined**

### Usage
```javascript
GetShareSessionToken()
```


---

## OpIsEmpty

### Required Keys
- **values**

### Usage
```javascript
OpIsEmpty(values=[<values>])
```


---

## EscapeAsset

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
EscapeAsset(filePath=[<filePath>], space=[<space>])
```


---

## AdminGetProjectMarkdown

### Required Keys
- **project**

### Usage
```javascript
AdminGetProjectMarkdown(project=[<project>])
```


---

## SaveEngineAssets

**Description:** 

        Save a single or multiple files in the projects assets folder

### Required Keys
- **engine**
- **filePath**
- **content**

### Usage
```javascript
SaveEngineAssets(engine=[<engine>], filePath=[<filePath>], content=[<content>])
```


---

## CanItRun

### Required Keys
- **hfModelId**

### Usage
```javascript
CanItRun(hfModelId=[<hfModelId>])
```


---

## PredictMetamodel

### Required Keys
- **filePath**
- **space**
- **delimiter**
- **rowCount**

### Usage
```javascript
PredictMetamodel(filePath=[<filePath>], space=[<space>], delimiter=[<delimiter>], rowCount=[<rowCount>])
```


---

## GetPanelFilterState

### Required Keys
- **column**
- **filterWord**
- **limit**
- **offset**
- **panel**

### Usage
```javascript
GetPanelFilterState(column=[<column>], filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], panel=[<panel>])
```


---

## ProfileValidator

### Required Keys
- **no keys defined**

### Usage
```javascript
ProfileValidator()
```


---

## ValidateR

### Required Keys
- **script**

### Usage
```javascript
ValidateR(script=[<script>])
```


---

## Version

### Required Keys
- **reload**

### Usage
```javascript
Version(reload=[<reload>])
```


---

## ListPrompt

### Required Keys
- **limit**
- **offset**
- **filters**
- **metaKeys**
- **metaFilters**

### Usage
```javascript
ListPrompt(limit=[<limit>], offset=[<offset>], filters=[<filters>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>])
```


---

## VectorFileDownload

### Required Keys
- **engine**
- **fileNames**

### Usage
```javascript
VectorFileDownload(engine=[<engine>], fileNames=[<fileNames>])
```


---

## ExtractDatabaseMeta

### Required Keys
- **database**
- **descriptions**

### Usage
```javascript
ExtractDatabaseMeta(database=[<database>], descriptions=[<descriptions>])
```


---

## PushProjectFolderToCloud

### Required Keys
- **project**

### Usage
```javascript
PushProjectFolderToCloud(project=[<project>])
```


---

## EngineActivity

### Required Keys
- **engine**

### Usage
```javascript
EngineActivity(engine=[<engine>])
```


---

## HyperParameters

### Required Keys
- **no keys defined**

### Usage
```javascript
HyperParameters()
```


---

## MyProjects

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **onlyFavorites**
- **metaKeys**
- **metaFilters**
- **permissionFilters**
- **noMeta**
- **onlyPortals**
- **userT**

### Usage
```javascript
MyProjects(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], onlyFavorites=[<onlyFavorites>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], permissionFilters=[<permissionFilters>], noMeta=[<noMeta>], onlyPortals=[<onlyPortals>], userT=[<userT>])
```


---

## AdminExecQuery

### Required Keys
- **qs**

### Usage
```javascript
AdminExecQuery(qs=[<qs>])
```


---

## SaveAppAssets

**Description:** 

        Save a single or multiple files in the projects assets folder

### Required Keys
- **project**
- **filePath**
- **content**

### Optional Keys
- **comment**

### Usage
```javascript
SaveAppAssets(project=[<project>], filePath=[<filePath>], content=[<content>], comment=[<comment>])
```


---

## GetDatabaseUserAccessRequest

### Required Keys
- **database**

### Usage
```javascript
GetDatabaseUserAccessRequest(database=[<database>])
```


---

## PushDatabaseToCloud

### Required Keys
- **database**

### Usage
```javascript
PushDatabaseToCloud(database=[<database>])
```


---

## GetInsightMetaValues

### Required Keys
- **metaKeys**

### Usage
```javascript
GetInsightMetaValues(metaKeys=[<metaKeys>])
```


---

## CopyInsight

### Required Keys
- **recipe**
- **limit**
- **drop**

### Usage
```javascript
CopyInsight(recipe=[<recipe>], limit=[<limit>], drop=[<drop>])
```


---

## GzipFile

**Description:** 

        This reactor is used to gzip a file

### Required Keys
- **filePath**

### Optional Keys
- **fileName**
- **space**

### Usage
```javascript
GzipFile(filePath=[<filePath>], fileName=[<fileName>], space=[<space>])
```


---

## WriteObjectToFile

### Required Keys
- **value**
- **encoded**
- **filePath**
- **space**

### Usage
```javascript
WriteObjectToFile(value=[<value>], encoded=[<encoded>], filePath=[<filePath>], space=[<space>])
```


---

## OneDriveListFiles

### Usage
```javascript
OneDriveListFiles()
```


---

## DownloadCurrentRecipe

### Required Keys
- **no keys defined**

### Usage
```javascript
DownloadCurrentRecipe()
```


---

## GetModelMaxTokenLength

### Required Keys
- **engine**

### Usage
```javascript
GetModelMaxTokenLength(engine=[<engine>])
```


---

## ResetPanelEvents

### Required Keys
- **panel**

### Usage
```javascript
ResetPanelEvents(panel=[<panel>])
```


---

## GetExcelForm

### Required Keys
- **filePath**
- **database**
- **sheetName**

### Usage
```javascript
GetExcelForm(filePath=[<filePath>], database=[<database>], sheetName=[<sheetName>])
```


---

## AddPanelComment

### Required Keys
- **panel**
- **comment**

### Usage
```javascript
AddPanelComment(panel=[<panel>], comment=[<comment>])
```


---

## SetFrameFilter

### Required Keys
- **filters**
- **taskRefresh**

### Usage
```javascript
SetFrameFilter(filters=[<filters>], taskRefresh=[<taskRefresh>])
```


---

## CreateModelEngine

### Required Keys
- **model**
- **modelDetails**
- **global**

### Usage
```javascript
CreateModelEngine(model=[<model>], modelDetails=[<modelDetails>], global=[<global>])
```


---

## DeleteInsightCache

### Required Keys
- **project**
- **id**
- **paramValues**

### Usage
```javascript
DeleteInsightCache(project=[<project>], id=[<id>], paramValues=[<paramValues>])
```


---

## SetPanelFilter

### Required Keys
- **panel**
- **filters**
- **taskRefresh**

### Usage
```javascript
SetPanelFilter(panel=[<panel>], filters=[<filters>], taskRefresh=[<taskRefresh>])
```


---

## GetProjectStatus

### Required Keys
- **project**

### Usage
```javascript
GetProjectStatus(project=[<project>])
```


---

## ParseMetamodel

### Required Keys
- **filePath**
- **space**
- **delimiter**
- **rowCount**
- **propFile**

### Usage
```javascript
ParseMetamodel(filePath=[<filePath>], space=[<space>], delimiter=[<delimiter>], rowCount=[<rowCount>], propFile=[<propFile>])
```


---

## With

### Required Keys
- **panel**

### Usage
```javascript
With(panel=[<panel>])
```


---

## CheckEngineName

### Required Keys
- **engine**

### Usage
```javascript
CheckEngineName(engine=[<engine>])
```


---

## AdminMyProjects

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **project**
- **metaKeys**
- **metaFilters**
- **noMeta**
- **userT**

### Usage
```javascript
AdminMyProjects(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], project=[<project>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>], noMeta=[<noMeta>], userT=[<userT>])
```


---

## GetQueriedDatabases

### Required Keys
- **no keys defined**

### Usage
```javascript
GetQueriedDatabases()
```


---

## AdditionalTypesHelp

### Required Keys
- **no keys defined**

### Usage
```javascript
AdditionalTypesHelp()
```


---

## RunMCPTool

**Description:** 

        Execute a tool defined in the app

### Required Keys
- **project**
- **function**
- **paramValues**

### Usage
```javascript
RunMCPTool(project=[<project>], function=[<function>], paramValues=[<paramValues>])
```


---

## GetEngineUserAccessRequest

### Required Keys
- **engine**

### Usage
```javascript
GetEngineUserAccessRequest(engine=[<engine>])
```


---

## MakePortal

### Required Keys
- **project**
- **portalName**
- **client**

### Optional Keys
- **reconf**
- **archetype**
- **version**

### Usage
```javascript
MakePortal(project=[<project>], portalName=[<portalName>], client=[<client>], reconf=[<reconf>], archetype=[<archetype>], version=[<version>])
```


---

## AddFilterModelState

### Required Keys
- **panel**
- **frame**
- **filters**

### Usage
```javascript
AddFilterModelState(panel=[<panel>], frame=[<frame>], filters=[<filters>])
```


---

## CreatePythonFunctionEngine

### Required Keys
- **function**
- **functionDetails**
- **content**

### Usage
```javascript
CreatePythonFunctionEngine(function=[<function>], functionDetails=[<functionDetails>], content=[<content>])
```


---

## AddSheet

### Required Keys
- **sheet**

### Usage
```javascript
AddSheet(sheet=[<sheet>])
```


---

## GetOwlDictionary

### Required Keys
- **database**

### Usage
```javascript
GetOwlDictionary(database=[<database>])
```


---

## GetEngineSMSS

### Required Keys
- **engine**

### Usage
```javascript
GetEngineSMSS(engine=[<engine>])
```


---

## RemoveAppCollaborator

### Required Keys
- **repository**
- **collaborator**
- **username**
- **password**

### Usage
```javascript
RemoveAppCollaborator(repository=[<repository>], collaborator=[<collaborator>], username=[<username>], password=[<password>])
```


---

## RetrieveInsightOrnament

### Required Keys
- **no keys defined**

### Usage
```javascript
RetrieveInsightOrnament()
```


---

## Command

### Required Keys
- **command**

### Usage
```javascript
Command(command=[<command>])
```


---

## PredictOwlLogicalNames

### Required Keys
- **database**
- **concept**
- **column**

### Usage
```javascript
PredictOwlLogicalNames(database=[<database>], concept=[<concept>], column=[<column>])
```


---

## GetMCPResourcesTemplates

### Required Keys
- **project**

### Usage
```javascript
GetMCPResourcesTemplates(project=[<project>])
```


---

## CachedPanelClone

### Required Keys
- **panel**
- **cloneId**

### Usage
```javascript
CachedPanelClone(panel=[<panel>], cloneId=[<cloneId>])
```


---

## TextToSQL

**Description:** 

        Convert natural language to SQL. Also allows users to pass in old SQL and SQL errors to generate new SQL

### Required Keys
- **database**
- **model**
- **command**

### Optional Keys
- **paramValues**
- **error**
- **sql**

### Usage
```javascript
TextToSQL(database=[<database>], model=[<model>], command=[<command>], paramValues=[<paramValues>], error=[<error>], sql=[<sql>])
```


---

## PressKey

**Description:** 

        Key to press on the Browser App rendered on the server.

### Required Keys
- **input**

### Usage
```javascript
PressKey(input=[<input>])
```


---

## CancelR

### Required Keys
- **no keys defined**

### Usage
```javascript
CancelR()
```


---

## OpIsObjectEmpty

### Required Keys
- **values**

### Usage
```javascript
OpIsObjectEmpty(values=[<values>])
```


---

## SubQueryExpression

### Required Keys
- **qs**

### Usage
```javascript
SubQueryExpression(qs=[<qs>])
```


---

## Import

### Required Keys
- **qs**
- **frame**

### Usage
```javascript
Import(qs=[<qs>], frame=[<frame>])
```


---

## CopyEnginePermissions

### Required Keys
- **sourceEngine**
- **targetEngine**

### Usage
```javascript
CopyEnginePermissions(sourceEngine=[<sourceEngine>], targetEngine=[<targetEngine>])
```


---

## GetCurrentInsightId

### Required Keys
- **no keys defined**

### Usage
```javascript
GetCurrentInsightId()
```


---

## ToPPT

### Required Keys
- **baseUrl**
- **url**
- **fileName**
- **filePath**
- **imageWaitTime**

### Usage
```javascript
ToPPT(baseUrl=[<baseUrl>], url=[<url>], fileName=[<fileName>], filePath=[<filePath>], imageWaitTime=[<imageWaitTime>])
```


---

## SemanticDescription

### Required Keys
- **input**

### Usage
```javascript
SemanticDescription(input=[<input>])
```


---

## RdbmsReconnect

### Required Keys
- **database**

### Usage
```javascript
RdbmsReconnect(database=[<database>])
```


---

## RunAliasMatch

### Required Keys
- **no keys defined**

### Usage
```javascript
RunAliasMatch()
```


---

## InitMCP

### Required Keys
- **project**
- **protocolVersion**

### Usage
```javascript
InitMCP(project=[<project>], protocolVersion=[<protocolVersion>])
```


---

## OneDriveUploader

### Required Keys
- **filename**

### Usage
```javascript
OneDriveUploader(filename=[<filename>])
```


---

## Vector

### Required Keys
- **no keys defined**

### Usage
```javascript
Vector()
```


---

## GoogleGmailReadEmail

**Description:** 

        Get the contents of an email based on the id

### Required Keys
- **id**

### Usage
```javascript
GoogleGmailReadEmail(id=[<id>])
```


---

## AdminPushLocalToCloud

### Required Keys
- **dryRun**

### Usage
```javascript
AdminPushLocalToCloud(dryRun=[<dryRun>])
```


---

## GetAppConfig

### Required Keys
- **no keys defined**

### Usage
```javascript
GetAppConfig()
```


---

## ProfileProcessor

### Required Keys
- **no keys defined**

### Usage
```javascript
ProfileProcessor()
```


---

## ChangeOption

### Required Keys
- **querypart**

### Usage
```javascript
ChangeOption(querypart=[<querypart>])
```


---

## CloseSheet

### Required Keys
- **sheet**

### Usage
```javascript
CloseSheet(sheet=[<sheet>])
```


---

## CopyAppRepo

### Required Keys
- **database**
- **repository**

### Usage
```javascript
CopyAppRepo(database=[<database>], repository=[<repository>])
```


---

## ReplaceFrameFilter

### Required Keys
- **filters**
- **taskRefresh**

### Usage
```javascript
ReplaceFrameFilter(filters=[<filters>], taskRefresh=[<taskRefresh>])
```


---

## SetCurrentFrame

### Required Keys
- **frame**

### Usage
```javascript
SetCurrentFrame(frame=[<frame>])
```


---

## EditOwlConceptConceptualName

### Required Keys
- **database**
- **concept**
- **conceptual**

### Usage
```javascript
EditOwlConceptConceptualName(database=[<database>], concept=[<concept>], conceptual=[<conceptual>])
```


---

## RdbmsReplaceDatabaseCsvUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **createIndex**

### Usage
```javascript
RdbmsReplaceDatabaseCsvUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], createIndex=[<createIndex>])
```


---

## SharePointFileRetriever

### Required Keys
- **driveId**
- **fileId**

### Usage
```javascript
SharePointFileRetriever(driveId=[<driveId>], fileId=[<fileId>])
```


---

## FrameFilterWithSQL

### Required Keys
- **frame**
- **query**

### Usage
```javascript
FrameFilterWithSQL(frame=[<frame>], query=[<query>])
```


---

## GetDatabaseMetaValues

### Required Keys
- **metaKeys**

### Usage
```javascript
GetDatabaseMetaValues(metaKeys=[<metaKeys>])
```


---

## ListAppCollaborators

### Required Keys
- **repository**
- **username**
- **password**

### Usage
```javascript
ListAppCollaborators(repository=[<repository>], username=[<username>], password=[<password>])
```


---

## Map

### Required Keys
- **no keys defined**

### Usage
```javascript
Map()
```


---

## SetInsightMetakeyOptions

### Required Keys
- **metaoptions**

### Usage
```javascript
SetInsightMetakeyOptions(metaoptions=[<metaoptions>])
```


---

## FindSemanticColumnOwlRelationships

### Required Keys
- **database**
- **tables**
- **store**

### Usage
```javascript
FindSemanticColumnOwlRelationships(database=[<database>], tables=[<tables>], store=[<store>])
```


---

## IfQueryFilterComponentOr

### Required Keys
- **filters**

### Usage
```javascript
IfQueryFilterComponentOr(filters=[<filters>])
```


---

## RefreshAllPanelTasks

### Required Keys
- **limit**

### Usage
```javascript
RefreshAllPanelTasks(limit=[<limit>])
```


---

## Frame

### Required Keys
- **frame**

### Usage
```javascript
Frame(frame=[<frame>])
```


---

## GetProjectDependencies

**Description:** 

        Set the engine dependencies for a project

### Required Keys
- **project**

### Usage
```javascript
GetProjectDependencies(project=[<project>])
```


---

## UpdateRoomOptions

### Required Keys
- **roomId**

### Optional Keys
- **roomOptions**

### Usage
```javascript
UpdateRoomOptions(roomId=[<roomId>], roomOptions=[<roomOptions>])
```


---

## EmbedderKeywordExtraction

**Description:** 

        Utilizes a keyBERT model to extract the keywords from the text input

### Required Keys
- **model**
- **input**

### Optional Keys
- **percentile**
- **limit**

### Usage
```javascript
EmbedderKeywordExtraction(model=[<model>], input=[<input>], percentile=[<percentile>], limit=[<limit>])
```


---

## CreateProject

### Required Keys
- **project**
- **projectType**
- **global**
- **portal**
- **portalName**
- **provider**
- **url**

### Usage
```javascript
CreateProject(project=[<project>], projectType=[<projectType>], global=[<global>], portal=[<portal>], portalName=[<portalName>], provider=[<provider>], url=[<url>])
```


---

## UserActivity

### Required Keys
- **no keys defined**

### Usage
```javascript
UserActivity()
```


---

## Rerank

### Required Keys
- **function**

### Optional Keys
- **limit**

### Usage
```javascript
Rerank(function=[<function>], limit=[<limit>])
```


---

## RemoveUserRoom

### Required Keys
- **roomId**

### Usage
```javascript
RemoveUserRoom(roomId=[<roomId>])
```


---

## SimilarCatalog

### Required Keys
- **database**

### Usage
```javascript
SimilarCatalog(database=[<database>])
```


---

## TaxRetrieveValue2

### Required Keys
- **no keys defined**

### Usage
```javascript
TaxRetrieveValue2()
```


---

## DirectJdbcConnection

### Required Keys
- **query**
- **conDetails**

### Usage
```javascript
DirectJdbcConnection(query=[<query>], conDetails=[<conDetails>])
```


---

## FrameFilterEmptyValues

### Required Keys
- **columns**

### Usage
```javascript
FrameFilterEmptyValues(columns=[<columns>])
```


---

## ListAllJobs

### Required Keys
- **project**
- **username**
- **myJobs**
- **jobTags**

### Usage
```javascript
ListAllJobs(project=[<project>], username=[<username>], myJobs=[<myJobs>], jobTags=[<jobTags>])
```


---

## GetHTML

**Description:** 

        Get the current HTML of the Browser App rendered on the server.

### Required Keys
- **no keys defined**

### Usage
```javascript
GetHTML()
```


---

## StandardDeviation

### Required Keys
- **columns**

### Usage
```javascript
StandardDeviation(columns=[<columns>])
```


---

## RemoveOwlRelationship

### Required Keys
- **database**
- **startT**
- **endT**
- **startC**
- **endC**
- **store**

### Usage
```javascript
RemoveOwlRelationship(database=[<database>], startT=[<startT>], endT=[<endT>], startC=[<startC>], endC=[<endC>], store=[<store>])
```


---

## ImplicitFilterOverride

### Required Keys
- **no keys defined**

### Usage
```javascript
ImplicitFilterOverride()
```


---

## AdminGetProjectUsage

### Required Keys
- **project**

### Usage
```javascript
AdminGetProjectUsage(project=[<project>])
```


---

## DayName

### Required Keys
- **columns**

### Usage
```javascript
DayName(columns=[<columns>])
```


---

## GetAsset

### Required Keys
- **filePath**

### Optional Keys
- **version**
- **space**

### Usage
```javascript
GetAsset(filePath=[<filePath>], version=[<version>], space=[<space>])
```


---

## ReloadDatabaseOwl

### Required Keys
- **database**

### Usage
```javascript
ReloadDatabaseOwl(database=[<database>])
```


---

## SetPanelLabel

### Required Keys
- **panel**
- **panelLabel**

### Usage
```javascript
SetPanelLabel(panel=[<panel>], panelLabel=[<panelLabel>])
```


---

## GetUserConversationRooms

### Optional Keys
- **project**

### Usage
```javascript
GetUserConversationRooms(project=[<project>])
```


---

## RenameRoom

### Required Keys
- **roomId**
- **name**

### Usage
```javascript
RenameRoom(roomId=[<roomId>], name=[<name>])
```


---

## CreateExternalGraphDatabase

### Required Keys
- **database**
- **filePath**
- **space**
- **graphTypeId**
- **graphNameId**
- **graphMetamodel**
- **useLabel**

### Usage
```javascript
CreateExternalGraphDatabase(database=[<database>], filePath=[<filePath>], space=[<space>], graphTypeId=[<graphTypeId>], graphNameId=[<graphNameId>], graphMetamodel=[<graphMetamodel>], useLabel=[<useLabel>])
```


---

## OpSmall

### Required Keys
- **numValues**

### Usage
```javascript
OpSmall(numValues=[<numValues>])
```


---

## WidgetT

### Required Keys
- **array**

### Usage
```javascript
WidgetT(array=[<array>])
```


---

## VariableExists

### Required Keys
- **variable**

### Usage
```javascript
VariableExists(variable=[<variable>])
```


---

## OpSumIfs

### Required Keys
- **no keys defined**

### Usage
```javascript
OpSumIfs()
```


---

## Panel

### Required Keys
- **panel**

### Usage
```javascript
Panel(panel=[<panel>])
```


---

## UpdateInsight

### Required Keys
- **project**
- **insightName**
- **id**
- **layout**
- **global**
- **recipe**
- **description**
- **tags**
- **params**
- **image**
- **encoded**
- **cache**
- **cacheMinutes**
- **schemaName**

### Usage
```javascript
UpdateInsight(project=[<project>], insightName=[<insightName>], id=[<id>], layout=[<layout>], global=[<global>], recipe=[<recipe>], description=[<description>], tags=[<tags>], params=[<params>], image=[<image>], encoded=[<encoded>], cache=[<cache>], cacheMinutes=[<cacheMinutes>], schemaName=[<schemaName>])
```


---

## DetoxifyGuardrailEngine

### Required Keys
- **prompt**
- **threshold**

### Usage
```javascript
DetoxifyGuardrailEngine(prompt=[<prompt>], threshold=[<threshold>])
```


---

## AllConceptualNames

### Required Keys
- **no keys defined**

### Usage
```javascript
AllConceptualNames()
```


---

## DateFormat

### Required Keys
- **columns**

### Usage
```javascript
DateFormat(columns=[<columns>])
```


---

## AdminEngineInfo

### Required Keys
- **engine**
- **metaKeys**

### Usage
```javascript
AdminEngineInfo(engine=[<engine>], metaKeys=[<metaKeys>])
```


---

## NER

### Required Keys
- **engine**
- **prompt**
- **entities**

### Optional Keys
- **paramValues**
- **maskEntities**

### Usage
```javascript
NER(engine=[<engine>], prompt=[<prompt>], entities=[<entities>], paramValues=[<paramValues>], maskEntities=[<maskEntities>])
```


---

## Clipboard

### Usage
```javascript
Clipboard()
```


---

## QuerySelectorExpressionAssimilator

### Required Keys
- **no keys defined**

### Usage
```javascript
QuerySelectorExpressionAssimilator()
```


---

## ToDatabase

### Required Keys
- **task**
- **targetDatabase**
- **targetTable**
- **override**
- **insertId**
- **ignoreOWL**
- **batchSize**

### Usage
```javascript
ToDatabase(task=[<task>], targetDatabase=[<targetDatabase>], targetTable=[<targetTable>], override=[<override>], insertId=[<insertId>], ignoreOWL=[<ignoreOWL>], batchSize=[<batchSize>])
```


---

## GetMetaTags

### Required Keys
- **database**
- **concept**

### Usage
```javascript
GetMetaTags(database=[<database>], concept=[<concept>])
```


---

## SetActiveSheet

### Required Keys
- **sheet**

### Usage
```javascript
SetActiveSheet(sheet=[<sheet>])
```


---

## RemoveVariable

### Required Keys
- **variable**
- **dropNow**

### Usage
```javascript
RemoveVariable(variable=[<variable>], dropNow=[<dropNow>])
```


---

## StopPixelExecution

**Description:** 

        Stop the current execution of a pixel job

### Required Keys
- **id**

### Usage
```javascript
StopPixelExecution(id=[<id>])
```


---

## GetSpecificInsightMeta

### Required Keys
- **project**
- **id**

### Usage
```javascript
GetSpecificInsightMeta(project=[<project>], id=[<id>])
```


---

## RDFFileSource

### Required Keys
- **filePath**
- **rdfType**
- **baseUri**
- **query**

### Usage
```javascript
RDFFileSource(filePath=[<filePath>], rdfType=[<rdfType>], baseUri=[<baseUri>], query=[<query>])
```


---

## AsTask

### Required Keys
- **value**

### Usage
```javascript
AsTask(value=[<value>])
```


---

## DeleteUserAsset

### Required Keys
- **relativePath**

### Usage
```javascript
DeleteUserAsset(relativePath=[<relativePath>])
```


---

## GetPlaygroundMessages

### Required Keys
- **roomId**

### Optional Keys
- **offset**
- **limit**
- **sort**

### Usage
```javascript
GetPlaygroundMessages(roomId=[<roomId>], offset=[<offset>], limit=[<limit>], sort=[<sort>])
```


---

## SyncAppFilesO

### Required Keys
- **database**
- **repository**
- **dual**
- **syncDatabase**
- **files**

### Usage
```javascript
SyncAppFilesO(database=[<database>], repository=[<repository>], dual=[<dual>], syncDatabase=[<syncDatabase>], files=[<files>])
```


---

## GetEngineUsage

### Required Keys
- **engine**

### Usage
```javascript
GetEngineUsage(engine=[<engine>])
```


---

## RunDocumentSummarization

### Required Keys
- **fileOrigin**
- **userInput**
- **numSentences**
- **numTopics**
- **numTopicTerms**

### Usage
```javascript
RunDocumentSummarization(fileOrigin=[<fileOrigin>], userInput=[<userInput>], numSentences=[<numSentences>], numTopics=[<numTopics>], numTopicTerms=[<numTopicTerms>])
```


---

## ClearInsight

### Required Keys
- **suppress**
- **output**

### Usage
```javascript
ClearInsight(suppress=[<suppress>], output=[<output>])
```


---

## GetEngineUsagePerProject

### Required Keys
- **engine**
- **limit**
- **offset**
- **startDate**
- **endDate**

### Usage
```javascript
GetEngineUsagePerProject(engine=[<engine>], limit=[<limit>], offset=[<offset>], startDate=[<startDate>], endDate=[<endDate>])
```


---

## NewAppAssetsDirectory

**Description:** 

        Create a new empty directory in the projects assets folder

### Required Keys
- **project**
- **filePath**

### Optional Keys
- **comment**

### Usage
```javascript
NewAppAssetsDirectory(project=[<project>], filePath=[<filePath>], comment=[<comment>])
```


---

## Between

### Required Keys
- **column**
- **start**
- **end**

### Usage
```javascript
Between(column=[<column>], start=[<start>], end=[<end>])
```


---

## AddOwlLogicalNames

### Required Keys
- **database**
- **concept**
- **column**
- **logicalNames**

### Usage
```javascript
AddOwlLogicalNames(database=[<database>], concept=[<concept>], column=[<column>], logicalNames=[<logicalNames>])
```


---

## GetFrameTableStructure

### Required Keys
- **frame**

### Usage
```javascript
GetFrameTableStructure(frame=[<frame>])
```


---

## GetProjectPropertiesContent

### Required Keys
- **project**

### Usage
```javascript
GetProjectPropertiesContent(project=[<project>])
```


---

## AskCOTRoom

**Description:** 

        Takes a user's query and returns a chain-of-thought JSON plan by combining tool descriptions,
RAG context (if vector db is provided), and the original query according to a strict JSON schema.
If the output matches the schema, the type is 'COT'. Otherwise, returns as simple chat (type 'CHAT').


### Required Keys
- **engine**
- **command**

### Optional Keys
- **paramValues**
- **image**
- **vectorDb**
- **context**
- **roomId**
- **url**

### Usage
```javascript
AskCOTRoom(engine=[<engine>], command=[<command>], paramValues=[<paramValues>], image=[<image>], vectorDb=[<vectorDb>], context=[<context>], roomId=[<roomId>], url=[<url>])
```


---

## NLPQuery

### Required Keys
- **command**
- **model**

### Usage
```javascript
NLPQuery(command=[<command>], model=[<model>])
```


---

## CompileApps

**Description:** 

        Compile the custom reactors associcated with an app

### Required Keys
- **project**
- **release**

### Usage
```javascript
CompileApps(project=[<project>], release=[<release>])
```


---

## GetRoomMessages2

### Required Keys
- **roomId**

### Optional Keys
- **offset**
- **limit**
- **sort**

### Usage
```javascript
GetRoomMessages2(roomId=[<roomId>], offset=[<offset>], limit=[<limit>], sort=[<sort>])
```


---

## RdbmsReplaceDatabaseExcelUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**

### Usage
```javascript
RdbmsReplaceDatabaseExcelUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>])
```


---

## ListUserApps

### Required Keys
- **username**
- **password**

### Usage
```javascript
ListUserApps(username=[<username>], password=[<password>])
```


---

## GetMetaDescription

### Required Keys
- **database**
- **concept**

### Usage
```javascript
GetMetaDescription(database=[<database>], concept=[<concept>])
```


---

## GenerateXRayMatching

### Required Keys
- **filePath**
- **space**
- **database**
- **override**
- **config**
- **similarity**
- **candidate**
- **matchSameDb**
- **rowComparison**

### Usage
```javascript
GenerateXRayMatching(filePath=[<filePath>], space=[<space>], database=[<database>], override=[<override>], config=[<config>], similarity=[<similarity>], candidate=[<candidate>], matchSameDb=[<matchSameDb>], rowComparison=[<rowComparison>])
```


---

## DownloadAppAsset

**Description:** 

        Download a file or directory from within the projects assets folder

### Required Keys
- **project**
- **filePath**

### Usage
```javascript
DownloadAppAsset(project=[<project>], filePath=[<filePath>])
```


---

## CreateAppFromBlocks

### Required Keys
- **project**
- **global**
- **portalName**
- **provider**
- **url**
- **json**

### Usage
```javascript
CreateAppFromBlocks(project=[<project>], global=[<global>], portalName=[<portalName>], provider=[<provider>], url=[<url>], json=[<json>])
```


---

## AdminDatabase

### Required Keys
- **database**

### Usage
```javascript
AdminDatabase(database=[<database>])
```


---

## FrameFilterModel

### Required Keys
- **column**
- **filterWord**
- **limit**
- **offset**
- **panel**

### Usage
```javascript
FrameFilterModel(column=[<column>], filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], panel=[<panel>])
```


---

## GetTemplateList

### Required Keys
- **project**

### Usage
```javascript
GetTemplateList(project=[<project>])
```


---

## UpdateTemplate

### Required Keys
- **project**
- **template_name**
- **template_file**

### Usage
```javascript
UpdateTemplate(project=[<project>], template_name=[<template_name>], template_file=[<template_file>])
```


---

## RunMatrixRegression

### Required Keys
- **yColumn**
- **xColumns**
- **defaultValue**
- **panel**

### Usage
```javascript
RunMatrixRegression(yColumn=[<yColumn>], xColumns=[<xColumns>], defaultValue=[<defaultValue>], panel=[<panel>])
```


---

## GetParamValues

### Required Keys
- **sql**
- **params**

### Usage
```javascript
GetParamValues(sql=[<sql>], params=[<params>])
```


---

## RdfReplaceDatabaseLoaderSheetUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **customBaseURI**

### Usage
```javascript
RdfReplaceDatabaseLoaderSheetUpload(database=[<database>], filePath=[<filePath>], space=[<space>], customBaseURI=[<customBaseURI>])
```


---

## DashboardInsightConfig

### Required Keys
- **insights**
- **oldIds**
- **layout**

### Usage
```javascript
DashboardInsightConfig(insights=[<insights>], oldIds=[<oldIds>], layout=[<layout>])
```


---

## GetAppBlocksJson

### Required Keys
- **project**

### Usage
```javascript
GetAppBlocksJson(project=[<project>])
```


---

## AdminLockAccountWarning

### Required Keys
- **no keys defined**

### Usage
```javascript
AdminLockAccountWarning()
```


---

## GetDQRules

### Required Keys
- **no keys defined**

### Usage
```javascript
GetDQRules()
```


---

## AdminRemoveDuplicates

### Required Keys
- **database**
- **table**
- **drop**

### Usage
```javascript
AdminRemoveDuplicates(database=[<database>], table=[<table>], drop=[<drop>])
```


---

## AutoTaskOptions

### Required Keys
- **panel**
- **layout**
- **ignoreFilters**

### Usage
```javascript
AutoTaskOptions(panel=[<panel>], layout=[<layout>], ignoreFilters=[<ignoreFilters>])
```


---

## API

### Usage
```javascript
API()
```


---

## DeleteFrameFilter

### Required Keys
- **index**
- **taskRefresh**

### Usage
```javascript
DeleteFrameFilter(index=[<index>], taskRefresh=[<taskRefresh>])
```


---

## AddPanelSortBy

### Required Keys
- **panel**
- **column**
- **values**

### Usage
```javascript
AddPanelSortBy(panel=[<panel>], column=[<column>], values=[<values>])
```


---

## Month

### Required Keys
- **months**

### Usage
```javascript
Month(months=[<months>])
```


---

## StoreInsightRecipeStepMetadata

### Required Keys
- **pixelId**
- **alias**
- **description**

### Usage
```javascript
StoreInsightRecipeStepMetadata(pixelId=[<pixelId>], alias=[<alias>], description=[<description>])
```


---

## PBEDecrypt

### Required Keys
- **query**

### Usage
```javascript
PBEDecrypt(query=[<query>])
```


---

## MyOpenInsights

### Required Keys
- **no keys defined**

### Usage
```javascript
MyOpenInsights()
```


---

## SetProjectMetadata

**Description:** 

        Define metadata on a project

### Required Keys
- **project**
- **meta**
- **encoded**
- **jsonCleanup**

### Usage
```javascript
SetProjectMetadata(project=[<project>], meta=[<meta>], encoded=[<encoded>], jsonCleanup=[<jsonCleanup>])
```


---

## PredictExcelRangeMetadata

### Required Keys
- **filePath**
- **space**
- **sheetName**
- **sheetRange**
- **password**

### Usage
```javascript
PredictExcelRangeMetadata(filePath=[<filePath>], space=[<space>], sheetName=[<sheetName>], sheetRange=[<sheetRange>], password=[<password>])
```


---

## IsGit

### Required Keys
- **project**

### Usage
```javascript
IsGit(project=[<project>])
```


---

## Distinct

### Required Keys
- **no keys defined**

### Usage
```javascript
Distinct()
```


---

## CreateGoogleFuntionEngine

### Required Keys
- **function**
- **functionDetails**
- **filePaths**

### Optional Keys
- **fileName**

### Usage
```javascript
CreateGoogleFuntionEngine(function=[<function>], functionDetails=[<functionDetails>], filePaths=[<filePaths>], fileName=[<fileName>])
```


---

## RetrievePanelColorByValue

### Required Keys
- **panel**
- **name**

### Usage
```javascript
RetrievePanelColorByValue(panel=[<panel>], name=[<name>])
```


---

## GetPrompt

### Required Keys
- **promptId**

### Usage
```javascript
GetPrompt(promptId=[<promptId>])
```


---

## AddOwlProperty

### Required Keys
- **database**
- **concept**
- **column**
- **dataType**

### Optional Keys
- **logicalNames**
- **additionalDataType**
- **description**
- **conceptual**

### Usage
```javascript
AddOwlProperty(database=[<database>], concept=[<concept>], column=[<column>], dataType=[<dataType>], logicalNames=[<logicalNames>], additionalDataType=[<additionalDataType>], description=[<description>], conceptual=[<conceptual>])
```


---

## GetInsightDatasources

### Required Keys
- **no keys defined**

### Usage
```javascript
GetInsightDatasources()
```


---

## TextInput

### Required Keys
- **no keys defined**

### Usage
```javascript
TextInput()
```


---

## GetEngineFiles

### Required Keys
- **engine**
- **filePath**

### Usage
```javascript
GetEngineFiles(engine=[<engine>], filePath=[<filePath>])
```


---

## AskPlayground

**Description:** 

        This method is used to run an LLM text-generation call (Playground)â€”returns both input and response message objects.

### Required Keys
- **engine**
- **command**

### Optional Keys
- **paramValues**
- **image**
- **mcpToolID**
- **parentMessageId**
- **context**
- **roomId**
- **url**

### Usage
```javascript
AskPlayground(engine=[<engine>], command=[<command>], paramValues=[<paramValues>], image=[<image>], mcpToolID=[<mcpToolID>], parentMessageId=[<parentMessageId>], context=[<context>], roomId=[<roomId>], url=[<url>])
```


---

## GetUserDatabaseVotes

### Required Keys
- **database**

### Usage
```javascript
GetUserDatabaseVotes(database=[<database>])
```


---

## DownloadOwlRelationships

### Required Keys
- **database**

### Usage
```javascript
DownloadOwlRelationships(database=[<database>])
```


---

## LoadInsight

### Required Keys
- **project**
- **id**
- **params**
- **additionalPixels**
- **cache**

### Usage
```javascript
LoadInsight(project=[<project>], id=[<id>], params=[<params>], additionalPixels=[<additionalPixels>], cache=[<cache>])
```


---

## GetEngineMetaValues

### Required Keys
- **engineTypes**
- **metaKeys**

### Usage
```javascript
GetEngineMetaValues(engineTypes=[<engineTypes>], metaKeys=[<metaKeys>])
```


---

## JdbcSource

### Required Keys
- **conDetails**

### Usage
```javascript
JdbcSource(conDetails=[<conDetails>])
```


---

## NLPInstanceCache

### Required Keys
- **database**
- **table**
- **columns**
- **updateExistingValues**

### Usage
```javascript
NLPInstanceCache(database=[<database>], table=[<table>], columns=[<columns>], updateExistingValues=[<updateExistingValues>])
```


---

## ReplaceParam

### Required Keys
- **sql**
- **params**
- **value**

### Usage
```javascript
ReplaceParam(sql=[<sql>], params=[<params>], value=[<value>])
```


---

## RemoveOwlLogicalNames

### Required Keys
- **database**
- **concept**
- **column**
- **logicalNames**

### Usage
```javascript
RemoveOwlLogicalNames(database=[<database>], concept=[<concept>], column=[<column>], logicalNames=[<logicalNames>])
```


---

## DeleteProject

### Required Keys
- **project**

### Usage
```javascript
DeleteProject(project=[<project>])
```


---

## RemoveLlmFeedback

### Required Keys
- **messageId**

### Usage
```javascript
RemoveLlmFeedback(messageId=[<messageId>])
```


---

## LastUsedFrame

### Required Keys
- **no keys defined**

### Usage
```javascript
LastUsedFrame()
```


---

## GetOwlMetamodel

### Required Keys
- **database**

### Usage
```javascript
GetOwlMetamodel(database=[<database>])
```


---

## PSelect

### Required Keys
- **column**

### Usage
```javascript
PSelect(column=[<column>])
```


---

## SyncApp

### Required Keys
- **database**
- **repository**
- **username**
- **password**
- **dual**
- **syncDatabase**

### Usage
```javascript
SyncApp(database=[<database>], repository=[<repository>], username=[<username>], password=[<password>], dual=[<dual>], syncDatabase=[<syncDatabase>])
```


---

## AdminExportAllUsers

### Required Keys
- **task**
- **fileName**
- **filePath**
- **password**

### Usage
```javascript
AdminExportAllUsers(task=[<task>], fileName=[<fileName>], filePath=[<filePath>], password=[<password>])
```


---

## ImageEmbeddings

### Required Keys
- **engine**
- **values**

### Optional Keys
- **paramValues**

### Usage
```javascript
ImageEmbeddings(engine=[<engine>], values=[<values>], paramValues=[<paramValues>])
```


---

## MyDiscoverableDatabases

### Required Keys
- **filterWord**
- **limit**
- **offset**
- **database**
- **metaKeys**
- **metaFilters**

### Usage
```javascript
MyDiscoverableDatabases(filterWord=[<filterWord>], limit=[<limit>], offset=[<offset>], database=[<database>], metaKeys=[<metaKeys>], metaFilters=[<metaFilters>])
```


---

## RunSentimentAnalysis

### Required Keys
- **sentimentCol**
- **groupCol**
- **addEmotionCols**
- **panel**

### Usage
```javascript
RunSentimentAnalysis(sentimentCol=[<sentimentCol>], groupCol=[<groupCol>], addEmotionCols=[<addEmotionCols>], panel=[<panel>])
```


---

## GetAppAssetsBase64

**Description:** 

        Retrieve the contents of an image file as base64 encoded string in the projects assets folder

### Required Keys
- **project**
- **filePath**

### Usage
```javascript
GetAppAssetsBase64(project=[<project>], filePath=[<filePath>])
```


---

## PullProjectFolderFromCloud

### Required Keys
- **project**

### Usage
```javascript
PullProjectFolderFromCloud(project=[<project>])
```


---

## SetUserMetadata

**Description:** 

        Define metadata on a user

### Required Keys
- **userId**
- **provider**
- **meta**

### Optional Keys
- **jsonCleanup**
- **encoded**

### Usage
```javascript
SetUserMetadata(userId=[<userId>], provider=[<provider>], meta=[<meta>], jsonCleanup=[<jsonCleanup>], encoded=[<encoded>])
```


---

## Subquery

### Required Keys
- **frame**
- **qs**

### Usage
```javascript
Subquery(frame=[<frame>], qs=[<qs>])
```


---

## AdminUploadUsers

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
AdminUploadUsers(filePath=[<filePath>], space=[<space>])
```


---

## ProjectReconnectServer

### Required Keys
- **project**

### Usage
```javascript
ProjectReconnectServer(project=[<project>])
```


---

## ValidateUserProjectDependencies

**Description:** 

        Return a map `{'engineid':true/false}` for the users access to each engine dependency listed in this project

### Required Keys
- **project**

### Usage
```javascript
ValidateUserProjectDependencies(project=[<project>])
```


---

## ExportToExcelNN

### Optional Keys
- **fileName**
- **filePath**
- **usePanel**
- **export_template**
- **exportAudit**

### Usage
```javascript
ExportToExcelNN(fileName=[<fileName>], filePath=[<filePath>], usePanel=[<usePanel>], export_template=[<export_template>], exportAudit=[<exportAudit>])
```


---

## GetSheetState

### Required Keys
- **sheet**
- **output**

### Usage
```javascript
GetSheetState(sheet=[<sheet>], output=[<output>])
```


---

## GetPanelState

### Required Keys
- **panel**
- **output**

### Usage
```javascript
GetPanelState(panel=[<panel>], output=[<output>])
```


---

## ValidateProjectDependencies

**Description:** 

        Return true if the user has access to all engine dependencies listed in this project

### Required Keys
- **project**

### Usage
```javascript
ValidateProjectDependencies(project=[<project>])
```


---

## RunClassification

### Required Keys
- **classify**
- **attributes**
- **panel**

### Usage
```javascript
RunClassification(classify=[<classify>], attributes=[<attributes>], panel=[<panel>])
```


---

## GetMCPPrompts

### Required Keys
- **project**

### Usage
```javascript
GetMCPPrompts(project=[<project>])
```


---

## EditInsightRecipeStep

### Required Keys
- **pixelId**
- **pixel**

### Usage
```javascript
EditInsightRecipeStep(pixelId=[<pixelId>], pixel=[<pixel>])
```


---

## Context

### Required Keys
- **no keys defined**

### Usage
```javascript
Context()
```


---

## Java

### Required Keys
- **no keys defined**

### Usage
```javascript
Java()
```


---

## Convert

### Required Keys
- **frameType**
- **frame**
- **alias**

### Usage
```javascript
Convert(frameType=[<frameType>], frame=[<frame>], alias=[<alias>])
```


---

## SetPanelCollect

### Required Keys
- **panel**
- **limit**

### Usage
```javascript
SetPanelCollect(panel=[<panel>], limit=[<limit>])
```


---

## RdbmsExternalUpload

### Required Keys
- **conDetails**
- **database**
- **metamodel**
- **existing**

### Usage
```javascript
RdbmsExternalUpload(conDetails=[<conDetails>], database=[<database>], metamodel=[<metamodel>], existing=[<existing>])
```


---

## Echo

**Description:** 

        This reactor returns back the input the user enters

### Required Keys
- **value**

### Usage
```javascript
Echo(value=[<value>])
```


---

## CreateNewRdbmsExternalDatabase

### Required Keys
- **conDetails**
- **database**
- **metamodelAdd**

### Usage
```javascript
CreateNewRdbmsExternalDatabase(conDetails=[<conDetails>], database=[<database>], metamodelAdd=[<metamodelAdd>])
```


---

## CheckRPackages

### Required Keys
- **reload**

### Usage
```javascript
CheckRPackages(reload=[<reload>])
```


---

## MapMap

### Required Keys
- **no keys defined**

### Usage
```javascript
MapMap()
```


---

## GetCurrentContextProjectId

### Required Keys
- **no keys defined**

### Usage
```javascript
GetCurrentContextProjectId()
```


---

## GetLocalSchema

### Required Keys
- **database**

### Usage
```javascript
GetLocalSchema(database=[<database>])
```


---

## GetFrames

### Required Keys
- **no keys defined**

### Usage
```javascript
GetFrames()
```


---

## DownloadAppNotebook

### Required Keys
- **project**

### Usage
```javascript
DownloadAppNotebook(project=[<project>])
```


---

## UpdateInsightImage

### Required Keys
- **project**
- **id**
- **url**
- **imageWaitTime**

### Usage
```javascript
UpdateInsightImage(project=[<project>], id=[<id>], url=[<url>], imageWaitTime=[<imageWaitTime>])
```


---

## AdminGetSystemInfo

**Description:** 

        	Admin only reactor returning a map with properties about the instance including:
	hostname, ipaddress, isCluster, storageProvider, isClusterScheduler, isClusterZK


### Required Keys
- **no keys defined**

### Usage
```javascript
AdminGetSystemInfo()
```


---

## AddVar

### Required Keys
- **variable**
- **frame**
- **expression**

### Optional Keys
- **format**
- **language**

### Usage
```javascript
AddVar(variable=[<variable>], frame=[<frame>], expression=[<expression>], format=[<format>], language=[<language>])
```


---

## UpdateNLPHistory

### Usage
```javascript
UpdateNLPHistory()
```


---

## Lower

### Required Keys
- **columns**

### Usage
```javascript
Lower(columns=[<columns>])
```


---

## SetUserMetakeyOptions

**Description:** 

        Define metadata options for users

### Required Keys
- **metaoptions**

### Usage
```javascript
SetUserMetakeyOptions(metaoptions=[<metaoptions>])
```


---

## DatabaseInfo

### Required Keys
- **database**
- **metaKeys**

### Usage
```javascript
DatabaseInfo(database=[<database>], metaKeys=[<metaKeys>])
```


---

## SearchRoomMessages

**Description:** 

        This reactor searches through the messages in the user's conversation rooms for a given keyword within a specified project. It returns the matching messages, including relevant details such as room ID, message text, and message ID, for rooms the user has access to.

### Required Keys
- **search**

### Optional Keys
- **project**

### Usage
```javascript
SearchRoomMessages(search=[<search>], project=[<project>])
```


---

## GetRemoteModelDeployConfigs

### Optional Keys
- **refresh**

### Usage
```javascript
GetRemoteModelDeployConfigs(refresh=[<refresh>])
```


---

## FilterLambda

### Required Keys
- **CODE**
- **imports**

### Usage
```javascript
FilterLambda(CODE=[<CODE>], imports=[<imports>])
```


---

## SetRoomWorkspace

### Required Keys
- **roomId**

### Optional Keys
- **workspaceId**

### Usage
```javascript
SetRoomWorkspace(roomId=[<roomId>], workspaceId=[<workspaceId>])
```


---

## SnowflakePut

**Description:** 

        Utility method to PUT a file into the appropirate stage. Snowflake docs found here: https://docs.snowflake.com/en/sql-reference/sql/put

### Required Keys
- **database**
- **filePath**

### Optional Keys
- **userStage**
- **sourceCompression**
- **parallel**
- **tableStage**
- **autoCompress**
- **namedStage**
- **overwrite**
- **space**

### Usage
```javascript
SnowflakePut(database=[<database>], filePath=[<filePath>], userStage=[<userStage>], sourceCompression=[<sourceCompression>], parallel=[<parallel>], tableStage=[<tableStage>], autoCompress=[<autoCompress>], namedStage=[<namedStage>], overwrite=[<overwrite>], space=[<space>])
```


---

## UserActivitySQLite

### Required Keys
- **no keys defined**

### Usage
```javascript
UserActivitySQLite()
```


---

## GetPhysicalToPhysicalMapping

### Required Keys
- **no keys defined**

### Usage
```javascript
GetPhysicalToPhysicalMapping()
```


---

## FuzzyMatches

### Required Keys
- **task**
- **frame**
- **frameCol**
- **outputFrame**

### Usage
```javascript
FuzzyMatches(task=[<task>], frame=[<frame>], frameCol=[<frameCol>], outputFrame=[<outputFrame>])
```


---

## BuildModelToolsArray

**Description:** 

        Return an array with the tools maps for each engine id being requested

### Required Keys
- **engine**

### Usage
```javascript
BuildModelToolsArray(engine=[<engine>])
```


---

## GetProjectEmailSession

### Required Keys
- **project**

### Usage
```javascript
GetProjectEmailSession(project=[<project>])
```


---

## GetCurrentRecipe

### Required Keys
- **no keys defined**

### Usage
```javascript
GetCurrentRecipe()
```


---

## GetXLSchema

### Required Keys
- **filePath**
- **sheetName**

### Usage
```javascript
GetXLSchema(filePath=[<filePath>], sheetName=[<sheetName>])
```


---

## GetUserMetadata

**Description:** 

        Retrieve metadata on a user

### Optional Keys
- **provider**
- **metaKeys**
- **userId**

### Usage
```javascript
GetUserMetadata(provider=[<provider>], metaKeys=[<metaKeys>], userId=[<userId>])
```


---

## RequestProject

### Required Keys
- **project**
- **permission**
- **comment**

### Usage
```javascript
RequestProject(project=[<project>], permission=[<permission>], comment=[<comment>])
```


---

## SetPanelSortBy

### Required Keys
- **panel**
- **columns**
- **sort**

### Usage
```javascript
SetPanelSortBy(panel=[<panel>], columns=[<columns>], sort=[<sort>])
```


---

## GetDatabaseSMSS

### Required Keys
- **database**

### Usage
```javascript
GetDatabaseSMSS(database=[<database>])
```


---

## TinkerReplaceDatabaseCsvUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **additionalDataTypes**
- **tinkerDriver**

### Usage
```javascript
TinkerReplaceDatabaseCsvUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], additionalDataTypes=[<additionalDataTypes>], tinkerDriver=[<tinkerDriver>])
```


---

## CheckRecommendOptimization

### Required Keys
- **database**

### Usage
```javascript
CheckRecommendOptimization(database=[<database>])
```


---

## GitlabPullLastSuccessfulJobArtifact

**Description:** 

        This reactor pulls the latest successful artifact for a GitLab project. Specifically, its pulls the last successful pipeline execution for a specific branch and for a specific job name as the pipeline might produce multiple artifacts based on the stages

### Required Keys
- **host**
- **gitlabProjectId**
- **gitlabBranchName**
- **gitlabJobName**
- **gitlabPrivateToken**
- **useApplicationCert**

### Usage
```javascript
GitlabPullLastSuccessfulJobArtifact(host=[<host>], gitlabProjectId=[<gitlabProjectId>], gitlabBranchName=[<gitlabBranchName>], gitlabJobName=[<gitlabJobName>], gitlabPrivateToken=[<gitlabPrivateToken>], useApplicationCert=[<useApplicationCert>])
```


---

## ReloadInsight

### Required Keys
- **project**
- **id**
- **params**
- **additionalPixels**
- **paramValues**
- **cache**
- **useExistingIfOpen**
- **origin**

### Usage
```javascript
ReloadInsight(project=[<project>], id=[<id>], params=[<params>], additionalPixels=[<additionalPixels>], paramValues=[<paramValues>], cache=[<cache>], useExistingIfOpen=[<useExistingIfOpen>], origin=[<origin>])
```


---

## AddWorkspace

### Required Keys
- **name**

### Optional Keys
- **systemPrompt**
- **vectorDb**
- **function**
- **description**
- **sharingEnabled**

### Usage
```javascript
AddWorkspace(name=[<name>], systemPrompt=[<systemPrompt>], vectorDb=[<vectorDb>], function=[<function>], description=[<description>], sharingEnabled=[<sharingEnabled>])
```


---

## ToNumericType

### Required Keys
- **columns**

### Usage
```javascript
ToNumericType(columns=[<columns>])
```


---

## OpenDatabase

### Required Keys
- **database**

### Usage
```javascript
OpenDatabase(database=[<database>])
```


---

## UniqueGroupConcat

### Required Keys
- **columns**

### Usage
```javascript
UniqueGroupConcat(columns=[<columns>])
```


---

## TrendingDatabases

### Required Keys
- **display**

### Usage
```javascript
TrendingDatabases(display=[<display>])
```


---

## GetDatabaseMetamodel

### Required Keys
- **database**
- **options**

### Usage
```javascript
GetDatabaseMetamodel(database=[<database>], options=[<options>])
```


---

## PositionInsightRecipeStep

### Required Keys
- **pixelId**
- **positionMap**

### Usage
```javascript
PositionInsightRecipeStep(pixelId=[<pixelId>], positionMap=[<positionMap>])
```


---

## FindDirectOwlRelationships

### Required Keys
- **database**
- **tables**

### Usage
```javascript
FindDirectOwlRelationships(database=[<database>], tables=[<tables>])
```


---

## GoogleGmailDeleteEmail

**Description:** 

        Delete an email

### Required Keys
- **id**

### Usage
```javascript
GoogleGmailDeleteEmail(id=[<id>])
```


---

## RunMultiClustering

### Required Keys
- **instance**
- **minNumClusters**
- **maxNumClusters**
- **attributes**

### Usage
```javascript
RunMultiClustering(instance=[<instance>], minNumClusters=[<minNumClusters>], maxNumClusters=[<maxNumClusters>], attributes=[<attributes>])
```


---

## LLM

**Description:** 

        This method is used to run an LLM text-generation call

### Required Keys
- **engine**
- **command**

### Optional Keys
- **useHistory**
- **paramValues**
- **context**

### Usage
```javascript
LLM(engine=[<engine>], command=[<command>], useHistory=[<useHistory>], paramValues=[<paramValues>], context=[<context>])
```


---

## QueryFilterComponentOr

### Required Keys
- **filters**

### Usage
```javascript
QueryFilterComponentOr(filters=[<filters>])
```


---

## VizRecommendations

### Required Keys
- **task**
- **max**

### Usage
```javascript
VizRecommendations(task=[<task>], max=[<max>])
```


---

## GenerateIdColumn

### Required Keys
- **database**
- **table**
- **columns**
- **dataType**
- **newCol**

### Usage
```javascript
GenerateIdColumn(database=[<database>], table=[<table>], columns=[<columns>], dataType=[<dataType>], newCol=[<newCol>])
```


---

## OpMatch

### Required Keys
- **value**
- **array**
- **matchType**

### Usage
```javascript
OpMatch(value=[<value>], array=[<array>], matchType=[<matchType>])
```


---

## NegEvaluator

### Required Keys
- **no keys defined**

### Usage
```javascript
NegEvaluator()
```


---

## MovePanel

### Required Keys
- **panel**
- **sheet**

### Usage
```javascript
MovePanel(panel=[<panel>], sheet=[<sheet>])
```


---

## EditWorkspace

### Required Keys
- **workspaceId**
- **name**

### Optional Keys
- **systemPrompt**
- **vectorDb**
- **function**
- **description**
- **isActive**
- **sharingEnabled**

### Usage
```javascript
EditWorkspace(workspaceId=[<workspaceId>], name=[<name>], systemPrompt=[<systemPrompt>], vectorDb=[<vectorDb>], function=[<function>], description=[<description>], isActive=[<isActive>], sharingEnabled=[<sharingEnabled>])
```


---

## CodeLambda

### Required Keys
- **CODE**
- **imports**

### Usage
```javascript
CodeLambda(CODE=[<CODE>], imports=[<imports>])
```


---

## Clone

### Required Keys
- **panel**
- **cloneId**
- **sheet**

### Usage
```javascript
Clone(panel=[<panel>], cloneId=[<cloneId>], sheet=[<sheet>])
```


---

## USAJobsByStateDownload

### Required Keys
- **cred**
- **state**
- **fileName**

### Usage
```javascript
USAJobsByStateDownload(cred=[<cred>], state=[<state>], fileName=[<fileName>])
```


---

## CachedSheet

### Required Keys
- **sheet**

### Usage
```javascript
CachedSheet(sheet=[<sheet>])
```


---

## RemovePanelEvents

### Required Keys
- **panel**
- **traversal**

### Usage
```javascript
RemovePanelEvents(panel=[<panel>], traversal=[<traversal>])
```


---

## ExecQuery

### Required Keys
- **qs**
- **commit**
- **customSuccessMessage**

### Usage
```javascript
ExecQuery(qs=[<qs>], commit=[<commit>], customSuccessMessage=[<customSuccessMessage>])
```


---

## ExecuteAppNotebook

### Required Keys
- **project**
- **map**

### Usage
```javascript
ExecuteAppNotebook(project=[<project>], map=[<map>])
```


---

## CollectSeaborn

### Required Keys
- **splot**
- **format**

### Usage
```javascript
CollectSeaborn(splot=[<splot>], format=[<format>])
```


---

## UpdateInsightParameter

### Required Keys
- **paramStruct**

### Usage
```javascript
UpdateInsightParameter(paramStruct=[<paramStruct>])
```


---

## DatabaseUsers

### Required Keys
- **database**

### Usage
```javascript
DatabaseUsers(database=[<database>])
```


---

## GetPipeline

### Required Keys
- **no keys defined**

### Usage
```javascript
GetPipeline()
```


---

## MapList

### Required Keys
- **no keys defined**

### Usage
```javascript
MapList()
```


---

## OpRound

### Required Keys
- **numValues**

### Usage
```javascript
OpRound(numValues=[<numValues>])
```


---

## PredictExcelDataTypes

### Required Keys
- **filePath**
- **space**
- **password**

### Usage
```javascript
PredictExcelDataTypes(filePath=[<filePath>], space=[<space>], password=[<password>])
```


---

## ExternalJdbcTablesAndViews

### Required Keys
- **conDetails**

### Usage
```javascript
ExternalJdbcTablesAndViews(conDetails=[<conDetails>])
```


---

## GetJanusGraphMetaModel

### Required Keys
- **filePath**
- **graphTypeId**

### Usage
```javascript
GetJanusGraphMetaModel(filePath=[<filePath>], graphTypeId=[<graphTypeId>])
```


---

## GetCSVSchema

### Required Keys
- **filePath**
- **delimiter**

### Usage
```javascript
GetCSVSchema(filePath=[<filePath>], delimiter=[<delimiter>])
```


---

## AddOwlDescription

### Required Keys
- **database**
- **concept**
- **column**
- **description**

### Usage
```javascript
AddOwlDescription(database=[<database>], concept=[<concept>], column=[<column>], description=[<description>])
```


---

## SharePointDriveSelector

### Required Keys
- **siteId**

### Usage
```javascript
SharePointDriveSelector(siteId=[<siteId>])
```


---

## GetProjectMetaValues

### Required Keys
- **metaKeys**

### Usage
```javascript
GetProjectMetaValues(metaKeys=[<metaKeys>])
```


---

## SurveyMonkeyListSurveys

### Required Keys
- **no keys defined**

### Usage
```javascript
SurveyMonkeyListSurveys()
```


---

## GetOwlLogicalNames

### Required Keys
- **database**
- **concept**
- **column**

### Usage
```javascript
GetOwlLogicalNames(database=[<database>], concept=[<concept>], column=[<column>])
```


---

## AddOperationAlias

### Required Keys
- **reactor**
- **alias**

### Usage
```javascript
AddOperationAlias(reactor=[<reactor>], alias=[<alias>])
```


---

## ModifyInsightDatasource

### Required Keys
- **options**

### Usage
```javascript
ModifyInsightDatasource(options=[<options>])
```


---

## CollectGraph

### Required Keys
- **frame**

### Usage
```javascript
CollectGraph(frame=[<frame>])
```


---

## RdfReplaceDatabaseCsvUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **metamodel**
- **propFile**
- **startRow**
- **endRow**
- **customBaseURI**

### Usage
```javascript
RdfReplaceDatabaseCsvUpload(database=[<database>], filePath=[<filePath>], space=[<space>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], metamodel=[<metamodel>], propFile=[<propFile>], startRow=[<startRow>], endRow=[<endRow>], customBaseURI=[<customBaseURI>])
```


---

## RemoveOwlDescription

### Required Keys
- **database**
- **concept**
- **column**
- **description**

### Usage
```javascript
RemoveOwlDescription(database=[<database>], concept=[<concept>], column=[<column>], description=[<description>])
```


---

## GetGraphProperties

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
GetGraphProperties(filePath=[<filePath>], space=[<space>])
```


---

## LoadPyFromFileProjectPy

### Required Keys
- **filePath**
- **alias**
- **space**

### Usage
```javascript
LoadPyFromFileProjectPy(filePath=[<filePath>], alias=[<alias>], space=[<space>])
```


---

## UpdateGraphPlanner2

### Required Keys
- **no keys defined**

### Usage
```javascript
UpdateGraphPlanner2()
```


---

## EmbeddedRoutine

### Required Keys
- **no keys defined**

### Usage
```javascript
EmbeddedRoutine()
```


---

## HelpJson

### Required Keys
- **reset**

### Usage
```javascript
HelpJson(reset=[<reset>])
```


---

## UploadEngine

**Description:** 

        Import a new engine. The user who uploads will by default be the owner of the engine

### Required Keys
- **filePath**
- **space**
- **global**

### Usage
```javascript
UploadEngine(filePath=[<filePath>], space=[<space>], global=[<global>])
```


---

## PCol

### Required Keys
- **column**

### Usage
```javascript
PCol(column=[<column>])
```


---

## GetPanelId

### Required Keys
- **panel**

### Usage
```javascript
GetPanelId(panel=[<panel>])
```


---

## GetNumTable

### Required Keys
- **url**
- **aliasMap**

### Usage
```javascript
GetNumTable(url=[<url>], aliasMap=[<aliasMap>])
```


---

## AddRPYProjectPath

**Description:** 

        Add the project assets folder to the python sys.path and/or the R setwd

### Required Keys
- **project**

### Usage
```javascript
AddRPYProjectPath(project=[<project>])
```


---

## AggregateAll

### Required Keys
- **math**
- **ignoreCols**

### Usage
```javascript
AggregateAll(math=[<math>], ignoreCols=[<ignoreCols>])
```


---

## SetInsightConfig

### Required Keys
- **no keys defined**

### Usage
```javascript
SetInsightConfig()
```


---

## GreedyJson

### Required Keys
- **no keys defined**

### Usage
```javascript
GreedyJson()
```


---

## OpLarge

### Required Keys
- **numValues**

### Usage
```javascript
OpLarge(numValues=[<numValues>])
```


---

## SetSheetConfig

### Required Keys
- **sheet**
- **sheetConfig**

### Usage
```javascript
SetSheetConfig(sheet=[<sheet>], sheetConfig=[<sheetConfig>])
```


---

## Cast

### Required Keys
- **columns**

### Usage
```javascript
Cast(columns=[<columns>])
```


---

## RdbmsUploadTableData

### Required Keys
- **database**
- **filePath**
- **existing**
- **delimiter**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**
- **replace**

### Usage
```javascript
RdbmsUploadTableData(database=[<database>], filePath=[<filePath>], existing=[<existing>], delimiter=[<delimiter>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>], replace=[<replace>])
```


---

## ResetTask

### Required Keys
- **taskId**

### Usage
```javascript
ResetTask(taskId=[<taskId>])
```


---

## CloseEngine

### Required Keys
- **engine**

### Usage
```javascript
CloseEngine(engine=[<engine>])
```


---

## IsExcelPasswordProtected

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
IsExcelPasswordProtected(filePath=[<filePath>], space=[<space>])
```


---

## ListPackagesInVirtualEnv

### Required Keys
- **engine**

### Usage
```javascript
ListPackagesInVirtualEnv(engine=[<engine>])
```


---

## SendEmail

### Required Keys
- **smtpHost**
- **smtpPort**
- **subject**
- **from**
- **message**
- **messageEncoded**
- **filePath**
- **space**
- **emailSession**
- **html**
- **mustache**
- **mustacheVars**
- **username**
- **password**
- **to**
- **cc**
- **bcc**
- **attachments**

### Usage
```javascript
SendEmail(smtpHost=[<smtpHost>], smtpPort=[<smtpPort>], subject=[<subject>], from=[<from>], message=[<message>], messageEncoded=[<messageEncoded>], filePath=[<filePath>], space=[<space>], emailSession=[<emailSession>], html=[<html>], mustache=[<mustache>], mustacheVars=[<mustacheVars>], username=[<username>], password=[<password>], to=[<to>], cc=[<cc>], bcc=[<bcc>], attachments=[<attachments>])
```


---

## InsightUsageStatistics

### Required Keys
- **project**
- **filterWord**
- **tags**
- **panel**

### Usage
```javascript
InsightUsageStatistics(project=[<project>], filterWord=[<filterWord>], tags=[<tags>], panel=[<panel>])
```


---

## SQLSetParams

### Required Keys
- **id**
- **value**
- **id_type**

### Usage
```javascript
SQLSetParams(id=[<id>], value=[<value>], id_type=[<id_type>])
```


---

## REnableUserRecovery

### Required Keys
- **enable**

### Usage
```javascript
REnableUserRecovery(enable=[<enable>])
```


---

## BackupDatabase

### Required Keys
- **database**

### Usage
```javascript
BackupDatabase(database=[<database>])
```


---

## DeleteInsight

### Required Keys
- **project**
- **id**

### Usage
```javascript
DeleteInsight(project=[<project>], id=[<id>])
```


---

## PositionInsightRecipe

### Required Keys
- **positionMap**

### Usage
```javascript
PositionInsightRecipe(positionMap=[<positionMap>])
```


---

## URLSource

### Required Keys
- **url**
- **delimiter**

### Usage
```javascript
URLSource(url=[<url>], delimiter=[<delimiter>])
```


---

## ReplaceInaccessibleEngines

**Description:** 

        Replaces UUID engine ids with new UUID values

### Required Keys
- **project**
- **map**

### Optional Keys
- **filePath**

### Usage
```javascript
ReplaceInaccessibleEngines(project=[<project>], map=[<map>], filePath=[<filePath>])
```


---

## UploadInsight

### Required Keys
- **project**
- **filePath**
- **space**

### Usage
```javascript
UploadInsight(project=[<project>], filePath=[<filePath>], space=[<space>])
```


---

## RemoveRPYProjectPath

**Description:** 

        Remove the project assets folder from the python sys.path and/or the R setwd

### Required Keys
- **project**

### Usage
```javascript
RemoveRPYProjectPath(project=[<project>])
```


---

## GetInsightConfig

### Required Keys
- **no keys defined**

### Usage
```javascript
GetInsightConfig()
```


---

## PowAssimilator

### Required Keys
- **no keys defined**

### Usage
```javascript
PowAssimilator()
```


---

## DeleteBlock

### Required Keys
- **blockId**

### Optional Keys
- **hardDelete**

### Usage
```javascript
DeleteBlock(blockId=[<blockId>], hardDelete=[<hardDelete>])
```


---

## CreateFunctionEngine

**Description:** 

        Create a new function engine based on the functionDetails parameters


### Required Keys
- **function**
- **functionDetails**

### Optional Keys
- **filePath**

### Usage
```javascript
CreateFunctionEngine(function=[<function>], functionDetails=[<functionDetails>], filePath=[<filePath>])
```


---

## Model

### Required Keys
- **model**

### Usage
```javascript
Model(model=[<model>])
```


---

## UnvoteDatabase

### Required Keys
- **database**

### Usage
```javascript
UnvoteDatabase(database=[<database>])
```


---

## AdminGetProjectPortalDetails

### Required Keys
- **project**

### Usage
```javascript
AdminGetProjectPortalDetails(project=[<project>])
```


---

## SendErrorMessage

### Required Keys
- **message**

### Usage
```javascript
SendErrorMessage(message=[<message>])
```


---

## GitClone

### Required Keys
- **url**

### Optional Keys
- **filePath**
- **space**

### Usage
```javascript
GitClone(url=[<url>], filePath=[<filePath>], space=[<space>])
```


---

## RetrievePanelComment

### Required Keys
- **panel**
- **comment**

### Usage
```javascript
RetrievePanelComment(panel=[<panel>], comment=[<comment>])
```


---

## UploadProject

### Required Keys
- **filePath**
- **space**
- **global**

### Usage
```javascript
UploadProject(filePath=[<filePath>], space=[<space>], global=[<global>])
```


---

## ProviderValidator

### Required Keys
- **no keys defined**

### Usage
```javascript
ProviderValidator()
```


---

## GetConceptProperties

### Required Keys
- **concepts**
- **database**

### Usage
```javascript
GetConceptProperties(concepts=[<concepts>], database=[<database>])
```


---

## GenerateFrameFromRVariable

### Required Keys
- **variable**
- **override**

### Usage
```javascript
GenerateFrameFromRVariable(variable=[<variable>], override=[<override>])
```


---

## AskCOTTriage

### Required Keys
- **engine**
- **command**

### Optional Keys
- **paramValues**
- **image**
- **context**
- **roomId**
- **url**

### Usage
```javascript
AskCOTTriage(engine=[<engine>], command=[<command>], paramValues=[<paramValues>], image=[<image>], context=[<context>], roomId=[<roomId>], url=[<url>])
```


---

## ExternalUpdateJdbcSchema

### Required Keys
- **database**
- **filters**

### Usage
```javascript
ExternalUpdateJdbcSchema(database=[<database>], filters=[<filters>])
```


---

## GoogleUploader

### Required Keys
- **filename**

### Usage
```javascript
GoogleUploader(filename=[<filename>])
```


---

## CreateExternalNeo4jDatabase

### Required Keys
- **database**
- **connectionString**
- **username**
- **password**
- **graphTypeId**
- **graphNameId**
- **graphMetamodel**
- **useLabel**

### Usage
```javascript
CreateExternalNeo4jDatabase(database=[<database>], connectionString=[<connectionString>], username=[<username>], password=[<password>], graphTypeId=[<graphTypeId>], graphNameId=[<graphNameId>], graphMetamodel=[<graphMetamodel>], useLabel=[<useLabel>])
```


---

## AuditLog

### Required Keys
- **auditEndpoint**

### Optional Keys
- **paramValues**

### Usage
```javascript
AuditLog(auditEndpoint=[<auditEndpoint>], paramValues=[<paramValues>])
```


---

## ExecuteScheduledJob

### Required Keys
- **jobId**
- **jobGroup**

### Usage
```javascript
ExecuteScheduledJob(jobId=[<jobId>], jobGroup=[<jobGroup>])
```


---

## SetEngineMetadata

**Description:** 

        Define metadata on a datasource

### Required Keys
- **engine**
- **meta**
- **encoded**
- **jsonCleanup**

### Usage
```javascript
SetEngineMetadata(engine=[<engine>], meta=[<meta>], encoded=[<encoded>], jsonCleanup=[<jsonCleanup>])
```


---

## DownloadInsightRecipe

### Required Keys
- **project**
- **id**

### Usage
```javascript
DownloadInsightRecipe(project=[<project>], id=[<id>])
```


---

## SetProjectDependencies

**Description:** 

        Set the engine dependencies for a project

### Required Keys
- **project**
- **dependencies**

### Usage
```javascript
SetProjectDependencies(project=[<project>], dependencies=[<dependencies>])
```


---

## SearchAsset

### Required Keys
- **search**

### Optional Keys
- **filePath**
- **space**

### Usage
```javascript
SearchAsset(search=[<search>], filePath=[<filePath>], space=[<space>])
```


---

## GetRequest

### Required Keys
- **url**
- **headersMap**
- **useApplicationCert**
- **saveFile**

### Usage
```javascript
GetRequest(url=[<url>], headersMap=[<headersMap>], useApplicationCert=[<useApplicationCert>], saveFile=[<saveFile>])
```


---

## SaveAsset

**Description:** 

        Save a single or multiple files in the space's file repository

### Required Keys
- **fileName**
- **content**

### Optional Keys
- **comment**
- **space**

### Usage
```javascript
SaveAsset(fileName=[<fileName>], content=[<content>], comment=[<comment>], space=[<space>])
```


---

## SQLGetParams

### Required Keys
- **id**
- **id_type**

### Usage
```javascript
SQLGetParams(id=[<id>], id_type=[<id_type>])
```


---

## ListVar

### Required Keys
- **no keys defined**

### Usage
```javascript
ListVar()
```


---

## SetDatabaseMetakeyOptions

### Required Keys
- **metaoptions**

### Usage
```javascript
SetDatabaseMetakeyOptions(metaoptions=[<metaoptions>])
```


---

## EditOwlPropertyDataType

### Required Keys
- **database**
- **concept**
- **column**
- **dataType**
- **additionalDataType**

### Usage
```javascript
EditOwlPropertyDataType(database=[<database>], concept=[<concept>], column=[<column>], dataType=[<dataType>], additionalDataType=[<additionalDataType>])
```


---

## GetPanelCollect

### Required Keys
- **panel**

### Usage
```javascript
GetPanelCollect(panel=[<panel>])
```


---

## AdminGetRDFMap

**Description:** 

        Admin reactor to get the contents of the RDF_Map.prop file with secrets concealed

### Usage
```javascript
AdminGetRDFMap()
```


---

## GetProjectAvailables

### Required Keys
- **project**

### Usage
```javascript
GetProjectAvailables(project=[<project>])
```


---

## GetDatabasesByDescription

### Required Keys
- **description**
- **access**
- **lowLimit**
- **margin**

### Usage
```javascript
GetDatabasesByDescription(description=[<description>], access=[<access>], lowLimit=[<lowLimit>], margin=[<margin>])
```


---

## AddPanelFilter

### Required Keys
- **panel**
- **filters**
- **taskRefresh**

### Usage
```javascript
AddPanelFilter(panel=[<panel>], filters=[<filters>], taskRefresh=[<taskRefresh>])
```


---

## GetFrameFilterRange

### Required Keys
- **column**
- **panel**
- **dynamic**
- **optionsCache**

### Usage
```javascript
GetFrameFilterRange(column=[<column>], panel=[<panel>], dynamic=[<dynamic>], optionsCache=[<optionsCache>])
```


---

## Pragma

### Required Keys
- **pragma**

### Usage
```javascript
Pragma(pragma=[<pragma>])
```


---

## DeleteEngine

### Required Keys
- **engine**

### Usage
```javascript
DeleteEngine(engine=[<engine>])
```


---

## GetWorkspaceKnowledgeBase

### Required Keys
- **workspaceId**

### Optional Keys
- **paramValues**

### Usage
```javascript
GetWorkspaceKnowledgeBase(workspaceId=[<workspaceId>], paramValues=[<paramValues>])
```


---

## ToXml

### Required Keys
- **task**
- **fileName**
- **filePath**
- **appendTimestamp**

### Usage
```javascript
ToXml(task=[<task>], fileName=[<fileName>], filePath=[<filePath>], appendTimestamp=[<appendTimestamp>])
```


---

## AdminGetEngineUsagePerUser

### Required Keys
- **engine**
- **limit**
- **offset**
- **startDate**
- **endDate**

### Usage
```javascript
AdminGetEngineUsagePerUser(engine=[<engine>], limit=[<limit>], offset=[<offset>], startDate=[<startDate>], endDate=[<endDate>])
```


---

## SyncAppO

### Required Keys
- **database**
- **repository**
- **username**
- **password**
- **dual**
- **syncDatabase**

### Usage
```javascript
SyncAppO(database=[<database>], repository=[<repository>], username=[<username>], password=[<password>], dual=[<dual>], syncDatabase=[<syncDatabase>])
```


---

## ToLoaderSheet

### Required Keys
- **database**

### Usage
```javascript
ToLoaderSheet(database=[<database>])
```


---

## ResumeJobTrigger

### Required Keys
- **jobId**
- **jobGroup**

### Usage
```javascript
ResumeJobTrigger(jobId=[<jobId>], jobGroup=[<jobGroup>])
```


---

## RdbmsReplaceDatabaseLoaderSheetUpload

### Required Keys
- **database**
- **filePath**
- **space**
- **existing**
- **dataTypeMap**
- **newHeaders**
- **additionalDataTypes**
- **clean**
- **deduplicate**

### Usage
```javascript
RdbmsReplaceDatabaseLoaderSheetUpload(database=[<database>], filePath=[<filePath>], space=[<space>], existing=[<existing>], dataTypeMap=[<dataTypeMap>], newHeaders=[<newHeaders>], additionalDataTypes=[<additionalDataTypes>], clean=[<clean>], deduplicate=[<deduplicate>])
```


---

## CheckPromptTitle

### Required Keys
- **promptTitle**

### Usage
```javascript
CheckPromptTitle(promptTitle=[<promptTitle>])
```


---

## HasDuplicates

### Required Keys
- **frame**
- **columns**

### Usage
```javascript
HasDuplicates(frame=[<frame>], columns=[<columns>])
```


---

## CreateRestFunctionEngine

**Description:** 

        This reactor is deprecated and users should use the generic CreateFunctionEngine reactor.


### Required Keys
- **function**
- **functionDetails**

### Optional Keys
- **filePath**

### Usage
```javascript
CreateRestFunctionEngine(function=[<function>], functionDetails=[<functionDetails>], filePath=[<filePath>])
```


---

## GetDatabaseSelectors

### Required Keys
- **database**

### Usage
```javascript
GetDatabaseSelectors(database=[<database>])
```


---

## Mvn

### Optional Keys
- **mvn_goals**

### Usage
```javascript
Mvn(mvn_goals=[<mvn_goals>])
```


---

## Generic

### Required Keys
- **no keys defined**

### Usage
```javascript
Generic()
```


---

## QueryRowCount

### Required Keys
- **qs**

### Usage
```javascript
QueryRowCount(qs=[<qs>])
```


---

## SetRoomName

### Required Keys
- **roomId**
- **roomName**

### Usage
```javascript
SetRoomName(roomId=[<roomId>], roomName=[<roomName>])
```


---

## UnzipFile

**Description:** 

        Unzips the updated project and routes the extracted files

### Required Keys
- **filePath**

### Optional Keys
- **space**

### Usage
```javascript
UnzipFile(filePath=[<filePath>], space=[<space>])
```


---

## SendWarningMessage

### Required Keys
- **message**

### Usage
```javascript
SendWarningMessage(message=[<message>])
```


---

## ExecuteGuardrailEngine

### Required Keys
- **engine**

### Usage
```javascript
ExecuteGuardrailEngine(engine=[<engine>])
```


---

## DateManipulation

### Required Keys
- **type**
- **date**
- **recurrence**
- **timeunit**

### Usage
```javascript
DateManipulation(type=[<type>], date=[<date>], recurrence=[<recurrence>], timeunit=[<timeunit>])
```


---

## LoadEngineMetadata

**Description:** 

        Define metadata on an engine through a JSON file

### Required Keys
- **filePath**
- **space**

### Usage
```javascript
LoadEngineMetadata(filePath=[<filePath>], space=[<space>])
```


---

## ParallelPixelRun

### Required Keys
- **pixel**

### Usage
```javascript
ParallelPixelRun(pixel=[<pixel>])
```


---

## RemoveLayer

### Required Keys
- **panel**
- **layer**

### Usage
```javascript
RemoveLayer(panel=[<panel>], layer=[<layer>])
```


---

## Week

### Required Keys
- **weeks**

### Usage
```javascript
Week(weeks=[<weeks>])
```


---

## OpOr

### Required Keys
- **values**

### Usage
```javascript
OpOr(values=[<values>])
```


---

## NaturalLanguageSearch

### Required Keys
- **query**
- **database**
- **global**
- **panel**

### Usage
```javascript
NaturalLanguageSearch(query=[<query>], database=[<database>], global=[<global>], panel=[<panel>])
```


---

## CheckInsightNameExists

### Required Keys
- **project**
- **insightName**

### Usage
```javascript
CheckInsightNameExists(project=[<project>], insightName=[<insightName>])
```


---

## ModifyParamQuery

### Required Keys
- **filterWord**
- **query**

### Usage
```javascript
ModifyParamQuery(filterWord=[<filterWord>], query=[<query>])
```


---

## RefreshPanelView

### Required Keys
- **panel**

### Usage
```javascript
RefreshPanelView(panel=[<panel>])
```


---

## CollectMeta

### Required Keys
- **task**
- **values**

### Usage
```javascript
CollectMeta(task=[<task>], values=[<values>])
```


---

## GetPanelColorByValue

### Required Keys
- **panel**

### Usage
```javascript
GetPanelColorByValue(panel=[<panel>])
```


---

## GetPanelSort

### Required Keys
- **panel**

### Usage
```javascript
GetPanelSort(panel=[<panel>])
```


---

## ToPostgresCopy

**Description:** 

        This reactor is deprecated. Please use PostgresCopy() instead.

### Required Keys
- **database**
- **table**
- **format**
- **nullValue**
- **delimiter**
- **filePath**
- **space**

### Usage
```javascript
ToPostgresCopy(database=[<database>], table=[<table>], format=[<format>], nullValue=[<nullValue>], delimiter=[<delimiter>], filePath=[<filePath>], space=[<space>])
```


---

## Limit

### Required Keys
- **numValue**

### Usage
```javascript
Limit(numValue=[<numValue>])
```


---

## AddPlaygroundToolExecution

**Description:** 

        Add a tool execution input message to the message history.
If all the tools have been executed from the previous tool response message, this will return the LLM response.
Otherwise, a default string message that more tools responses are needed


### Required Keys
- **engine**
- **roomId**
- **toolId**
- **toolName**

### Optional Keys
- **toolExecutionResponse**
- **tool_execution_response**
- **parentMessageId**
- **toolParameterValues**

### Usage
```javascript
AddPlaygroundToolExecution(engine=[<engine>], roomId=[<roomId>], toolId=[<toolId>], toolName=[<toolName>], toolExecutionResponse=[<toolExecutionResponse>], tool_execution_response=[<tool_execution_response>], parentMessageId=[<parentMessageId>], toolParameterValues=[<toolParameterValues>])
```


---

## ReloadProjectProperties

### Required Keys
- **project**

### Usage
```javascript
ReloadProjectProperties(project=[<project>])
```

